/**
 * ESLint для Mini App — одно правило: порядок хуков (DRF-2388).
 *
 * До этого файла конфигурации ESLint здесь не было **вовсе**, и CI линтер не
 * гонял. Поэтому нарушение порядка хуков дожило до пилота (DRF-2374:
 * `useMemo` после условного `return`) — его некому было поймать: `tsc` такие
 * вещи не видит, а vitest замечает только если сломанную ветку кто-то
 * отрисует.
 *
 * Замер на 24.09 (411 файлов): **8 нарушений в 3 файлах**. То есть найденный
 * глазами случай был не единственным и не самым крупным.
 *
 * ### Почему ровно одно правило
 *
 * Предмет листа — правило хуков и факт отсутствия линтера. Стилевые правила
 * сюда не тащатся: они дали бы сотни замечаний на существующем коде, и
 * настоящий сигнал утонул бы в них с первого дня.
 *
 * ### Почему ошибкой, а не предупреждением
 *
 * Предупреждение, которое всегда горит, учит его не замечать — это тот же
 * несуществующий линтер, только с иллюзией контроля. `rules-of-hooks` ловит
 * класс ошибок, который иначе находят глазами и поздно, и ложных
 * срабатываний у него практически нет: нарушение порядка хуков — дефект
 * всегда.
 *
 * ### Чего этот файл НЕ делает
 *
 * `react-hooks/exhaustive-deps` намеренно не включён: он про полноту списка
 * зависимостей, у него есть ложные срабатывания, и это отдельный предмет
 * (DRF-2391 — там же 15 мёртвых подавлений именно этого правила). Плагин
 * версии 7 привозит ещё и правила компилятора React — они тоже не включены.
 * Здесь включено ровно то, что названо.
 *
 * ### Почему версии прибиты точно
 *
 * `eslint`, плагин и парсер стоят без `^`, в отличие от остальных зависимостей.
 * Это калитка: патч-версия правила не должна менять то, что краснеет, — иначе
 * задание однажды покраснеет или позеленеет без единой правки в коде.
 */
import tsParser from "@typescript-eslint/parser";
import reactHooks from "eslint-plugin-react-hooks";

/**
 * Файлы, где правило пока выключено поимённо. **Список может только
 * сокращаться.**
 *
 * Это не послабление правила: оно остаётся ошибкой везде, а долг назван
 * поимённо и виден в конфигурации, а не растворён в предупреждениях. Новый
 * код нарушить правило уже не может.
 *
 * Добавлять сюда файл — **только через техлида**. Иначе список за месяц
 * станет местом, куда складывают неудобное, и правило превратится в
 * украшение.
 *
 * * `FoodScannerResultScreen.tsx` — 6 нарушений, лист **DRF-2390**. Механически
 *   не переносится: все шесть хуков читают `result`, а выше раннего возврата
 *   `if (!result) return null` его нет. Значит надо решить, что они считают при
 *   отсутствии результата, — это правка поведения клиентского экрана, и её
 *   нельзя делать под линтер.
 */
const RULES_OF_HOOKS_DEBT = ["src/screens/FoodScannerResultScreen.tsx"];

export default [
  {
    ignores: ["dist/**", "coverage/**", "node_modules/**"],
  },
  {
    // Расширения перечислены шире, чем в коде сегодня (`.ts`/`.tsx`) намеренно:
    // компонент, добавленный однажды как `.jsx` или `.mts`, иначе остался бы
    // непроверенным, а задание — зелёным. Это тот же класс дыры, ради которого
    // лист и заводился. Набор файлов задаётся ЗДЕСЬ и только здесь: задание
    // `lint` зовёт `eslint .` и своего списка не держит.
    files: ["src/**/*.{ts,tsx,mts,cts,js,jsx,mjs,cjs}"],
    // Доклад о неиспользованных `eslint-disable` выключен намеренно: в коде их
    // 26, и все мёртвые — они писались для линтера, которого не существовало
    // (11 на `no-console`, 15 на `react-hooks/exhaustive-deps`). Двадцать шесть
    // постоянных предупреждений учили бы ровно той слепоте, против которой
    // заводился линтер. Их судьба — DRF-2391: сначала решение, потом уборка.
    linterOptions: { reportUnusedDisableDirectives: "off" },
    languageOptions: {
      parser: tsParser,
      parserOptions: {
        ecmaVersion: "latest",
        sourceType: "module",
        ecmaFeatures: { jsx: true },
      },
    },
    plugins: { "react-hooks": reactHooks },
    rules: {
      "react-hooks/rules-of-hooks": "error",
    },
  },
  {
    files: RULES_OF_HOOKS_DEBT,
    rules: {
      "react-hooks/rules-of-hooks": "off",
    },
  },
];
