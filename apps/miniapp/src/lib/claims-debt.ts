/**
 * Размер слепого пятна механизма утверждений — числом (DRF-2347).
 *
 * Механизм (`claims.ts`) видит только те сообщения, что идут через примитив
 * вывода с признаком `claim`. Всё остальное — собственная разметка экрана,
 * свои тосты, свои строки состояния — для него невидимо.
 *
 * Названный предел без размера читается как мелочь. Поэтому он измерен: ниже
 * число мест, где экран говорит о СДЕЛАННОМ мимо примитива. Сторож
 * (`claims-debt.test.ts`) держит его точным в обе стороны — новое место
 * краснит, исправленное тоже (долг нужно уменьшить, иначе он врёт).
 *
 * Долг — не «потом когда-нибудь». Это перепись: пока число не ноль, сторож
 * класса на этой поверхности неполон, и видно, насколько.
 */

/**
 * Слова, которыми экран сообщает о выполненном действии. Список грубый
 * нарочно: лучше поймать лишнее и снять руками, чем пропустить утверждение.
 * Он же — основа будущего разбора при переводе экранов на примитив.
 */
export const CLAIM_WORDS = [
  "отменен",
  "отменён",
  "сохранен",
  "сохранён",
  "удален",
  "удалён",
  "опубликован",
  "отправлен",
  "передал",
  "записал",
  "добавлен",
  "применен",
  "применён",
  "создан",
  "обновлен",
  "обновлён",
  "готово",
] as const;

/**
 * Сколько строк-утверждений живёт мимо примитива на 23.09.2026.
 *
 * Замер: `src/**` без тестов, строковые литералы, содержащие слово из
 * :data:`CLAIM_WORDS` **без учёта регистра** (первый, черновой замер шёл с
 * учётом и дал 57 — число вышло меньше настоящего ровно на заглавные буквы в
 * начале фразы: «Отменена», «Готово»). Ни одна из этих строк сегодня не несёт
 * признака `claim` — признака до этого листа не существовало.
 *
 * Для соразмерности: экранов (не тестов) в приложении 79, примитив вывода
 * `Snackbar` используют 21 место. То есть пятно — не мелочь на полях.
 */
export const CLAIMS_OUTSIDE_PRIMITIVE_DEBT = 86;

/** Сколько файлов их держит. */
export const CLAIMS_DEBT_FILES = 37;
