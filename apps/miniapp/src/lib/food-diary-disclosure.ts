/**
 * Раскрытие дневника питания — Z9 (решение владельца).
 *
 * # Почему текст продублирован здесь, а не импортирован
 *
 * Python и TypeScript не делят модуль. Поэтому копия есть — и ровно
 * поэтому её стережёт тест сверки поверхностей: Gate A требует, чтобы
 * текст **совпадал на всех поверхностях**, а расхождение двух копий —
 * самый тихий из возможных дефектов (никто не падает, просто людям в
 * чате и в приложении обещают разное).
 *
 * Источник истины — `apps/consent/food_diary_disclosure.py`. Правка идёт
 * туда первой; сюда — тем же движением, и версия поднимается ВМЕСТЕ с
 * текстом в обоих файлах.
 *
 * # WORKING PRODUCT COPY
 *
 * До прохождения Privacy/Legal review этот текст не является окончательным
 * legal disclosure. Поверхность живёт за флагом
 * `FOOD_DIARY_CANONICAL_CONSENT`, выключенным по умолчанию. Снятие
 * ограничения — не действие исполнителя.
 */

/**
 * Версия раскрытия — псевдоним константы половины 1 (`food-scanner.ts`),
 * которая, в свою очередь, обязана совпадать с `apps/consent/nutrition.py`.
 * Своего значения здесь нет намеренно: две записи одной версии расходятся
 * молча, и тест паритета в `apps/consent/tests/test_food_diary_disclosure.py`
 * проверяет, что литерал в этом файле не появился.
 */
export { FOOD_DIARY_CONSENT_DOCUMENT_VERSION as FOOD_DIARY_DISCLOSURE_VERSION } from "./food-scanner";

/** Заголовок экрана согласия. */
export const DISCLOSURE_HEADLINE = "Дневник питания: что я делаю с данными";

/**
 * Шесть обязательных утверждений (список владельца), по одному на абзац.
 *
 * Порядок не косметический: сначала ЧТО обрабатываем, потом ЗАЧЕМ фото,
 * потом СКОЛЬКО оно живёт, потом ЧТО хранится отдельно, и лишь затем —
 * право на отзыв и его последствие.
 *
 * Шестой абзац разводит **запись о еде** и **исходную фотографию**
 * намеренно. Отзыв — граница БУДУЩЕЙ обработки: после него ни данные
 * дневника, ни снимок в новую обработку не идут. Запись и посчитанное по
 * ней уходят по установленному порядку удаления, а срок снимка держит
 * хранилище, и мы его не ускоряем. Обещать немедленное стирание на стороне
 * приложения, пока его нет в коде, запрещено прямо (решение владельца) —
 * это тот же дефект, что прежнее «удаляю фото сразу после распознавания».
 */
export const DISCLOSURE_BODY: readonly string[] = [
  "Я веду твой дневник питания: записываю еду и напитки — и то, что ты " +
    "пишешь текстом, и то, что присылаешь фотографией.",
  "Фотографию я использую, чтобы распознать блюдо и посчитать примерные " +
    "калории и БЖУ.",
  "Фотография может храниться до 30 дней в хранилище сервиса " +
    "распознавания.",
  "Запись о еде хранится отдельно от самой фотографии.",
  "Согласие можно отозвать в любой момент.",
  "После отзыва я больше не использую для новой обработки ни данные " +
    "дневника, ни фотографию. Записи дневника и то, что по ним посчитано, " +
    "удаляются по установленному порядку. Саму фотографию я не стираю " +
    "немедленно — её срок держит хранилище, о котором сказано выше.",
];

/** Кнопки — дословно по решению владельца. */
export const BUTTON_GRANT = "Разрешить";
export const BUTTON_DECLINE = "Не сейчас";
