/**
 * Vitest setup для тестов БЕЗ браузера (DRF-2389).
 *
 * Лёгкий двойник `setup.ts`. Тяжёлый вариант тянет матчеры `jest-dom` и
 * `cleanup` из React Testing Library — обоим нужен DOM, и обоих нет смысла
 * поднимать там, где DOM не поднимается вовсе. Замер 24.09.2026: общий
 * `setup` стоит ~0,73 с рабочего времени на файл (три файла под node: 1,61 с
 * с ним против 0,779 с без), а на списке из 46 файлов — около 34 с.
 *
 * Почему файл существует, а не просто «setup отключён для node»:
 * разделяя setup на два, легко получить то, ради чего его никто не делил, —
 * пустой для всех. Пусть у каждого окружения будет свой явный файл, и пусть
 * сторож (`nodeEnvironmentList.guard.test.ts`) требует, чтобы в DOM-варианте
 * матчеры и `cleanup` оставались на месте.
 *
 * Здесь намеренно НЕТ ничего, кроме этого объяснения: любой общий приём,
 * добавленный сюда, станет невидимой разницей между двумя наборами тестов.
 */

export {};
