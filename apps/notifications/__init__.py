"""Notification preferences + future notification policy.

Bundle B / item 3 backend (master-mobile §M7). Currently houses the
per-master toggle + quiet-hours model used by the M7 Mini App screen.
Forward-looking: consumer-side notification dispatchers (DM scheduler,
morning brief / evening summary, urgent-channel gating) will read from
this app's services in subsequent PRs.
"""
