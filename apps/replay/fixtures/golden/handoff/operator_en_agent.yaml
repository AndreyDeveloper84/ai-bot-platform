name: handoff_operator_en_agent
description: English "agent" keyword — handoff trigger.
input:
  channel: max
  text: Can I speak with an agent?
must_pass:
  - skill_used: human_handoff
voice_check:
  max_length: 600
  caps_lock: 0.5
expected_action_type: human_handoff
