name: anketa_slash_command
description: Slash command launches the anketa flow.
input:
  channel: max
  text: "/anketa"
must_pass:
  - response_contains_any:
      - пол
voice_check:
  max_length: 400
  caps_lock: 0.5
