name: water_cappuccino_specific
description: Specificity — капучино beats generic кофе.
input:
  channel: max
  text: капучино
must_pass:
  - response_contains_any:
      - капуч
      - 250
voice_check:
  max_length: 400
  caps_lock: 0.5
