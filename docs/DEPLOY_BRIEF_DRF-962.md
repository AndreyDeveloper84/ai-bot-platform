# БРИФ ДЕПЛОЯ — DRF-962 на пилотный контур

**Дата брифа:** 2026-08-09
**Автор:** Chief Architect agent (главное окно)
**Для:** отдельное окно-исполнитель (Opus-оркестратор) ИЛИ владелец вручную
**Статус:** ожидает GO владельца (деплой = изменение пилотного runtime)

---

## 1. Цель (одно предложение)

Довести пилотный контур BOT до dev-baseline ≥ `699639c` и провести live-приёмку сценария «поиск мастера → тап карточки → запись», после чего DRF-962 можно закрывать как Done.

## 2. Контекст — что уже сделано, что нет

| Факт | Статус | Evidence |
|---|---|---|
| Фикс DRF-962 (сервис-контекст в handoff discovery→booking) смержен в `dev` репо `ai-bot-platform` | VERIFIED | squash `699639c`, PR #1159; ревью 2 раунда, P0/P1 = 0; CI зелёный |
| На пилотном хосте задеплоен | `e6460bd` (БЕЗ фикса) | VERIFIED на 08.08 по Linear-evidence — **перепроверить на хосте перед деплоем** |
| Live-баг «Контекст записи устарел» при выборе мастера | воспроизводился владельцем на проде до фикса | причина устранена в `699639c` |

**Правило NO FALSE SUCCESS:** merge ≠ release. DRF-962 остаётся In Progress до успешной live-приёмки.

## 3. Пилотный контур (куда деплоим)

- Хост: `api-dev.gobeauty.site` (194.87.99.126), пользователь по ssh — как в прошлых деплоях.
- Репо на хосте: `/home/taximeter/ai-bot-platform-dev` (INFERRED из Linear-evidence прошлых деплоев — проверить).
- Docker compose project: `ayla-bot-staging`; nginx проксирует на `:8014`.
- Флаги пилота: `BOOKING_VIA_AYLA_REST=ON`; EventBus активирован 08.08 (4 топика booking.*/appointment.rescheduled).
- Пилотный тенант: formula-tela, UUID `b32a057a-56c7-4bf0-ae50-e11e76ab44be`.
- **Backend (`beautygo_backend`) НЕ деплоим и НЕ трогаем.** Конфиги EventBus/env НЕ менять — фиксу они не нужны.

## 4. Процедура (bundle-workflow, как в прошлых деплоях)

1. **Зафиксировать точку отката:** на хосте `git -C /home/taximeter/ai-bot-platform-dev rev-parse HEAD` → записать (ожидаем `e6460bd…`). Это rollback-SHA.
2. Локально: `git fetch origin dev` в клоне `ai-bot-platform`, убедиться что `origin/dev` содержит `699639c` (`git merge-base --is-ancestor 699639c origin/dev`).
3. Собрать bundle: `git bundle create drf962.bundle e6460bd..origin/dev` (если старый SHA на хосте отличается — от фактического).
4. `scp drf962.bundle` на хост → в репо: `git fetch /path/to/drf962.bundle dev:refs/tmp/drf962 && git checkout <SHA=699639c или новее из origin/dev>` (детач или fast-forward ветки dev — как принято в прошлых деплоях).
5. Пересобрать и перезапустить: `docker compose -p ayla-bot-staging build && docker compose -p ayla-bot-staging up -d` (уточнить фактический compose-файл/скрипт на хосте — если есть deploy-скрипт из прошлых итераций, использовать его).
6. Health: контейнеры Up/healthy; логи воркеров без tracebacks за первые 2–3 мин; Redis Streams consumer живой.

## 5. Live-приёмка (критерий Done для DRF-962)

Диалог в MAX с глобальным ботом, реальные данные пилота:

1. «ищу массаж» → бот уточняет.
2. «Пенза, спортивный» (или фактические город/услуга пилотного мастера formula-tela).
3. **Карточка мастера содержит название услуги** (формат `• {Мастер} · {Услуга} · {Город}`), callback кнопки — трёхчастный (`cb:discover:book:{tenant}:{master}:{service}`).
4. Тап карточки → **НЕТ** «Контекст записи устарел» → date picker → выбор слота → подтверждение → запись создана (backend отвечает 201; событие `booking.created` в outbox/EventBus).
5. Негативный кросс-чек: тап по карточке без однозначной услуги (если воспроизводимо) → бот честно просит услугу и перечисляет реальные услуги мастера — не тупик.

**Если приёмка падает** — НЕ чинить на хосте руками; откат (п.6), лог + скрин в DRF-962, возврат в главное окно.

## 6. Откат

`git checkout <rollback-SHA из п.4.1>` → rebuild → up -d → health-check. Поведение вернётся к состоянию до деплоя (баг вернётся — это ожидаемо и приемлемо, пилот ещё не запущен).

## 7. Evidence и Linear (обязательно)

- DRF-962 (issueId `cd788ae2-2c59-421c-b8c6-4d394683fb5b`): комментарий с задеплоенным SHA, датой/временем, результатом live-диалога (текст реплик бота или скрины). Только после успеха — статус Done.
- DRF-945 (issueId `f3fe6429-c1b6-4796-8626-ef1423b9aecd`): короткий комментарий «runtime-acceptance DRF-962 пройдена/не пройдена».
- Доступ к Linear: ключ в `C:\Users\user\PycharmProjects\Ayla\.mcp.json` (env `LINEAR_API_KEY`), прямой GraphQL, Authorization без Bearer.

## 8. Границы

- Только репо `ai-bot-platform`, только пилотный контур. Никаких изменений кода, миграций БД в этом деплое нет (фикс — чистый код + тесты).
- DRF-963 (разговорность) и DRF-957 (питание) — НЕ в этом деплое.
- Любое отклонение от брифа (другой SHA на хосте, незнакомый деплой-скрипт, красный health) → остановиться и вернуть вопрос владельцу/главному окну.
