# Выкладка на пилот 25.08 — 16 коммитов

**Пилот на `91ec947`, `dev` на `e51bd89`.** Миграций между ними **нет** — проверено `git diff --name-only 91ec947 origin/dev -- '*/migrations/*.py'`, пусто. Отсутствие шага `migrate` в деплое (DRF-1047) в этот раз не мешает.

**Мини-апп трогается** — 22 файла в `apps/miniapp/`. Значит нужны обе выкладки: контейнеры и статика.

## 1. Бот

```bash
ssh taximeter@194.87.99.126
cd /home/taximeter/ai-bot-platform-dev
git checkout dev && git pull --ff-only origin dev
git rev-parse --short HEAD          # ожидаем e51bd89

docker compose -p ayla-bot-staging \
  -f docker-compose.yml -f docker-compose.staging.yml -f docker-compose.staging.local.yml \
  up -d --build --no-deps web worker shadow-worker celery-worker celery-beat
```

## 2. Мини-апп — отдельно, это статика, а не контейнер

```bash
./infra/deploy/miniapp-release.sh
```

`npm ci`, не `npm install` — намеренно. 23.08 на хосте стоял `react-router-dom` 6.30.4 при запиненном 6.30.3, и людям отдавали сборку с версией, которой репозиторий никогда не пинил.

Откат — переброс симлинка, без пересборки: `./infra/deploy/miniapp-release.sh --rollback`

## 3. Доказательство, что доехало

**Не по дате файла.** Пересборка двухнедельного чекаута обновляет все отметки времени и выглядит свежей — именно так двенадцать дней прошли незамеченными.

Снаружи, по HTTPS:

```bash
B=$(curl -s https://miniapp-dev.gobeauty.site/ | grep -oE 'index-[A-Za-z0-9_-]+\.js' | head -1)
curl -s "https://miniapp-dev.gobeauty.site/assets/$B" | grep -c "customer/records"
```

Ждём **ненулевое** — #1299 перевёл кнопку «Открыть запись» на канонический `/customer/records/:id`.

## 4. Что проверить живьём после выкладки

| проверка | ожидание |
|---|---|
| написать боту «спасибо большое» | **не** уходит в скрининг здоровья (#1298) |
| написать «болгарский перец» | тоже не уходит — закрыт остаток класса |
| каталог в мини-аппе | блок «Ayla подобрала» **отсутствует** — оснований бэкенд не отдаёт (#1299) |
| запись → «Открыть запись» | ведёт на `/customer/records/:id`, не на `/my-visits/:id` (#1299) |
| список бесед у мастера | номер, набранный клиентом в тексте, **отредактирован** (#1300) |

Последнее — главное: это решение владельца DRF-1039, и до сегодня номер доезжал дословно.

## Что НЕ выложено и почему

- **#1296** (резолвер пишет выбор консьержа) — `ci:failure` на mypy, `Any | None` там, где ждут `str`. Настоящий дефект, у окна голоса.
- **#1288** — конфликтует, ждёт ребейза окна записи.
- **#1301** (услуги без исполнителей) — конфликтует после мержа #1299.
- **#1302–#1306** — прогоны ещё идут.
