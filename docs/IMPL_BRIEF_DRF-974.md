# БРИФ — DRF-974 «Backend: SpecialistService formula-tela = декартово произведение 4×58»

**Дата брифа:** 2026-08-09
**Автор:** Chief Architect agent (главное окно)
**Для:** отдельное окно-исполнитель (Opus-оркестратор). Прочитай файл целиком до первого действия.
**Критический путь пилота:** DRF-974 → чистка миррора (DRF-967, GO-гейт) → повторная live-приёмка DRF-962 → DRF-945.

---

## 0. Правила обмена и отчётности (обязательно)

- Отчёты (итоговые и промежуточные) пиши в `C:\Users\user\PycharmProjects\Ayla\docs\REPORT_DRF-974.md`, секциями с датой. Не полагайся на пасту в чат.
- Эскалации владельцу — туда же, секцией «Эскалация».
- Задачи в Linear создаёт ТОЛЬКО главное окно — кандидатов фиксируй в отчёте. Linear-доступ тебе не нужен: снапшоты и апдейты делает главное окно; всё, что должно попасть в Linear, помечай в отчёте «→ в Linear».

## 1. Цель (одно предложение)

Убрать из backend-канона тенанта formula-tela ложную решётку «каждый из 4 мастеров выполняет все 58 услуг»: найти, как она возникла (в т.ч. не создаёт ли backend рёбра автоматически), привести данные к реальным компетенциям мастеров и дать сигнал для чистки миррора бота.

## 2. Решение владельца (зафиксировано 09.08.2026)

**«Мастера делают только свои услуги»** — решётка 4×58 официально признана дефектом данных. Вариант «все мастера универсальны» отклонён.

## 3. Контекст (VERIFIED, диагностика окна DRF-967 на хосте 09.08.2026)

- Backend по specialist-services endpoint отдаёт для тенанта formula-tela (UUID в боте: `b32a057a-56c7-4bf0-ae50-e11e76ab44be`) полную решётку: 4 специалиста × 58 услуг = 232 ребра, все `is_active=True`, снапшот `complete=True`. Мастера: Архипкин Денис, Сазонова Инна, Татьяна Паламарчук, Тихонова Ольга.
- Миррор бота идентичен этому канону (232=232, расхождений 0) — бот ни при чём, источник решётки выше.
- Потребитель на стороне бота: `apps/catalog/services/http_client.py:258` (`fetch_specialist_services`) в репо `ai-bot-platform` — по нему видно, какой endpoint и формат ожидается (читать можно в `C:\Users\user\PycharmProjects\ai-bot-platform-drf967`, НИЧЕГО там не менять).
- Следствие решётки: discovery-фильтрация по услуге в боте инертна, гард неоднозначности DRF-962 срабатывает почти всегда, запись через discovery не работает. Блокирует пилот.

## 4. Что НЕИЗВЕСТНО главному окну (UNKNOWN — твой шаг 1)

- Где живёт backend: какой репозиторий/сервис отвечает на api-dev.gobeauty.site (specialist-services endpoint). Локального клона backend-репо на этой машине НЕ найдено (есть legacy `mysite`/formula_tela и мобильный клиент — это не то).
- Как backend задеплоен на хосте (compose-проект? отдельный?), где его код и БД.

**Шаг 1 — идентификация (read-only):** ssh `taximeter@194.87.99.126` (доступ владелец давал окну DRF-967 — если пермишен спросит, владелец подтвердит). Посмотри nginx-конфиг для api-dev.gobeauty.site (upstream), `docker ps` / compose-проекты, найди сервис и код backend. Зафиксируй в отчёте: репо/путь, способ деплоя, где БД. До выяснения — никаких изменений.

## 5. План работ

### 5.1. Диагностика данных (read-only)
В backend-БД/шелле: когда и как созданы 232 ребра SpecialistService (created_at кучные? created_by? миграция/сигнал/скрипт?). Гипотеза для проверки: авто-создание ребра на каждую услугу при заведении специалиста (или bulk-заведение при онбординге салона).

### 5.2. Код: есть ли авто-создание
Найди в backend-коде создание SpecialistService. Если есть автоматика «специалист → все услуги» — это дефект кода, чинить (создание без услуг или по явному списку). Если автоматики нет — дефект только в данных, код не трогать.

### 5.3. Реальная раскладка «мастер → услуги» (вход от владельца)
Владелец предоставит реальные компетенции 4 мастеров (файл `docs/formula-tela-master-services.md`; если файла нет к моменту готовности — эскалируй в отчёт и жди). До получения раскладки данные НЕ менять.

### 5.3-bis. Дополнение (09.08, вечер): раскладка получена — обязательная методика маппинга
Файл `docs/formula-tela-master-services.md` выложен владельцем. ВАЖНО: названия в нём неформальные и НЕ совпадают 1:1 с каноническими названиями 58 услуг каталога (пример: «Классичесий массаж (Релакс, расслабяющий)» против каталожных «Классический массаж» и «Классический массаж задней поверхности тела»; «Лазерная эпиляция → Подмышки/Ноги полностью» может соответствовать нескольким позициям; «Ольга Тихонова» = «Тихонова Ольга»). Поэтому:
1. Построй таблицу маппинга «строка раскладки → каноническая услуга (или список услуг) из 58» с пометками уверенности; неоднозначности и услуги без исполнителя (например, RF-лифтинг, если его нет ни у кого) — отдельным списком.
2. Таблицу целиком включи в план для GO-гейта §5.4 — владелец подтверждает ИМЕННО её, не только счётчики. Ничего не угадывать молча: спорный маппинг = вопрос в плане, а не тихое решение.
3. Ожидаемый и допустимый исход: у большинства из 58 услуг не будет ни одного исполнителя — это честно (каталог шире компетенций салона), discovery по ним вернёт пусто/честный ответ.

### 5.4. GO-гейт владельца (обязательная остановка)
Правка данных backend = мутация пилотного runtime. Перед применением запиши в отчёт план: сколько рёбер удаляется/остаётся по каждому мастеру, способ (админка/скрипт/management-команда), бэкап удаляемого — и дождись GO.

### 5.5. Применение и верификация (после GO)
Привести данные к раскладке (с бэкапом). Верифицировать: specialist-services endpoint отдаёт реальные наборы (не решётку); `complete=True` сохраняется. Записать в отчёт до/после.

### 5.6. Сигнал для чистки миррора
В отчёт: секция «ГОТОВО ДЛЯ DRF-967» — сигнал главному окну, что можно активировать GO-гейт чистки миррора в окне DRF-967 (бот-сторона: удаление 232 бессмертных строк + ручной синк — НЕ твоя работа, не делай).

## 6. Границы

- НЕ трогать: репо `ai-bot-platform` (только чтение), контейнеры/данные бота (`ayla-bot-staging`), EventBus-конфиги, флаги.
- Миграции схемы backend — только через эскалацию.
- Любая мутация данных/кода на хосте — только после GO (§5.4).
- Если backend-код требует фикса авто-создания: ветка/PR по принятому в том репо процессу, планка — независимое ревью, P0/P1=0; деплой backend-фикса — отдельным решением владельца (опиши в отчёте, что потребуется).

## 7. Эскалация

Любое из: backend не идентифицируется уверенно; данные правятся только миграцией; авто-создание вплетено в онбординг так, что фикс ломает другие тенанты; нет раскладки от владельца — **стоп**, секция «Эскалация» в `REPORT_DRF-974.md`, вернуть вопрос владельцу/главному окну.

## 8. Linear (справочно, доступ не нужен)

DRF-974 (High, блокирует DRF-945, related DRF-967/DRF-962), label «NOW · Controlled Pilot». Связанные: DRF-967 (чистка миррора, PR #1161), DRF-975 (неаудируемый bulk-путь в боте — НЕ твой объём), DRF-962 (приёмка ждёт).
