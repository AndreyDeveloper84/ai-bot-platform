# Инвентарь DRF-1331 и три вопроса #1555 — 11.09.2026

Сабагент окна ЦЕЛИ И ПЛАН по брифу `docs/BRIEF_SUB_INVENTORY_1331.md`. Только чтение: Linear не менялся, код не писался, `ssh` не использовался.

**Базы (после `git fetch origin dev`, 11.09):**

```
ai-bot-platform   origin/dev  d5e3c75e3775eb2587ba1eef3ad3c143447412ef  07:16 MSK  §134–§138 (#1564)
beautygo_backend  origin/dev  e26e873104e4c1e421bf03a0a69b818bc6907cf0  07:03 MSK  purge §120 (#327)
ayla-knowledge    origin/main 207eeb638580e529aaff66e0d1412aa6b559c0a3  (для DRF-1332/1333 — канон)
```

Реестр решений читался из `git show origin/dev:docs/OPEN_DECISIONS.md` (9265 строк, до §148). Linear — через MCP `get_issue` / `list_issues(parentId=DRF-1331)` / `list_comments` по всем одиннадцати детям, эпику и DRF-1349.

---

## 1. Эпик DRF-1331

`Backlog`, приоритет Urgent, оценки нет, комментариев нет, `updatedAt` 24.08. Детей 11, сумма оценок детей **36 SP**. Статусы детей: Done 8 · In Progress 1 · Backlog 2. «Не начат» в Linear — про эпик, не про детей: восемь закрыты 01.09 пакетом (03:44–03:46 UTC).

Общее для всего среза — три решения владельца, которых в тексте эпика (24.08) нет:

* **§35 п.10 (06.09)**: «Вес и замеры в пилоте не используются… до отдельного согласия, настоящих данных и понятной пользы». **§35 п.13**: прогресс цели не строить до пилотных данных; на пилоте один экран «Моя цель» без шкал.
* **§112 (10.09)**: «Не создавать ещё одну сущность Goal. `ClientGoal` + `DesiredOutcome` → ОДИН authoritative Goal domain. И только после него: Goal → PlanEligibility → optional Plan… К реализации Plan не переходить, пока не закрыты Goal Domain Contract и DecisionReadiness».
* **§133 (10.09)**: Controlled Pilot обязан содержать Goals и «Plan Lite» — связный путь Goal → следующий шаг → Recommendation → Booking.

Следствие: срез «снижение веса» целиком **вне пилота** (§35 п.10), а то, что в пилоте обязано быть (Plan Lite, §133), **в эпике не представлено ни одним ребёнком**. Ни одно из решений не отменяет трек — они переносят его за пилот и ставят перед ним Goal Domain Contract.

## 2. Одиннадцать детей

Категория — по §138 (ИСПОЛНЕНО / ЗАПЕРТО / ОТМЕНЕНО / ПОДЛЕЖИТ). Все greps — по `origin/dev` (бот `d5e3c75e`, каталог `e26e8731`), рабочие копии не читались.

| # | ID | Название (сокр.) | Linear | SP | Вердикт | §138 | Довод |
|---|---|---|---|---|---|---|---|
| 1 | DRF-1332 | Канонизировать норму «действие ≠ доказательство результата» (В-5) | In Progress | 2 | **живо** | ПОДЛЕЖИТ | Комментарий 25.08: «внесено в канон, `e1b39be`, AYLA-DEC-0082 + amendment LDT §9». Проверка: `git merge-base --is-ancestor e1b39be origin/main` → **no**; коммит только на `origin/drf-1148/canon-under-version-control` (3 впереди / 10 позади main). На `origin/main` LDT `version: "1.1"`, а `AYLA-DEC-0082` занят другим решением — «BOT-001 First Contact Q1–Q3» (`OWNER_DECISION_REGISTER.md:1330`). **Коллизия ID.** Норму владелец велел канонизировать первой; §97/§112 её не отменяют. |
| 2 | DRF-1333 | Узкий consent scope для persistent wellness observation (В-1) | Backlog | 3 | **живо** | ПОДЛЕЖИТ | Драфт `docs/CONSENT_SCOPE_DRAFT_WELLNESS_OBSERVATION.md` есть на `origin/dev` бота; передан в Privacy/Legal 25.08 с четырьмя вопросами — ответа в задаче нет (последний комментарий 25.08). В канонe: `git grep -ci wellness_observation origin/main` → **0**; `health_related_signal` по-прежнему в запрете (`Consent Scope Registry.md:159`). §92/§99 дали два согласия питания (дневник / персональный расчёт) — это согласие на расчёт в `NutritionProfile`, не на ряд наблюдений; §35 п.10 прямо требует «отдельного согласия». Блокирует включение записи (Gate O). |
| 3 | DRF-1334 | Plan Adherence по плановому обязательству | Done | 3 | **живо** | ЗАПЕРТО | Код: `wellness/adherence.py:68 compute_plan_adherence`, `wellness/models.py:362 PlanAction`, `wellness/fact_providers.py:45 count_facts` (миграция `0002_planaction`). Итога по плану нет — «нет процента, суммы или цвета» (докстринг), что согласуется с §35 п.13. Вызывающих вне модуля и тестов: `git grep compute_plan_adherence origin/dev` → **0**. Построено, никем не читается. |
| 4 | DRF-1335 | Имя — Personal Plan; `CareEpisode` не использовать (В-6) | Done | 1 | **живо** | ИСПОЛНЕНО | `git grep CareEpisode origin/dev -- apps/` (бот) → 0; то же по каталогу → 0; `care plan` в коде обоих → 0. Glossary v2.2 — по комментарию, на `origin/main` канона не проверял (см. §5). Решение исполнено «по построению». |
| 5 | DRF-1338 | Гейт «бот пишет первым» — два согласия | Done | 3 | **живо** | ИСПОЛНЕНО | `apps/notifications/proactive.py:146 consent_blocker(..., required_consents=None)`, `:129 "no_health_consent"` в `BLOCK_REASONS`, `:137 DEFAULT_BLOCK_REASONS`. Единственный вызов с обоими согласиями — `apps/wellness_proactive/tasks.py:270`. Сделано как описано. |
| 6 | DRF-1339 | Нормы считаются от выдуманных 70 кг, маркер допущенных входов | Done | 3 | **живо** | ИСПОЛНЕНО (строже) | Маркер `assumed_input` **снят** вместе с подстановкой: `nutrition/services/nutrition_profile_service.py:40` — `DEFAULT_WEIGHT_KG` закомментирован, `:51-55` «маркер это признание, а не отказ… теперь пропуск отменяет расчёт: `insufficient_inputs`»; `profile_upsert_service.py:305 "assumed_inputs": []` всегда пуст. Основание — §85/§103 (ориентир отсутствует как `NULL`). По §108 «строже — оставляем». Цель ребёнка достигнута иным средством; сам его артефакт в коде отсутствует намеренно. |
| 7 | DRF-1340 | Допуск weight-loss плана поверх принудительного `maintain` | Done | 3 | **живо** | ЗАПЕРТО | `wellness/admission.py:78 personal_plan_admission`; `nutrition.*` не импортирует (докстринг `:11`, grep по файлу — 0). **Устаревшая предпосылка:** вторая ветка отказа (`ASSUMED_WEIGHT_MARKER in verdict.assumed_inputs`, `:104-105`) мертва — список всегда пуст (см. #6), а новое состояние `insufficient_inputs` модуль не читает: `git grep -c insufficient_inputs origin/dev -- wellness/` → **0**. Вызывающих вне тестов → **0**. |
| 8 | DRF-1341 | Вес перезаписывается; ряда наблюдений нет | Done | 5 | **живо** | ЗАПЕРТО | `wellness/models.py:207 ProgressObservation`, `:315 EvidenceRegistryEntry`; writer `wellness/services.py:122 record_observation` → всегда `blocked_pending_privacy_legal` через `body_observation_gate` (`:88-103`), сторож `wellness/tests/test_fail_closed.py`. Вызывающих `record_observation` вне модуля → 0. Пометка «мержить нельзя» в тексте задачи снята комментариями 25.08 (GOALS-R5 §5–6). §35 п.10 держит запись вне пилота. |
| 9 | DRF-1342 | Desired Outcome и Personal Plan не существуют в коде | Done | 5 | **живо** | ЗАПЕРТО | `wellness/models.py:23 DesiredOutcome`, `:93 PersonalPlan`, `:132 PlanOutcomeLink`; writer `record_outcome` → всегда `scope_not_approved` (Gate D). Создателей `PersonalPlan` вне тестов/миграций → 0; API — один `GET` (`wellness/urls.py:7 me-wellness-context`). **REFRAMED §112 / STALE PREMISE §118:** `DesiredOutcome` подлежит слиянию с `ClientGoal` в один Goal domain, а Plan — только после Goal Domain Contract. Не «отменено»: владелец велел слить, а не удалить. |
| 10 | DRF-1343 | Outcome Progress отделён от Adherence, старт не из питания | Done | 5 | **живо** | ЗАПЕРТО | `wellness/progress.py:102 compute_outcome_progress`, импорты только `.models` (`:33`), четыре состояния (`:40-43`), чисел наружу нет; читается одним местом — `wellness/context_read.py:55 progress_state` → в бот. §35 п.13 запрещает показывать прогресс на пилоте — код состояние отдаёт, показа в боте нет (см. #11). |
| 11 | DRF-1344 | Повод «внести текущий вес» (OBSERVE) | **Backlog** | 3 | **живо** | ЗАПЕРТО | **Статус Linear устарел:** код слит 29.08 — `dcc16629 feat(wellness_proactive): DRF-1344 … (#1319)`, `apps/wellness_proactive/tasks.py:91 OCCASION_FAMILY = "OBSERVE"`, гейты `:270 consent_blocker(required_consents=…)`. Рубильник `config/settings/base.py:1573 WELLNESS_PROACTIVE_ENABLED` по умолчанию `false`. Ни комментария о мерже, ни перевода в Done. |

**Итог: живо 11 · отменено 0 · нет предмета 0.** Из них ИСПОЛНЕНО 3 (#4, #5, #6), ЗАПЕРТО 6 (#3, #7, #8, #9, #10, #11), ПОДЛЕЖИТ 2 (#1, #2). Ни одно решение владельца 10–11.09 не противоречит ребёнку прямо; три ребёнка несут устаревшую предпосылку (#7 — маркер, #9 — вторая сущность Goal, #11 — статус).

## 3. Чего не хватает — без чего срез «снижение веса» не собирается

Порядок — по тому, что стоит впереди по §112, а не по цене.

1. **Goal Domain Contract (§97 OD-GOAL-A…F, §112).** Задачи в Linear не нашёл: `list_issues(query="Goal Domain Contract")` и `query="ClientGoal"` за 14 дней — ни одной с таким предметом. Код на `origin/dev` каталога решения не отражает: `goals/models.py:71 is_active = BooleanField`, `:88 clientgoal_one_active_per_client`; `PAUSED|ACHIEVED|ARCHIVED|LEGACY_INACTIVE` в `goals/` → 0 попаданий. Вердикт замера `docs/MEASUREMENT_GOAL_DOMAIN_AUTHORITY_CURRENT.md` §15 — **NOT READY**, три позиции R1–R3 открыты. По §112 без этого к Plan не переходить.
2. **Слияние `ClientGoal` + `DesiredOutcome` (§112).** Задачи нет. `DesiredOutcome` живёт в `wellness` (0 строк по комментарию 25.08), `ClientGoal` — в `goals`; перекрёстных ссылок нет: `git grep 'ClientGoal' origin/dev -- wellness/` → 1 попадание, и то в докстринге `models.py:10`.
3. **Связь питание ↔ цель.** `git grep -E 'ClientGoal|DesiredOutcome|wellness' origin/dev -- nutrition/` → 1 файл, ложное попадание (`deficit_recommendations.py:9` «general wellness»). DRF-1623 (In Progress, 40 SP) это зафиксировал: «Питание → цель: пути нет… проектировать до OD-GOAL-*/OD-PLAN-* нельзя». §112 задаёт форму: Nutrition через evidence, не напрямую в Goal progress. Актуально на обеих головах.
4. **Consent scope в Registry и ответ Privacy/Legal.** См. #2 таблицы: 0 попаданий в каноне, четыре вопроса юристам без записанного ответа. Без этого Gate O не открывается, и все шесть ЗАПЕРТО остаются заперты.
5. **Создатель плана и поверхность.** Ни одного пути создать `PersonalPlan`/`DesiredOutcome` (writers fail-closed, callers 0), ни одного экрана: `git grep -li 'personal_plan\|PersonalPlan' origin/dev -- apps/` (бот, без тестов) → 0 файлов. **Plan Lite по §133 — обязательная часть пилота — в эпике отсутствует как задача.**
6. **Канон В-5 не на `main`** (таблица #1): норма, на которую ссылаются `wellness/adherence.py` («AYLA-DEC-0082») и DRF-1343, на `origin/main` канона под этим ID означает другое решение.
7. **Допуск не знает об отказе от расчёта** (таблица #7): после §103 профиль может не иметь норм вовсе (`insufficient_inputs`), и `personal_plan_admission` такой профиль пропустит как `admitted`, если `goal_overridden_by` пуст.
8. **Данные тела на пилоте.** §120/§144 — удалить весь набор анкеты у шести профилей; команда `nutrition/management/commands/purge_unconsented_body_parameters.py` слита сегодня (#327, голова `e26e8731`). Исполнена ли на пилоте — не проверял (без `ssh`, п. 5 брифа). До исполнения `NutritionProfile.weight_kg` у четверых — незаконное основание для любого стартового веса (DRF-1343 его и так не читает).
9. **Linear не отражает состояние:** DRF-1344 Backlog при слитом коде; эпик Backlog при 8/11 Done; в текстах #8–#10 «мержить нельзя» (снято только комментариями). По §118 у #7 и #9 должна стоять пометка STALE PREMISE / REFRAMED — её нет.

## 4. PR #1555 — три вопроса владельцу

`docs/BREAKDOWN_DRF1329_FOOD_DIARY.md`, коммит `80e38d51` 11.09 06:59 MSK (03:59 UTC). Документ на `origin/dev` уже в редакции «Обновлено 11.09»: §3 хранит вопрос вместе с ответом. Реестровые §134–§138 слиты **через 17 минут** (`d5e3c75e`, #1564) — в промежутке документ ссылался на ещё не существующие параграфы; сейчас ссылки верны.

**Вопрос 1 (дословно заголовок): «Имена происхождения для текстового пути».** Предложено `text_estimated_confirmed` / `text_user_corrected`; третьей пары для голоса не предложено.
→ **ОТВЕЧЕН — §136**: четыре значения приняты дословно; голос — канал ввода `voice`, обрабатывается как текст, без своей пары; «канал и происхождение — разные оси».

**Вопрос 2: «Тридцать суток отменены? И от какого момента считать сутки?»** (`FOOD_SCANNER_DECISION.md:65,184` — 30 суток; §132 — 24/72 ч; щель «извлечение удалось, человек не ответил»).
→ **ОТВЕЧЕН — §134**: §132 отменён явно, единый TTL 30 суток **с момента загрузки**, шесть правил (удаление кодом, счётчик неудалившихся, продлевать нельзя).

**Вопрос 3: «Что, кроме изображения, попадает под срок»** (`FoodScan.raw_response`, `nutrition/models.py:72`; разбор `:53-56`).
→ **ОТВЕЧЕН — §135**: сырой ответ живёт те же 30 суток, удаляется вместе с фото и временными результатами; добавлен запрет выводов о теле из сырого ответа.

**Открытых из трёх — ноль.** Проверка зеркал:

* `docs/OPEN_DECISIONS.md` — все три записаны как решения (§134–§136), в разделе «открыто» их нет.
* **DRF-1349** — последний комментарий 10.09 13:07 UTC («11 решений CLOSED»); в нём п. 11 по-прежнему гласит **24 ч / 72 ч** — редакция §132, отменённая §134. Ни §134–§136, ни сами три вопроса в DRF-1349 не появлялись (`updatedAt` задачи `2026-09-10T13:07:48Z`). То есть вопросы прошли через реестр в файле, минуя правило самого реестра («ни один вопрос не считается заданным, пока он не в этом файле» — описание DRF-1349), и зеркало в Linear несёт отменённое решение как действующее.

## 5. Что не смог проверить и почему

* **Боевой контур**: число строк в шести таблицах `wellness`, исполнение purge §120, значения флагов `WELLNESS_PROACTIVE_ENABLED`/`FOOD_PHOTO_SCAN_ENABLED` на пилоте — бриф запрещает `ssh` и обращения к 176.119.159.141 / 194.87.99.126. «0 строк» цитируется из комментариев 25.08 и в срок годности не укладывается.
* **Ответ Privacy/Legal** на четыре вопроса DRF-1333 — не нашёл ни в Linear (комментарии DRF-1333, DRF-1349), ни в реестре §92–§148. Отсутствие в этих местах ≠ отсутствие ответа.
* **Glossary v2.2 (DRF-1335)** и **Consent Scope Registry** проверялись по `ayla-knowledge` `origin/main`, которого в брифе нет; читал только `git grep`/`git show`, рабочую копию не трогал. Сама ветка `drf-1148/canon-under-version-control` могла быть заменена другим путём внесения — я проверил только `merge-base`.
* **Полный текст замеров** `MEASUREMENT_NUTRITION_GOAL_PLAN_INTEGRATION_CURRENT.md` (91 КБ) и `MEASUREMENT_G2_TRACK_A_CLIENTGOAL.md` (81 КБ) не читал целиком — брал §13–§15 замера авторитета цели и описание DRF-1623.
* Голова канона `ayla-knowledge` — `origin/main`; если действующей считается `canon/consolidated`, коллизию AYLA-DEC-0082 надо перепроверить там.

## 6. Убрано за собой

Временные файлы лежали только в scratchpad сессии (`OD.md`, `BD1329.md` — выгрузки `git show`); удалены после написания этого документа. В репозиториях ничего не менялось, `git status` обоих не трогался.
