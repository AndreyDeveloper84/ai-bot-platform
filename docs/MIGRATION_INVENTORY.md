# Опись переноса пилота на новую машину

Составлена 03.09.2026 по живому осмотру `194.87.99.126`.

## Зачем

Диск текущей машины отвечает на запись за **3.5 секунды** (`w_await=3512 мс`, `%util` 80–116% при 0.5 записи/с). Это ограничение хостинга, не нагрузка: наши контейнеры потребляют доли процента CPU.

Следствия, наблюдавшиеся 01–03.09: пересборка образа **3.5 часа и зависание**, перенос контекста по 20 КБ/с, обрывы `git fetch`, первый запрос к бэкенду 20 с.

**Мы третий день не можем выложить готовый код.** Не написать и не проверить — выложить.

---

## 1. ЧТО ПЕРЕНОСИМ

### 1.1. Бот — проект `ayla-bot-staging`

Каталог: `/home/taximeter/ai-bot-platform-dev`

| Контейнер | Образ |
|---|---|
| `ayla-bot-staging-web-1` | свой |
| `ayla-bot-staging-worker-1` | свой |
| `ayla-bot-staging-shadow-worker-1` | свой |
| `ayla-bot-staging-celery-worker-1` | свой |
| `ayla-bot-staging-celery-beat-1` | свой |
| `ayla-bot-staging-postgres-1` | `postgres:16-alpine` |
| `ayla-bot-staging-redis-1` | `redis:7-alpine` |
| `ayla-bot-staging-minio-1` | `minio/minio:latest` |
| `ayla-bot-staging-chromadb-1` | `chromadb/chroma:0.5.20` — **в состоянии `Created`, не запущен** |

**Данные:**
- `ayla-bot-staging_postgres_data` — БД бота
- `ayla-bot-staging_minio_data` — файлы

**Настройки:** `.env.staging` + **22 файла** `.env.staging.bak-*`. Содержат токены ботов MAX, ключ Anthropic, учётные данные прокси. **Переносить руками, не через git.**

Набор compose-файлов: `docker-compose.yml`, `docker-compose.staging.yml`, `docker-compose.staging.local.yml`. Имя проекта `ayla-bot-staging` — **указывать явно** (`-p`), иначе compose возьмёт имя каталога и уйдёт в чужой стек.

### 1.2. Бэкенд — проект `dev`

Каталог: `/home/taximeter/beautygo/dev`

| Контейнер | Образ |
|---|---|
| `dev-web-1` | свой |
| `dev-celery_worker-1` | свой |
| `dev-celery_beat-1` | свой |
| `dev-db-1` | `postgres:16-alpine` |
| `dev-redis-1` | `redis:7-alpine` |
| `dev-minio-1` | `minio/minio:latest` |

**Данные:**
- `dev_postgres_data` — **живые данные пилота**: 9 мастеров, записи, учётные записи приглашённых людей. Размер БД `beautygo` — **7.5 МБ** (замер 02.09).
- `dev_minio_data`, `dev_redis_data`

**Настройки:** `.env` + резервные копии. Один compose-файл: `docker-compose.yml`, имя проекта `dev`.

Выкладывается **сам** при merge в `dev` — на новой машине понадобится перенастроить секрет `VPS_HOST` в GitHub.

### 1.3. nginx и домены

Наши: `api-dev.gobeauty.site`, `miniapp-dev.gobeauty.site`, `proapp.gobeauty.site`.

`miniapp-dev` раздаёт `apps/miniapp/dist` **прямо из рабочей копии** — не из контейнера. На новой машине нужен тот же приём либо иной, но осознанно выбранный.

Сертификаты: `/home/taximeter/cerbot`.

---

## 2. ЧТО НЕ ПЕРЕНОСИМ

**Чужой проект `proff58_staging`** — 8 контейнеров, свои БД, веб, очередь, nginx, тома `proff58_staging_*`. Не наш. Именно он делит с нами диск.

**Осиротевший стек `ai-bot-platform-dev`** — 4 контейнера, поднят 32 часа. Существовал до нас; 01.09 я по ошибке добавил в него два контейнера (снял). Разобраться, чей он, и **не тащить**.

**Прочее на машине:** `chromadb-dev`, `admin-2`, `business_markets`, `har20`, `itsolve-legacy`, домены `bosfor`, `dev.proff58.ru`, `formula_tela`, `mysite-stage`, `test`.

---

## 3. ПОРЯДОК

1. **Новая машина**: docker, каталоги, пользователь. ~1 ч
2. **Секреты** — руками, по списку из §1. ~1 ч, спешить нельзя
3. **Код** — `git clone` обоих репозиториев на нужные коммиты
4. **Данные** — дамп/восстановление обеих БД + тома minio. Минуты: базы крошечные
5. **Сборка образов** — на SSD **10–15 минут вместо 3.5 часов**
6. **nginx + сертификаты** для трёх доменов. ~1.5 ч
7. **Секреты GitHub** — `VPS_HOST` и ключ развёртывания на новый адрес
8. **Проверка живого пути** до переключения DNS. ~1 ч
9. **DNS** — переключить A-записи. Минуты + распространение
10. **Старая машина остаётся поднятой** до подтверждения

**Итого: рабочий день спокойной работы.**

---

## 4. РИСКИ

**Токены ботов MAX.** Если домены сохраняются — перенастройка не нужна. Проверить до переключения.

**Данные пилота живые.** Там записи и учётные записи приглашённых людей. Дамп делать при остановленной записи либо принять окно.

**Секреты в 23 файлах.** Легко потерять один и обнаружить это через сутки.

**Обратный ход** — возврат DNS, полчаса, пока старая машина жива.

---

## 5. ЧТО ПОЧИНИТСЯ САМО

- Пересборка образа: **3.5 ч → 10–15 мин**
- `git fetch` перестанет рваться
- Первый запрос к бэкенду: **20 с → доли секунды**
- Пропадёт конкуренция за диск с чужим проектом
