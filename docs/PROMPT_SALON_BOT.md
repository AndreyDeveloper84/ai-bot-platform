Ты — окно-исполнитель эпика DRF-1061: салонный бот, отдельная поверхность для исполнителей. Работа крупная — раскладывай её на своих сабагентов сам.

ПЕРВЫМ ДЕЛОМ прочитай целиком:
  C:\Users\user\PycharmProjects\Ayla\docs\IMPL_BRIEF_SALON_BOT.md   — твой бриф
  C:\Users\user\PycharmProjects\Ayla\docs\WINDOW_PROTOCOL.md        — правила окна
  C:\Users\user\PycharmProjects\Ayla\docs\REPORT_SALON_OPS_AUDIT.md — аудит от 14.08, фактическая база

РАБОЧИЙ КАТАЛОГ
  Заведи СВОЙ worktree бота. НЕ занимай ai-bot-platform-drf1032, -drf1033,
  -drf1035, -drf1062 — там работают четыре других исполнителя. Проверь
  занятость перед стартом: git -C <путь> status.

ГЛАВНОЕ

  • Админка салона УЖЕ написана и развёрнута — Mini App бота: /admin/team,
    /admin/services, /admin/availability-requests, /master/dashboard,
    /master/schedule. Твоя задача не построить её, а ОТКРЫТЬ В НЕЁ ДВЕРЬ.
  • Заперта она пустотой: TenantStaff — 0 строк, CatalogMaster.linked_bot_user —
    0 из 4. Роль вычисляется в apps/identity/services/role_resolver.py:229-300,
    поэтому все 12 пользователей резолвятся как customer.
  • Приложения Ayla Pro в рабочем виде НЕ существует — две из четырёх вкладок
    заглушки. Планов вокруг него не строить.

ЧТО УЖЕ ГОТОВО К ТВОЕМУ ПРИХОДУ

  • Бот зарегистрирован владельцем, токен лежит на пилотном хосте в
    .env.staging как SALON_BOT_TOKEN. В репозиторий его не переносить.
  • Адрес вебхука тот же, что у клиентского бота:
    https://api-dev.gobeauty.site/api/v1/ingress/max/
    Отдельный не нужен — боты различаются ПО ТОКЕНУ внутри запроса.
  • Механизм нескольких ботов в коде уже есть: GLOBAL_BOT_TOKENS (множественное
    число), is_global_bot_token() в apps/ingress/services.py:79, разведение по
    токену на два пути. Тебе нужна третья ветка, а не новый транспорт.
    ПРОВЕРЬ это первым делом — от объёма зависит план.

РАЗГРАНИЧЕНИЕ С ОКНОМ РАСПИСАНИЯ (DRF-1062), оно работает параллельно
  Бэкенд: User с телефоном + TUR role=admin — делает ОНО, у него в объёме.
  Бот:    TenantStaff и CatalogMaster.linked_bot_user — делаешь ТЫ.
  Это две разные системы, обе нужны. Не дублируй чужую половину и не оставляй
  свою. Границу двигать — только через меня.

ПОРЯДОК
  Сначала верни план (пять разделов, §6 брифа), код после согласования.
  Развилки, требующие владельца, эскалируй; технические решения принимай сам
  с обоснованием.

ШИНА
  Отчёт: docs/REPORT_SALON_BOT.md — веди правками.
  Ответы: docs/REPLY_SALON_BOT.md — я пишу сверху. Поставь монитор; маркер md5
  снимай ПОСЛЕ отработки секции, иначе пропустишь ответ.

ГРАНИЦЫ
  Мутации Linear — только главное окно. Merge и деплой — только с разрешения.
  Клиентского бота не трогать: детектор, воронка записи, клиент записей — там
  работают три окна.
  Классы доказательности обязательны: VERIFIED / INFERRED / CLAIMED / UNKNOWN.
  Секреты не печатать. На диске ~5 ГБ — лишних окружений не разворачивай.
  КОММИТЬ ПРОМЕЖУТОЧНОЕ каждые несколько шагов: 14–15.08 пять исполнителей
  подряд оборвались на сбоях среды, коммит в своей ветке переживает обрыв.
  PR открывать ПРОТИВ dev, не против main — сегодня PR против main собрал
  1029 файлов вместо трёх. Проверяй сразу: gh pr view <N> --json baseRefName.

КРИТЕРИЙ ГОТОВНОСТИ
  Владелец открывает салонного бота, вводит код приглашения и становится
  администратором «Формулы тела». После этого у него открывается уже
  построенная админка и он видит свою команду. Проверяется живым проходом.
