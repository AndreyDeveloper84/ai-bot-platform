# ОТВЕТ главного окна → окну DRF-1004

## №2 (2026-08-12): GO НА ФАЗУ C — ДЕПЛОЙ РАЗРЕШЁН

**Владелец дал явное GO** (чат главного окна, 2026-08-12: «Go»). Разрешение:

1. **Деплой `1864488`** (merge-коммит PR #1164) на пилотный хост подготовленным bundle-способом: scp → fetch → checkout `186448896eb4235894916a2d7950768e2d637a15` → пересборка образов → `up -d` проекта `ayla-bot-staging`.
2. **Rollback-точка `0d94aac`** — откат при проблемах health-чеков предразрешён, без дополнительных согласований, с фиксацией в REPORT.
3. Health: `/healthz/` на `127.0.0.1:8014` → 200, статусы контейнеров, логи worker'а 5 минут. Известный шум (НЕ поломка): `worker.subscriber_audit`, `pii_protected_provider.no_active_scope`, `events.emit.non_canonical`.
4. **Smoke read-only — ключевая проверка этого деплоя:** в скоупе тенанта `b32a057a-56c7-4bf0-ae50-e11e76ab44be` вызвать `get_services()` и подтвердить: вернулось **58** услуг, среди них `a4f31641-8d1c-4dce-bd57-aae85b4e4ef8` («УЗ-кавитация — 1 зона»), **цена ненулевая**. Дополнительно — что `allowed_service_ids` на уровне скилла непусто. Мутаций данных пилота не делать, записей не создавать.
5. Зафиксируй в REPORT состояние очереди `ingress:max_global` (`XINFO GROUPS`, `pending`) до и после деплоя.
6. Затем секция **«ГОТОВО К LIVE-ПРИЁМКЕ»** со сценарием: услуга → мастер → дата → **время** → карточка подтверждения → ✅. Шаг «время» — тот самый, что валился с `unknown_service`; на нём внимание.

**Live-приёмку в MAX делает только владелец.**

---

## №1 (2026-08-11): работа принята; MERGE PR #1164 РАЗРЕШЁН; деплой — после GO владельца

**Отчёт и код приняты.** Я перечитал дифф на `3390b72` сам, а не по отчёту. Сделано ровно по брифу: канонический `catalog/salon-services/` с фильтрами `tenant`/`is_active`, честное падение без tenant-скоупа вместо пустого каталога, `base_price` с корректным fallback (сравнение через `is not None` — правильно, иначе нулевая цена уходила бы в легаси-ветку), обход страниц со сверкой `count` и явной ошибкой `catalog_incomplete`. Особо отмечу решение маппить `catalog_incomplete` в «сервис расписания недоступен», а не в stale-context: обрезанный каталог выглядел бы для пользователя ровно как чинимый дефект, и это была бы ловушка на будущее.

**Проверено мной дополнительно (то, что ты не мог проверить без backend):** `InternalSpecialistServiceViewSet.filterset_fields = ['tenant', 'specialist', 'salon_service', 'is_active']` (`services/internal_api.py:57-73`) — фильтр `specialist` поддерживается. Твой ⚠️ по апстрим-контракту снимаю: если бы `specialist` молча игнорировался, DRF вернул бы все 95 связок и per-specialist ветка пропускала бы чужие услуги. Не пропускает.

**CI зелёный (VERIFIED, `gh pr checks 1164`):** оба прогона `pytest + ruff + mypy` — pass, `replay fixtures` — pass. Флакер DRF-999 в этот раз не сработал.

**MERGE PR #1164 в `dev` — РАЗРЕШЁН, выполняй сам.** Обычный merge (**НЕ squash**). После merge:

1. Впиши в REPORT merge-SHA на `dev` и цитату финального `gh pr checks` (секция 3 сейчас `ОЖИДАЕТСЯ`).
2. Подготовь фазу C read-only: bundle `0d94aac..origin/dev`, `git bundle verify`, перепроверь rollback-точку на хосте.
3. **Деплой не начинай** — жду GO владельца, он придёт секцией «GO НА ФАЗУ C».

**Кандидаты в Linear (все четыре) регистрирую я, тебе делать ничего не нужно.** По второму замечу: контрактный док действительно отсутствует в репозитории бота — это не твой пробел, зафиксирую отдельной задачей.

---

## №0 (2026-08-11): файл создан, ответов пока нет

Работай по брифу `IMPL_BRIEF_DRF-1004.md`. Решения по эскалациям и GO на деплой (фаза C) появятся здесь новыми секциями **сверху**.

**Поставь persistent-монитор на этот файл** (md5, §2 протокола) и убедись, что он реально доставляет уведомления в твою сессию — у двух предыдущих окон монитор молча не работал, из-за чего они пропускали блокирующие требования. Cron-проверка md5 каждые 1–2 минуты работает надёжно.

Свой отчёт пиши в `docs/REPORT_DRF-1004.md` (создай сам). Пиши его **правками, а не полной перезаписью** — предыдущее окно дважды теряло содержимое отчёта.
