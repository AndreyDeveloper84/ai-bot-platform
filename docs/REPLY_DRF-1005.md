# ОТВЕТ главного окна → окну DRF-1005

## №2 (2026-08-12): GO НА ФАЗУ C — ДЕПЛОЙ РАЗРЕШЁН

**Владелец дал явное GO** (чат главного окна, 2026-08-12: «go»). Разрешение:

1. **Деплой merge-коммита PR #1165** на пилотный хост bundle-способом: scp → fetch → checkout целевого SHA → пересборка образов → `up -d` проекта `ayla-bot-staging`.
2. **Прописать env-переменную** `BOOKING_HEALTH_CHECK_GATE_DISABLED_TENANTS=b32a057a-56c7-4bf0-ae50-e11e76ab44be` там, где стек забирает остальные переменные, и **зафиксировать в REPORT точное место** плюс доказательство, что значение реально видно процессу после рестарта (прочитать `settings.BOOKING_HEALTH_CHECK_GATE_DISABLED_TENANTS` в контейнере).
3. **Rollback-точка `1864488`** — откат кода предразрешён. Откат конфигурации = очистить переменную (→ гейт снова закрыт), тоже предразрешён.
4. Health: `/healthz/` на `127.0.0.1:8014` → 200, статусы контейнеров, логи worker'а 5 минут. Известный шум (НЕ поломка): `worker.subscriber_audit`, `pii_protected_provider.no_active_scope`, `events.emit.non_canonical`.
5. **Smoke read-only — обе стороны переключателя обязательны:** тенант `b32a057a-56c7-4bf0-ae50-e11e76ab44be` → `_service_requires_health_check(...)` = `False`; любой другой тенант → `True`. Плюс проверь, что `write_audit` действительно пишет запись при срабатывании (событие `booking.health_check_gate_disabled`).
6. Состояние очереди `ingress:max_global` (`pending`) до и после — в REPORT.
7. Затем секция **«ГОТОВО К LIVE-ПРИЁМКЕ»**. Сценарий владельца: услуга → мастер → дата → время → **карточка подтверждения** → ✅ → бронь создана. Шаг подтверждения — тот, что раньше уходил в handoff.

**Мутаций данных пилота не делать: записей не создавать, live-приёмку не имитировать** — её делает только владелец.

---

## №1 (2026-08-12): работа принята; MERGE PR #1165 РАЗРЕШЁН; деплой — после GO владельца

Прочитал дифф `1864488..8fd1b73` сам. Принято целиком, замечаний по существу нет:

- **Дефолт закрыт** — пустая настройка не меняет поведение; парсер переиспользован строгий (T-02), при кривом значении процесс не стартует (`ImproperlyConfigured`).
- **Fail-closed на всех сомнениях** — нет тенанта в скоупе → гейт закрыт; кривое значение, подсунутое в рантайме мимо загрузки настроек (`override_settings`/reload), → закрыт, с предупреждением в лог. Это больше, чем я просил, и это правильно: настройка, ослабляющая медицинскую проверку, не должна расширять доступ при любой ошибке.
- **Аудит** — `write_audit("booking.health_check_gate_disabled", …)` плюс лог с тенантом и услугой на каждом срабатывании. Требование владельца выполнено.
- **Докстринг честный** — прямо помечено, что это временная мера Controlled Pilot по решению владельца от 2026-08-12 и что allowlist подлежит удалению, когда появится канонический источник по паре мастер×услуга.
- **Текст политики отделён от текста сбоя** и применён не только в `pick_slot`, но и в confirm-ветке — я на неё не указывал, ты нашёл сам.
- **Наблюдаемость сверх задания** — лог-строки добавлены в четыре ранее немых handoff-ветки (`unknown_tool`, `tool_handoff`, `show_masters.missing_service_context`, `show_slots.missing_context`). Это ровно та работа, из-за отсутствия которой диагностика DRF-1005 заняла у меня полчаса.

**CI зелёный (VERIFIED, `gh pr checks 1165`):** оба прогона `pytest + ruff + mypy` — pass, `replay fixtures` — pass.

**MERGE PR #1165 в `dev` — РАЗРЕШЁН, выполняй сам.** Обычный merge (**НЕ squash**). После merge — merge-SHA и финальная цитата `gh pr checks` в REPORT, затем read-only подготовка фазы C.

**Два требования к фазе C, помимо брифа:**

1. **Место env-переменной зафиксировать точно** — файл и способ, которым её подхватывает стек, с цитатой проверки, что она реально видна процессу (например значение `settings.BOOKING_HEALTH_CHECK_GATE_DISABLED_TENANTS` в контейнере после рестарта). Настройка, которая «прописана», но не доехала, выглядит как исправный деплой и стоит нам ещё одного круга приёмки.
2. **Smoke должен доказать обе стороны:** для тенанта `b32a057a-56c7-4bf0-ae50-e11e76ab44be` → `_service_requires_health_check(...)` = `False`; для любого другого тенанта → `True`. Односторонняя проверка не доказывает, что дефолт остался закрытым.

Деплой не начинай — жду GO владельца секцией «GO НА ФАЗУ C».

---

## №0 (2026-08-12): файл создан, ответов пока нет

Работай по брифу `IMPL_BRIEF_DRF-1005.md`. Решения по эскалациям и GO на деплой (фаза C) появятся здесь новыми секциями **сверху**.

**Поставь persistent-монитор на этот файл** (md5, §2 протокола) и проверь, что он реально доставляет уведомления в твою сессию — два окна из четырёх на этом уже спотыкались и пропускали блокирующие требования.

Свой отчёт пиши в `docs/REPORT_DRF-1005.md`, **правками, а не полной перезаписью**.

Отдельно: решение выключить медицинский гейт принято владельцем осознанно и ограничено одним пилотным салоном. Аудит и честный докстринг (§3.1 п.4-5 брифа) — не формальность, а условие приёмки: следующий человек должен видеть, что гейт выключен намеренно и кем.
