# ОТВЕТ главного окна → окну DRF-1029

## №6 (2026-08-13): GO УЖЕ ЛЕЖИТ НИЖЕ — читай секцию №5 и начинай деплой

Твой cron docладывает «изменений нет», и это не сбой шины: ты записал эталонный md5 (`dc4f09b8…`) **после** того, как я вписал секцию №5 с GO. То есть маркер снят с файла, уже содержавшего разрешение, и сравнение с тех пор честно сравнивает GO сам с собой. Секцию №5 ты не читал ни разу.

Правлю файл специально, чтобы отпечаток сдвинулся и cron тебя поднял.

**Действуй по секции №5 ниже — она в силе целиком.** Кратко, чтобы не было разночтений:

- Деплой разрешён владельцем, деплоишь ты, за оба окна. Окно DRF-911 отправлено ждать и на хост не ходит.
- `origin/dev` = `4373bb547549d0e66b0ee91b4346a49fa105229a`, bundle `1ea47b9..4373bb5`, rollback-точка `1ea47b9`.
- `HANDOFF_NOTIFY_MAX_CHAT_IDS=-77378770496155` (операторская группа «IT Ayla»; **значение отрицательное, проверь, что минус доехал целым**).
- `HANDOFF_ADMIN_BASE_URL=https://api-dev.gobeauty.site`.
- Переменные обязаны быть видны в контейнере **worker**, не только в web.
- Канал я уже проверил: тестовая отправка в эту группу вернула 2xx, сообщение доставлено.

**И почини маркер на будущее:** снимай md5 *после* того, как прочитал и отработал секцию, а не в момент постановки проверки. Иначе следующий ответ снова окажется невидимым.

---

## №5 (2026-08-13): GO НА ФАЗУ C — ДЕПЛОЙ РАЗРЕШЁН. Деплоишь ты, за оба окна

**Владелец дал явное GO** (чат главного окна, 2026-08-13: «G деплой»). Оба follow-up-теста в `dev`, условие выполнено.

**Деплой делаешь только ты.** Окно DRF-911 я отправляю ждать — деплой один на оба коммита, два параллельных захода на живой контур недопустимы.

### Что деплоим

`origin/dev` = **`4373bb547549d0e66b0ee91b4346a49fa105229a`** — четыре коммита: DRF-911 (просмотр записей), DRF-1029 (уведомления), два follow-up-теста. Bundle `1ea47b9..4373bb5` → scp → checkout → пересборка → `up -d` проекта `ayla-bot-staging`.

**Rollback-точка `1ea47b9`** — откат кода предразрешён. Откат конфигурации = вернуть `.bak` и `up -d`, тоже предразрешён. Если `/healthz/` не 200 или контейнеры не поднимаются — откатывайся, не разбираясь на живом контуре.

### Переменные окружения

Сделай `.bak`-копию `.env.staging` до правки, затем добавь:

```
HANDOFF_NOTIFY_MAX_CHAT_IDS=-77378770496155
HANDOFF_ADMIN_BASE_URL=https://api-dev.gobeauty.site
```

`-77378770496155` — операторская группа «IT Ayla» в MAX (владелец завёл её сегодня, бот в ней состоит, 4 участника). **Значение отрицательное — проверь, что минус доехал** до контейнера целым и не был съеден парсингом CSV.

**Проверь переменные в контейнере `worker`, а не только в `web`.** Входящие сообщения обрабатывает worker, `create_admin_task` и твой `on_commit` выполняются там же. Если переменная окажется только в web — механизм будет выглядеть настроенным и молчать. Докажи чтением настроек Django внутри worker-контейнера.

**Канал уже проверен мной (VERIFIED):** тестовая отправка в эту группу через `botapi.max.ru/messages?chat_id=-77378770496155` вернула 2xx, сообщение доставлено. Если после деплоя уведомление не придёт — причина в конфигурации или во врезке, но не в канале и не в токене.

### Проверки после `up -d`

1. Web-контейнер перешёл в `Up (healthy)`, а не остался в `starting`/`unhealthy`.
2. `/healthz/` на `127.0.0.1:8014` → 200.
3. Логи worker'а 5 минут. Известный шум: `worker.subscriber_audit`, `pii_protected_provider.no_active_scope`, `events.emit.non_canonical`.
4. `df -h /` до и после, оба значения в REPORT. До деплоя было 26 ГБ свободно — запас есть, `docker builder prune -f` скорее всего не понадобится. **`docker system prune -a` и любые операции с томами запрещены** — там базы Postgres и MinIO обоих стеков.

### Границы на живом контуре

**Тенанту `b32a057a-56c7-4bf0-ae50-e11e76ab44be` задач и записей не создавать.** Теперь это особенно важно: с включёнными уведомлениями любая созданная задача уедет сообщением в группу владельца, где сидят люди. Smoke — read-only на синтетике. Живая бронь владельца от 14 августа («УЗ-кавитация») — только чтение.

### Затем

Секция **«ГОТОВО К LIVE-ПРИЁМКЕ»** в REPORT: короткий сценарий для владельца из двух шагов — спросить бота про свои записи и увидеть бронь на 14 августа; написать «позови менеджера» и получить карточку эскалации в группе «IT Ayla». Live-приёмку делает только владелец.

---

## №4 (2026-08-13): решение владельца — сначала тест, потом деплой. Начинай follow-up

Владелец решил (чат главного окна, 2026-08-13): **сначала закрываем тесты, потом один общий деплой**. GO на фазу C будет здесь же, после того как твой follow-up и follow-up окна DRF-911 окажутся в `dev`. Деплой поедет одним заходом на оба коммита, `HANDOFF_NOTIFY_MAX_CHAT_IDS` выдам вместе с GO.

Твоя часть — ответ №3 ниже, без изменений: ветка от свежего `origin/dev` (сейчас `b0849ed`), правится **только** `apps/handoff/tests/test_notify.py`, тест на `django_assert_num_queries(0)` вокруг `build_admin_task_notification`, доказательство недекоративности (временно добавить обращение к `task.bot_user.display_name`, показать красноту, убрать), PR, зелёный CI, merge.

**Перед merge перечитай этот файл.** Сегодня мы дважды разошлись во времени: ответ приходил в ту же минуту, что и merge. Ничьей вины тут нет, но проверка стоит десять секунд.

Пока деплоя нет — на пилоте `1ea47b9`, механизм уведомлений там отсутствует, переменные окружения пусты.

---

## №3 (2026-08-13): merge разошёлся со стопом во времени — претензий нет; тест доделать follow-up'ом

Merge PR #1170 прошёл в 04:38:12Z, мой стоп был написан в ту же минуту. Ты его физически не мог прочитать — монитор просто не успел. Вопрос закрыт, разбирать нечего; стоп из ответа №2 считай снятым.

**Что осталось — ровно один тест, follow-up'ом:**

1. Ветка от свежего `origin/dev` (merge-коммит `b0849ed`), правится **только** `apps/handoff/tests/test_notify.py`. Модуль `notify.py` не трогай — он в порядке.
2. Тест: `build_admin_task_notification(task)` внутри `django_assert_num_queries(0)`.
3. Докажи, что тест не декоративный: временно добавь в текст обращение к `task.bot_user.display_name`, покажи, что тест краснеет, убери. Красный-до/зелёный-после — в REPORT.
4. PR, зелёный CI, merge. Здесь merge по зелёному CI разрешаю сразу и без подтверждения: содержание PR — сам тест, разойтись нам не в чем.

Зачем — повторю коротко, чтобы было в отчёте: на пилоте `STRICT_TENANT_SCOPE` пуст, то есть режим `audit`, и тенантный queryset без активного контекста молча отдаёт пустоту вместо ошибки (`apps/tenancy/managers.py:193-206`). Колбэк `on_commit` может выполняться уже вне `tenant_scope`. Сегодня запросов нет только потому, что `task.tenant` закэширован при создании. Тест превращает везение в правило.

**Дальше — деплой.** Он общий: сначала follow-up DRF-911 (тест на частичный отказ), потом твой, и оба коммита едут на пилот одним заходом после GO владельца. Значение `HANDOFF_NOTIFY_MAX_CHAT_IDS` выдам здесь вместе с GO.

---

## №2 (2026-08-13): СТОП merge. Предразрешение снято до выполнения требования из №1

**Не мержи PR #1170**, даже по зелёному CI. Предразрешение из §0 брифа этим ответом отозвано: merge только после моего явного подтверждения здесь.

Причина: в PR тринадцать тестов и среди них нет требуемого в ответе №1 — на ноль запросов к БД при сборке текста. Ты открыл PR раньше, чем прочитал ответ; претензии нет, но требование в силе.

**Что сделать:** добавить в `apps/handoff/tests/test_notify.py` тест, оборачивающий `build_admin_task_notification(task)` в `django_assert_num_queries(0)`. Push в ту же ветку, CI перепрогонится.

Повторю зачем, потому что это не формальность. На пилоте `STRICT_TENANT_SCOPE` не задан — значит режим `audit`, а в нём тенантный queryset без активного контекста **молча возвращает пустоту** (`apps/tenancy/managers.py:193-206`). Колбэк `on_commit` в общем случае выполняется после выхода из `tenant_scope`. Сегодня текст собирается без запросов только потому, что `task.tenant` оказался закэширован при создании — это счастливое свойство, а не гарантия. Первый, кто добавит в уведомление `task.bot_user.display_name`, получит на проде пустую строку вместо имени, тесты у него будут зелёные, и никто не узнает. Тест на ноль запросов делает это правилом, а не везением.

**После merge — сразу проверь REPLY.** Оба окна сегодня уходили в PR и merge, не заглянув сюда; из-за этого требование к DRF-911 переехало в отдельный follow-up. Перед каждым необратимым шагом — merge, деплой — читай этот файл заново, монитор мог отстать.

---

## №1 (2026-08-13): реализация принята; один обязательный тест и данные по пилоту

Прочитал модуль целиком, не отчёт. Сделано по контракту: выключено по умолчанию, `on_commit`, широкое сдерживание исключений, изоляция получателей, таймаут константой и зафиксирован тестом, аудит на неудаче. Отдельно отмечу два решения, которых я не требовал: `on_failure`-хук сам обёрнут в try (провал аудита не отменяет остальных получателей) и `send_max_notification` вынесен channel-agnostic примитивом — DRF-1030 переиспользует его, а не напишет второй такой же. Обоснование «синхронно, не Celery» принято.

**Что проверил сам (VERIFIED, чтобы ты не тратил на это время):**

1. **Текст собирается без единого запроса к БД.** `task.tenant` закэширован в `fields_cache` при `AdminTask.objects.create(tenant=tenant, …)`, `conversation_id`/`created_at`/`task_type` — простые атрибуты. Это важнее, чем кажется: колбэк `on_commit` в общем случае может выполняться уже вне `tenant_scope`, а `STRICT_TENANT_SCOPE` на пилоте не задан, то есть действует режим `audit` — тенантный queryset без контекста **молча возвращает пустоту** вместо ошибки (`apps/tenancy/managers.py:193-206`). Запрос в этом месте не упал бы, а тихо отдал бы не то.
2. **Токен на пилоте есть.** `MAX_BOT_TOKEN` задан в контейнере worker'а, и `send_message` берёт именно его — тот же токен, которым бот отвечает владельцу. Значит уведомление придёт от знакомого бота, а не от постороннего. `HANDOFF_NOTIFY_MAX_CHAT_IDS` и `HANDOFF_ADMIN_BASE_URL` на хосте пусты — механизм сейчас выключен, как и задумано.

**Обязательное требование — зафиксировать пункт 1 тестом.** Оберни `build_admin_task_notification` в `django_assert_num_queries(0)`. Сейчас отсутствие запросов — счастливое свойство реализации, а не гарантия: любой следующий разработчик, добавивший в текст `task.bot_user.display_name`, получит на пилоте пустую строку вместо имени и ничего не заметит, потому что режим `audit` не ругается. Тест на ноль запросов превращает это в правило.

**Дальше:** расширенный регресс, коммит, push, PR в `dev`, зелёный CI, merge (обычный, НЕ squash). Ловушка docs-skip (DRF-1027) в чисто кодовом PR не сработает, но сверься через `gh run list --commit <sha>`, если увидишь красноту. Деплой — только после GO владельца; значение `HANDOFF_NOTIFY_MAX_CHAT_IDS` выдам здесь вместе с GO.

---

## №0 (2026-08-12): файл создан, ответов пока нет

Работай по брифу `IMPL_BRIEF_DRF-1029.md`. Решения по эскалациям и GO на деплой (фаза C) появятся здесь новыми секциями **сверху**.

**Поставь persistent-монитор на этот файл** (md5, §2 протокола) и убедись, что он реально доставляет уведомления.

Отчёт — `docs/REPORT_DRF-1029.md`, **правками, а не полной перезаписью**.

Три акцента:

1. **Худший исход задачи — сделать эскалацию хрупче, чем она была.** Сейчас передача диалога человеку работает надёжно, просто молча. Если из-за уведомления упадёт создание задачи или диалог зависнет между состояниями, мы разменяем работающий механизм на удобство. Best-effort здесь не пожелание, а требование.
2. **Consumer однопоточный.** Один зависший сетевой вызов останавливает весь пилот — это DRF-989, где обработка слова «привет» заняла 11 минут 32 секунды. Короткий таймаут и никаких ретраев в синхронном пути.
3. **Сообщение уходит наружу навсегда.** MAX — не наш контур: удалить оттуда ПДн по требованию субъекта мы не сможем. Поэтому в уведомлении ссылка на задачу, а не переписка и не телефон.

**Значение `HANDOFF_NOTIFY_MAX_CHAT_IDS` для пилота выдам здесь вместе с GO на фазу C** — в репозиторий его не коммить.
