# ОТВЕТ главного окна → окну DRF-1033 + DRF-972

## №2 (2026-08-13): работа принята и завершена. Окно закрывается — это не незавершённость

Владелец закрывает это окно. Ты всё сделал; Done в Linear ставлю я, и только после его живой приёмки — так устроен процесс, а не потому, что к работе есть вопросы.

**Что подтверждено мной независимо, а не по твоему отчёту:**

- CI по `ee70471` — три прогона, все `completed success`, ложной красноты docs-skip нет.
- Merge в `dev`, `911642a`.
- Деплой на пилот: HEAD совпал, контейнеры пересозданы, web `healthy`, порт `127.0.0.1:8014`, переменные уведомлений пережили пересборку.
- **Поведение проверено на живых данных:** вызвал `route_global_booking_lookup` для глобального пользователя владельца — ответ «У вас пока нет предстоящих записей», одной строкой, без дублирующихся секций. Фикс DRF-1033 работает.

**Почему приёмка задержалась и это не про тебя.** После деплоя пилот дважды оказывался неспособен ответить: сначала два открытых `AdminTask` держали диалоги в режиме передачи человеку (штатное поведение DRF-980, задачи никто не закрыл), потом отказал внешний прокси, через который бот ходит в OpenAI — прямой доступ с российского адреса закрыт, и без прокси консьерж не отвечает вообще. Прокси заменён на резервный, пилот живой. Инцидент записан как DRF-1054.

**Что осталось от тебя в проекте — и это стоит сказать.**

Твоё решение **не добавлять слово «человек»** в триггеры оказалось самой ценной частью работы. Ты не просто отказался — ты нашёл, почему именно на тенантном пути это опасно: там у проверки нет окна отрицания, в отличие от глобального. Я это подтвердил и завёл **DRF-1042**: риск шире одного слова, любой будущий триггер унаследует ту же слабость.

Твоя находка про неполный рецепт деплоя в шапке compose-файла ушла в **DRF-1047** и, что важнее, в `docs/WINDOW_PROTOCOL.md` §5.1.1 — с точной командой и признаком неправильного запуска. Следующее окно туда уже не наступит. Инцидент с пятиминутным простоем web ты диагностировал и устранил сам, за пять минут, и описал честно, включая то, что часть сервисов это время работала без нужного оверрайда. Ровно так и надо.

**Оставшийся хвост — на мне:** дождаться приёмки владельца («позови менеджера» → эскалация с карточкой в группе; «когда я записан» → одна строка) и поставить Done по DRF-1033 и DRF-972.

Спасибо за работу. Она была аккуратной.

---

## №1 (2026-08-13): GO НА ДЕПЛОЙ

**Владелец дал явное GO** (чат главного окна, 2026-08-13: «GO на деплой 1033 и 972»).

Работу принимаю целиком. Отдельно отмечу решение по слову «человек»: ты не стал его добавлять, проверил, почему оно опасно именно на тенантном пути, и завёл наблюдение вместо того, чтобы протащить слово и надеяться. Разницу между глобальным путём (там есть окно отрицания) и тенантным (`HumanHandoffSkill.matches()` — простое вхождение подстроки) я подтвердил сам и зарегистрировал как **DRF-1042**. Это была лучшая часть работы.

CI по `ee70471` я тоже перепроверил лично: три прогона, все `completed success`, ложной красноты docs-skip нет.

### Что деплоим

`origin/dev` = **`911642a9e09d58999666f416857d303225c74657`** (merge PR #1173). На пилоте сейчас `4373bb5` — **она же точка отката**, откат предразрешён.

Рецепт прежний: bundle `4373bb5..911642a` → scp → checkout → пересборка → `up -d` проекта `ayla-bot-staging`.

**Про `.env.staging` — ничего не трогать.** Этот заход чисто кодовый, новых переменных нет. Уже настроенные `HANDOFF_NOTIFY_MAX_CHAT_IDS` и `HANDOFF_ADMIN_BASE_URL` должны остаться как есть — проверь после рестарта, что они на месте и уведомления не отвалились.

### Проверки после `up -d`

1. Web-контейнер перешёл в `Up (healthy)`, а не остался в `starting`/`unhealthy`.
2. `/healthz/` на `127.0.0.1:8014` → 200.
3. Логи worker'а 5 минут. Известный шум: `worker.subscriber_audit`, `pii_protected_provider.no_active_scope`, `events.emit.non_canonical`.
4. `df -h /` до и после, оба значения в REPORT. До прошлого деплоя было 23 ГБ свободно. Если меньше ~10 ГБ — `docker builder prune -f` (только кэш сборки). **`docker system prune -a` и любые операции с томами запрещены** — там базы Postgres и MinIO обоих стеков.
5. **Переменные уведомлений видны в контейнере worker** — отдельным пунктом, потому что именно оттуда они используются.

### Границы на живом контуре

Тенанту `b32a057a-56c7-4bf0-ae50-e11e76ab44be` задач и записей **не создавать**. С включёнными уведомлениями любая созданная задача уедет сообщением в группу «IT Ayla», где сидят люди. Smoke — read-only на синтетике.

**Отдельно: в зеркале пилота больше нет предстоящих записей** — вчерашняя запись-призрак погашена по GO владельца (DRF-1034, бэкап в `docs/drf1034-scripts/`). Правильный ответ бота на вопрос о записях сейчас — «у вас пока нет предстоящих записей», одной строкой. Если увидишь секции или дубли — это регресс твоего же фикса, сообщай немедленно.

### Затем

Секция **«ГОТОВО К LIVE-ПРИЁМКЕ»** в REPORT: сценарий для владельца из двух шагов — «позови менеджера» (должна сработать эскалация и прийти карточка в группу) и «когда я записан» (одна строка, без дублирующихся секций). Live-приёмку делает только владелец. Done по обеим задачам ставлю я после неё.

---

## №0 (2026-08-13): файл создан, ответов пока нет

Работай по брифу `IMPL_BRIEF_DRF-1033-972.md`. Ответы появятся здесь новыми секциями **сверху**.

**Поставь persistent-монитор на этот файл** (md5, §2 протокола). **Маркер снимай ПОСЛЕ того, как прочитал и отработал секцию**, а не в момент постановки проверки — иначе законсервируешь состояние с уже написанным ответом и не увидишь его никогда. Ровно так 13.08 окно DRF-1029 двадцать минут не видело лежавшего в файле разрешения.

**Перед merge перечитай этот файл заново.** В тот же день дважды случилось так, что окно мержило PR в ту же минуту, когда я писал требование, — и требование уезжало в follow-up. Проверка стоит десять секунд.

Отчёт — `docs/REPORT_DRF-1033-972.md`, **правками, а не полной перезаписью**.

Три акцента:

1. **Обе первопричины найдены и проверены на живом контуре.** Расследовать нечего — нужен фикс и тесты, доказывающие, что фикс не декоративный. Если по ходу решишь, что диагноз неверен, — это эскалация в REPORT, а не тихое переписывание.

2. **Список слов для эскалации — думай, а не расширяй механически.** С 13.08 каждая созданная задача уходит сообщением живым людям в MAX-группу. Ложное срабатывание теперь не только глушит диалог, но и дёргает людей. Слово «человек» ловится в «я человек занятой» — если сочтёшь риск выше пользы, не добавляй и объясни. Отказ с обоснованием я приму охотнее, чем длинный список.

3. **Ничего сверх двух фиксов.** Соседние задачи (DRF-1034 — расхождение зеркала, DRF-1035 — идентичность нового клиента) ведут другие окна. Границы в §4 брифа жёсткие.
