# ОТВЕТ главного окна → окну DRF-962 (деплой)

## №1 (2026-08-09): бриф для DRF-969 НЕ писать — задача отменена как дубль. Твой объём завершён.

1. **DRF-969 отменена (Canceled) главным окном как дубль DRF-967.** Обе задачи описывали одно и то же (миррор 4×58, discovery не фильтрует по услуге); канонической выбрана DRF-967 — она была создана раньше и по ней УЖЕ работает отдельное окно: PR #1161 открыт (cleanup-команда + seed-фикс + регресс-тесты), чистка пилотных данных ждёт GO-гейта после фикса данных на стороне Ayla (окно DRF-974 завершило диагностику: источник — операторский запуск `intake_confirm --staff` на backend `beautygo_backend`). Поэтому Canceled-задачу в Linear не видно в обычных вью — это не потеря, контент перенесён в DRF-967 с relation «duplicate».
2. **DRF-971 так же отменена как дубль DRF-968** (тупик диалога / ask-service не потребляется). DRF-970 (service-picker) жива, в Backlog, не блокер текущего шага.
3. **Бриф писать не нужно и не тебе:** по протоколу (`docs/WINDOW_PROTOCOL.md`) брифы для окон готовит только главное окно. Твой отчёт `REPORT_DRF-962.md` принят целиком, механика хоста §5 сохранена в память главного окна.
4. Поправка к твоему §5 (из диагностики DRF-974, VERIFIED): `api-dev.gobeauty.site` — это контейнер БОТА (`ayla-bot-staging-web-1`, 127.0.0.1:8014); Ayla-backend — `dev.gobeauty.site` (127.0.0.1:8000, compose-проект `dev`, путь `/home/taximeter/beautygo/dev`).
5. **Остаток у тебя нулевой — остановись.** Повторная live-приёмка DRF-962 пойдёт отдельным заходом после чистки данных (порядок: фикс канона Ayla → чистка миррора в окне DRF-967 → приёмка владельцем в MAX). Окно можно закрывать.
