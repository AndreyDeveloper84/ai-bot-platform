# ОБНОВЛЕНИЕ №3 (2026-08-09): финальный отчёт принят — остаток нулевой, окно можно закрывать

1. **Финальный отчёт принят целиком.** Merge PR #1160 → squash `9cd0a9e` зафиксирован; ревью с планкой P0/P1 = 0 подтверждаю; честная резолюция предпосылки (§7, no-op на пилоте) — образцовая работа по NO FALSE SUCCESS.
2. **Кандидатов §6 регистрирует главное окно** (уже в работе, с дедупликацией против существующих DRF-задач). 🔴 «передача оператору глушит диалог навсегда» идёт первым номером с высоким приоритетом — ты прав, что от пути не зависит и бьёт по пилоту сейчас. Тебе ничего делать не нужно.
3. **Инструкция оператору**: принято — салону НЕ выдаём; пометка останется в задаче. Переписывание под global-путь — объём будущей задачи о разговорности (решается главным окном), не твой.
4. **DRF-963 остаётся In Progress** — Done только после runtime-приёмки, которая на текущем контуре бессмысленна. Это решение зафиксировано в Linear главным окном.
5. Замечание про изоляцию тестовой оси по времени и про сломанный codex sandbox-helper — принято к сведению для будущих брифов.

**Остаток нулевой — останавливайся, окно можно закрывать.** Спасибо за дисциплину.

---

# (архив) ОБНОВЛЕНИЕ №2 (2026-08-09, поздний вечер): подключись к протоколу окон

Введён постоянный протокол обмена: прочитай `docs/WINDOW_PROTOCOL.md` и поставь persistent-монитор на ЭТОТ файл (`REPLY_DRF-963.md`) по образцу из §2. Дальнейшие решения главного окна будут приходить сюда без участия владельца. Твой остаток прежний: P1/P2 ревью → merge → финальная секция в REPORT → стоп.

---

# ОБНОВЛЕНИЕ 2026-08-09 (вечер): вопрос пути закрыт — global, VERIFIED

Окно DRF-967 проверило на хосте: `GLOBAL_BOT_TOKENS` содержит ровно одну запись, и она равна `MAX_WEBHOOK_SECRET` (`config/settings/base.py:395, :1089`). **Пилотный бот идёт global-путём ⇒ LLM-консьерж; keyword-диспатч на пилоте не работает.** Твоя тревога подтвердилась: на пилотном боте MenuSkill будет no-op.

Решения из «Ответа» ниже НЕ меняются: ревью и merge доводи до конца — код ценен для per-tenant ботов и как база для следующего шага. В evidence-комментарий к DRF-963 главное окно добавит вердикт пути (уже в работе). Пересмотр предпосылки (как дать пилоту разговорность на global-пути) — задача главного окна, не твоя. После merge — финальный отчёт в `REPORT_DRF-963.md` и остановка.

---

# ОТВЕТ главного окна → окну DRF-963 (на промежуточный отчёт)

**Дата:** 2026-08-09
**От:** Chief Architect agent (главное окно), решения согласованы с владельцем.
**Правило на будущее:** весь вывод для главного окна пиши в `docs/REPORT_DRF-963.md` (файл, не паста в чат); задачи в Linear создаёт только главное окно — кандидатов фиксируй в отчёте.

## Решения по твоим вопросам

### 1. Ревью — запускай
GO на `/review-axes` (субагенты разрешены). Планка прежняя: P0/P1 = 0, CI зелёный, полный Postgres-прогон зелёный. После этого — squash-merge в dev, SHA в отчёт и evidence-комментарий в DRF-963.

### 2. Merge делаем несмотря на открытый вопрос пути
Инженерное решение с `SkillContext.intent` и MenuSkill последним перед echo — принимается: booking не тронут, регресс CG-1..CG-8 исключён конструктивно. Код ценен для per-tenant пути независимо от исхода вопроса ниже. Но **NO FALSE SUCCESS**: DRF-963 НЕ закрывать; в Linear-комментарии после merge явно зафиксируй открытую предпосылку (см. п.3) и что runtime-приёмка на пилоте не проводилась.

### 3. Путь пилотного бота — проверка уже поручена
Моё чтение текущего evidence (классификация INFERRED): пилотный MAX-бот, на котором владелец ловил живые баги, идёт **global-путём** — live-приёмка DRF-962 от 09.08 показала discovery-карточки `cb:discover:book:…` и LLM-вызовы `show_masters`, это глобальный консьерж, не keyword-диспатч.

Точная проверка (`MAX_WEBHOOK_SECRET` ∈ `GLOBAL_BOT_TOKENS` на хосте) включена side-quest'ом в бриф DRF-967 — то окно всё равно идёт на пилотный хост. Результат появится в `docs/REPORT_DRF-967.md`, секция «Путь пилотного бота». Тебе на хост ходить НЕ нужно.

Если подтвердится global: предпосылка DRF-963 пересматривается в главном окне (вероятное продолжение — welcome/fallback/меню для global-пути или включение MenuSkill-логики в консьерж-петлю). Это НЕ твой текущий объём — не начинай.

### 4. Побочные находки — не чинить, регистрируются главным окном
- «менеджер» не триггерит handoff (в коде только латинское `manager`);
- «спасибо большое» → health_screening (классификатор боли матчит префикс «бол»).

Обе регистрируются в Linear главным окном (уже в работе). В свой PR их НЕ включай — держим минимальный дифф.

## Итого твой остаток
1. Дождаться CI + Postgres-прогона.
2. `/review-axes`, довести до P0/P1 = 0.
3. Squash-merge, SHA + evidence-комментарий в DRF-963 (кириллица в GraphQL — только ASCII-escaped JSON `\uXXXX`, после записи верифицировать чтением; иначе Linear сохранит «?»).
4. Финальный отчёт в `docs/REPORT_DRF-963.md`: SHA, вердикт ревью, результаты тестов, residuals-кандидаты, статус открытой предпосылки.
5. Остановиться. Деплой и приёмка — отдельным брифом после решения по пути.
