# ОТВЕТ №8 (2026-08-10, вечер): ⛔ СТОП НЕМЕДЛЕННО — работа УЖЕ ВЫПОЛНЕНА, ничего не запускать

Ты работаешь по устаревшему контексту. Весь объём DRF-967 **уже выполнен и принят сегодня днём** (см. №7 ниже и `REPORT_DRF-967.md`):

1. Хост УЖЕ на `9cd0a9e` (проверено главным окном только что: `git rev-parse HEAD` = `9cd0a9ec…`), контейнеры `ayla-bot-staging-*` работают 33 часа, `web` healthy. На этом runtime сегодня прошла live-приёмка владельца (три сценария discovery) — **DRF-967 закрыта в Done в Linear**.
2. Чистка миррора УЖЕ сделана: 232 NULL-provenance удалены (дамп `docs/drf974-scripts/ms-orphans-formula-tela-20260810-apply.json`), синк пересоздал 95 sync-owned, верификация 20/19/20/36 и 9/9 пар сошлась.
3. **НЕ выполнять:** рестарт стека, `cleanup_orphan_master_services` (ни dry-run, ни `--apply`), синк, любые другие команды на хосте. Пересобранные тобой образы не поднимать — работающий стек не трогать.

**Останавливайся немедленно. Окно закрыть.** Если успел выполнить на хосте что-то, кроме `git checkout 9cd0a9e` и `docker build` (без `up`), — перед закрытием напиши в `REPORT_DRF-967.md` секцию с точным перечнем выполненных команд.

---

# (архив) ОТВЕТ №7 (2026-08-10): чистка принята — остаток нулевой, окно можно закрывать

1. **Секция «2026-08-10 — VERIFIED» принята целиком.** Все контрольные точки GO №6 сошлись: `--check-upstream` 95/137 = ожидание; удалено 232 NULL-provenance строки, sync-owned не тронуты; синк created=95 / skipped=137; итог 95 строк, все sync-owned, 0 NULL; по мастерам 20/19/20/36 = матрица; 9/9 выборочных пар OK (включая «Кавитация → Ольга» и отсутствие «Бикини тотальное → Денис»). Дамп проверен локально: `docs/drf974-scripts/ms-orphans-formula-tela-20260810-apply.json` (129 901 байт, 232 строки) — есть.
2. **Отклонение от GO по деплою принято**: хост уже был на `9cd0a9e` (HEAD dev), разворачивать было нечего — правильно, что не делал лишнего рестарта; health до/после зафиксирован.
3. Evidence-комментарии в Linear (DRF-967, DRF-962) публикует главное окно. Статус DRF-967 в Done переведёт главное окно после live-приёмки DRF-962 владельцем в MAX (NO FALSE SUCCESS: два открытых пункта DoD теперь выполнимы, но подтверждаются только runtime-приёмкой).
4. Пилотный runtime дальше не трогать.

**Остаток нулевой — останавливайся, окно можно закрывать.** Полный цикл 967 (диагностика → фикс кода → чистка по GO) выполнен дисциплинированно, эскалационные пороги соблюдены.

---

# (архив) ОТВЕТ №6 (2026-08-10): **GO НА ЧИСТКУ МИРРОРА + СИНК** — backend готов

**Сигнал «ГОТОВО ДЛЯ DRF-967» получен** (`REPORT_DRF-974.md` §19): канон Ayla приведён к подтверждённой владельцем матрице и верифицирован по endpoint.

## Состояние backend (что тебе нужно знать)

- Endpoint `specialist-services` отдаёт **95 активных рёбер** и 137 неактивных (для синка неактивное = «ребра нет»). Снапшот целостный: `complete=True`, полный обход 232 строки при `count=232` — реконсиляция безопасна.
- В миррор-таблице бота по-прежнему 232 строки `MasterService` с NULL-провенансом (операторские, синк их не трогает).
- **Целевое состояние после твоего захода: 95 строк `MasterService`, все sync-owned** (`ayla_specialist_service_id` не NULL), по мастерам: Денис 20, Инна 19, Татьяна 20, Ольга 36.

## Предусловие: код чистки должен быть на хосте

`cleanup_orphan_master_services` появился в merge `8d7b958`; на хосте задеплоен `699639c` (старее). **Перед чисткой обнови рабочую копию бота на хосте до текущего HEAD `dev`** (включает `8d7b958` и `9cd0a9e`; последний на пилоте no-op — global-путь) по механике из `REPORT_DRF-962.md` §5: git bundle по scp, detached HEAD, 3 compose-файла, health по `/healthz/`. Rollback-точка прежняя: `e6460bd7ca8709c256007a83c69e74e96cf5e960`. Известный шум: startup-ERROR `worker.subscriber_audit` — не признак сломанного деплоя. После рестарта — health-check, прежде чем идти дальше.

## Порядок применения (одним заходом, окно «0 услуг» минимальное)

1. `cleanup_orphan_master_services --check-upstream` (dry-run): в отчёт коротко — что «вернётся» и что «потеряется». Ожидание: вернутся 95, потеряются 137. **Если цифры не сходятся — СТОП и эскалация сюда.**
2. Чистка с `--apply` (обязателен `--dump` — сохрани дамп локально в `docs/drf974-scripts/` рядом с бэкапами backend).
3. Сразу ручной запуск синка, один проход.
4. Верификация: 95 строк, все sync-owned; выборочно 3–5 пар против матрицы (`docs/formula-tela-full-matrix-DRAFT.md`), включая «Кавитация → Ольга» и отсутствие «Бикини тотальное → Денис».
5. Секция в REPORT с фактическими цифрами (VERIFIED) → стоп. Live-приёмку DRF-962 в MAX делает владелец.

Границы: тенант formula-tela, только миррор `MasterService` + деплой кода бота. Данные Ayla-backend не трогать — они уже канон.

---

# (архив) ОТВЕТ №5 (2026-08-09, ночь): финальный отчёт принят — жди GO-сигнала здесь

1. Финальная секция 5-bis принята: merge `8d7b958`, ревью 2 прохода до P0/P1=0, DoD-регресс-тест подтверждён. Evidence в Linear публикует главное окно, включая твою поправку про покрытие тестами (что «15 тестов» DoD не закрывали — честно зафиксируем).
2. Кандидат №2 (неаудируемый bulk-путь записи MasterService) регистрируется главным окном.
3. Новый порядок применения принят: **фикс данных Ayla → `--check-upstream` → чистка → синк одним заходом.** Статус backend-стороны (DRF-974): диагностика завершена, первопричина — операторский `intake_confirm --staff`; метод согласован (`is_active=False`); ждём GO владельца и разрешения неоднозначностей раскладки. Сигнал GO придёт сюда отдельной секцией.
4. До сигнала — ничего не делай, окно можно держать в ожидании (монитор на этот файл по §2 протокола).

---

# (архив) ОТВЕТ №4 (2026-08-09, поздний вечер): подключись к протоколу окон

Введён постоянный протокол обмена: прочитай `docs/WINDOW_PROTOCOL.md` и поставь persistent-монитор на ЭТОТ файл (`REPLY_DRF-967.md`) по образцу из §2 протокола. Дальше решения главного окна (включая будущий GO на чистку §5.3) будут приходить сюда без участия владельца — реагируй на события монитора. Твой текущий остаток не меняется: довести ревью PR #1161 до P0/P1=0, merge, финальная секция в REPORT, дальше ждать GO-сигнала здесь.

---

# ОТВЕТ №3 (2026-08-09, вечер): Э-1 РЕШЁН владельцем

**Решение владельца (дословно по смыслу):** «нет конечно, мастера делают только свои услуги». Решётка 4×58 — **дефект данных backend**, сценарий 1 из твоей развилки Э-1.

Что это значит для тебя:
1. Backend-фикс идёт отдельным окном по отдельному брифу (готовит главное окно); задача в Linear уже регистрируется.
2. Твой остаток без изменений: довести PR #1161 до merge по планке (независимое ревью, P0/P1=0), финальную секцию в отчёт — и остановиться.
3. GO-гейт §5.3 (чистка на пилоте) активируется, когда backend-окно доложит «данные приведены к реальным компетенциям» — сигнал придёт в этот файл. План применения из твоего §4 (232 строки, окно ≤15 мин до синка, дамп, откат) принят как есть; при GO чистку и ручной запуск синка делай одним заходом, чтобы окно «0 услуг» было минимальным.

---

# (архив) ОТВЕТ №2 главного окна → окну DRF-967 (на отчёт с диагностикой хоста)

**Дата:** 2026-08-09 (после твоего отчёта с §1–§8 и вердиктом «канон Ayla = 4×58»)

Диагностика принята целиком: методика (арифметика seed, аудит-forensics, сверка local↔remote) и вывод «источник не в bot-platform» — убедительны. Решения:

## Э-2 — GO на PR: открывай
PR `b5edf03` в `dev` — открыть сейчас. Обоснование: код полезен в обоих сценариях Э-1 (чистка нужна и для «Ayla починит данные», и как гигиена; seed-фикс закрывает реальный класс дефекта). Планка прежняя: независимое ревью, P0/P1 = 0, CI зелёный, squash, SHA в отчёт.

## Э-3 — согласен: чистку на пилоте откладываем
Твоя логика верна: без Ayla-фикса чистка+синк воссоздадут те же 232 ребра, а окно «0 услуг у всех мастеров» создавать незачем. Чистка будет выполнена одним заходом вместе с (или сразу после) фиксом данных на стороне Ayla — тогда и запросишь GO по §5.3. До этого пилотный runtime не трогать.

## Э-1 — передан владельцу, решение придёт через этот файл
Вопрос «правда ли 4 мастера formula-tela выполняют все 58 услуг» задан владельцу напрямую (он знает салон). Задача-расследование на backend-сторону уже регистрируется главным окном (кандидат №1 из твоего списка). Твой сценарий работ не меняется ни при каком ответе: PR мержишь, чистка ждёт.

## Э-4 — Linear ведёт главное окно
Evidence-комментарии в DRF-967/962 по твоему отчёту публикует главное окно (уже в работе). DoD-снапшот задачи лежит в `docs/LINEAR_DRF-967_snapshot.md` — сверься; главное расхождение с брифом: DoD требует регресс-автотест «мастер без запрошенной услуги не попадает в выдачу» — судя по твоим 15 тестам, вероятно уже покрыто; подтверди в отчёте явно.

## Твой остаток
1. Свериться со снапшотом DoD (файл выше).
2. Открыть PR, довести до merge по планке.
3. Финальная секция в `REPORT_DRF-967.md`: SHA merge, вердикт ревью, подтверждение регресс-теста из DoD.
4. Остановиться до решения Э-1 (сигнал придёт сюда же, в REPLY-файл).

---

# (архив) ОТВЕТ №1 главного окна → окну DRF-967 (на отчёт «Сессия 1» до диагностики)

**Дата:** 2026-08-09
**От:** Chief Architect agent (главное окно).

## Решения по эскалациям

### Э-1 (доступ к хосту) — решает владелец, рекомендация дана
Рекомендация главного окна владельцу: разрешить `ssh` в твоей сессии (разовое подтверждение пермишена или Bash-правило на `ssh taximeter@194.87.99.126 …`). Диагностика §5.1 — read-only, а мутации данных и так закрыты GO-гейтом §5.3 брифа, так что риск контролируем. Ответ владельца придёт прямо в твоё окно (разрешением пермишена). Если владелец выберет иной вариант — он сообщит.

### Э-2 (Linear) — решено: все операции Linear ведёт главное окно
- Тебе Linear не нужен. Снапшот DRF-967 (полный description, DoD, все комментарии) главное окно выкладывает в `docs/LINEAR_DRF-967_snapshot.md` — прочитай его перед продолжением; правило «Linear главнее брифа» применяй к этому файлу. Если файла ещё нет — он появится в течение нескольких минут.
- Статус In Progress и evidence-комментарии публикует главное окно по твоим отчётам в `REPORT_DRF-967.md`. Пиши туда всё, что должно попасть в Linear, помечая «→ в Linear».

### Э-3 (гипотеза seed опровергнута) — продолжай, остановка не требуется
Твой анализ принят: seed — сопутствующий дефект, не источник. Порядок работ уточняется так:
1. **Команда чистки `cleanup_orphan_master_services` — делай в любом случае** (§5.2): она не зависит от источника. Фикс seed — тоже делай, как гигиену, в том же PR (отдельная задача не нужна).
2. **Диагностика источника на хосте** (§5.1 + твоя forensics-методика по аудиту и `created_at`/`created_by`) — после снятия Э-1. Это решающий шаг.
3. **Ownership-контракт синка НЕ трогать** — граница брифа в силе. Если подтвердится операторский путь (MM4-матрица или invite): «фиксом источника» считаем защиту на стороне admin_api/UI (guard от «включить всё» / валидация), а НЕ смену приоритета sync-истины. Реализация такой защиты — за пределами DRF-967: зафиксируй вердикт и объём в отчёте, задачу заведёт главное окно (твой кандидат №2 уже в очереди на регистрацию, ждём вердикта диагностики).
4. Возможный исход «дефект в данных, а не в коде» — легитимен: тогда DRF-967 закрывается чисткой + seed-гигиеной + верификацией discovery, а защита операторского пути уходит новой задачей.

## Принятые к сведению
- Поправка PR: миррор рёбер — PR #1157 (`e791333`), не #1128. Главное окно исправило это в Linear.
- Кандидат №1 (seed без провенанса) — новую задачу не заводим, чинится в твоём PR.
- Side-quest §6 (путь пилотного бота) остаётся на тебе — выполни сразу после снятия Э-1, результат в `REPORT_DRF-967.md`, секция «Путь пилотного бота».

## Дополнение после снапшота Linear (файл `LINEAR_DRF-967_snapshot.md` уже выложен)

Два пункта DoD задачи, которые шире брифа (правило «Linear главнее брифа» — применить):
1. **Регресс-автотест обязателен**: «мастер без запрошенной услуги не попадает в выдачу» — не разовая верификация на хосте, а тест в кодовой базе (положи рядом с фиксом в PR).
2. Upstream-проверка specialist-services endpoint как отдельного кандидата-источника — в брифе §5.1 она есть («сверка с backend-истиной»), но в DoD подчёркнута отдельно: проверь не только локальных писателей, но и что реально отдаёт Ayla-сторона для этих 4 мастеров.

## Твой ближайший порядок действий
1. Прочитать `docs/LINEAR_DRF-967_snapshot.md` (появится в ближайшие минуты); расхождения с брифом — отметить в отчёте.
2. Не дожидаясь хоста: реализовать `cleanup_orphan_master_services` (dry-run по умолчанию, `--apply`, json-дамп до удаления) + фикс seed + тесты. Внимание: чистка должна уметь работать по критерию «рёбра без `ayla_specialist_service_id`» с фильтром по тенанту; у `MasterService` на хосте НЕТ `is_active` (бриф §10).
3. После снятия Э-1: §5.1 диагностика + forensics источника + side-quest §6 → отчёт.
4. Дальше по брифу: PR/ревью → GO-гейт §5.3 → чистка §5.4.
