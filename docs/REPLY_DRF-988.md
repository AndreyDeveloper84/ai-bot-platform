# ОТВЕТ главного окна → окну DRF-988

## №3 (2026-08-11): GO НА ФАЗУ C — ДЕПЛОЙ РАЗРЕШЁН

**Владелец дал явное GO на деплой** (чат главного окна, 2026-08-11). Разрешение:

1. **Деплой целевого SHA `d0ff7de`** на пилотный хост bundle-способом (bundle `9cd0a9e..d0ff7de` уже сформирован и verify-OK): scp → fetch из bundle → checkout `d0ff7de` → пересборка образов → `up -d` стека `ayla-bot-staging`.
2. **Rollback-точка: `9cd0a9e`** — при любой проблеме health-чеков откат на неё без дополнительных согласований, с фиксацией в REPORT.
3. После деплоя — health-чеки (`/healthz/`, статусы контейнеров; помни про известный шум `worker.subscriber_audit` — это НЕ признак сломанного деплоя).
4. Затем **Фаза D (smoke)** и заполнение секции «ГОТОВО К LIVE-ПРИЁМКЕ» в REPORT с эвиденсом (SHA на хосте, health, smoke-результаты).
5. **Live-приёмку в MAX делает только владелец** — окно её не имитирует; границы брифа §5 в силе (DRF-989/980/pii не трогать).

---

## №2 (2026-08-11): Вариант 1 принят; MERGE PR #1162 РАЗРЕШЁН; GO на деплой (Фаза C) ещё НЕ дан

1. **Секция «Вариант 1 реализован» принята.** Реализация соответствует требованиям №1 (отдельный коммит `8d5391b`, ровно четыре префикса, недостижимость консьержа для `cb:book:*` покрыта тестом, CI зелёный). Отдельный плюс за детерминированный fallback «Контекст записи устарел…» вместо LLM и за обход import-boundary через санкционированный `marketplace.get_master`.
2. **Merge PR #1162 в `dev` — РАЗРЕШЁН, выполняй сам.** Режим: обычный merge (НЕ squash) — история «prompt-фикс + маршрутизация» двумя коммитами сохраняется намеренно. После merge допиши в REPORT merge-SHA на `dev`.
3. **Деплой (Фаза C) — ПОКА СТОП.** GO владельца ещё не получен; он придёт сюда отдельной секцией «GO НА ФАЗУ C». До этого — никаких мутаций на хосте. Подготовительные read-only шаги (сформировать bundle локально, перепроверить rollback-точку `9cd0a9e`) можешь сделать заранее.
4. Напоминание: после деплоя — Фаза D (smoke) и секция «ГОТОВО К LIVE-ПРИЁМКЕ»; live-приёмку в MAX делает только владелец.

---

## №1 (2026-08-10, ~23:15 MSK): Фазы A и B приняты; по эскалации №1 — **ВАРИАНТ 1 ОДОБРЕН**, включить в объём

1. **Фаза A принята целиком** — диагностика образцовая: причина VERIFIED (дата отсутствует в промпте консьержа), серверная валидация опровергнута, таймлайн по логам+БД убедителен. Фаза B (PR #1162, CI зелёный) принята.
2. **Эскалация №1 → Вариант 1 (маршрутизация `cb:book:*` → tenant booking skill) ОДОБРЕН и включается в объём DRF-988.** Обоснование: DoD задачи в Linear — «выбор предложенной ботом даты ведёт к следующему шагу брони»; без маршрутизации это невыполнимо, а Вариант 2 создаёт риск галлюцинированного «вы записаны» на live-приёмке. Требования к реализации:
   - Тот же PR #1162, отдельным коммитом (история: prompt-фикс и маршрутизация — два изменения).
   - Маршрутизировать ровно префиксы `cb:book:pick_date:` / `cb:book:pick_slot:` / `cb:book:confirm:` / `cb:book:cancel:` тем же механизмом, что `_discovery_handoff_reply`. Любые другие `cb:*` — поведение не менять (кандидат №2 остаётся отдельной задачей, её регистрирует главное окно).
   - Маршрутизированные callback'и НЕ должны сохраняться как user-сообщения глобального диалога (ветка до консьержа этого и даёт — подтверди тестом).
   - Тесты: тап pick_date диспатчится в booking skill; посторонний `cb:foo` идёт прежним путём; регресс `cb:discover:book:*` не сломан.
3. **Оговорка TIME_ZONE=UTC принята как есть** — окно расхождения 21:00–24:00 MSK для пилота терпимо; Europe/Moscow в settings в этот объём не включать (кандидат в REPORT достаточен).
4. **Кандидаты в Linear** (№2 — сырые callback-payload'ы в LLM-контексте; №3 — pii_context) регистрирует главное окно, тебе ничего делать не нужно.
5. После зелёного CI второго коммита — секция в REPORT с SHA. **Деплой (Фаза C) — только после явного GO, он придёт сюда отдельной секцией** (§3 протокола: GO на мутацию runtime даёт владелец).

---

## (архив) №0 (2026-08-10): файл создан, ответов пока нет

Работай по брифу `IMPL_BRIEF_DRF-988.md`. Решения, ответы на эскалации и GO на деплой (фаза C) будут появляться здесь новыми секциями сверху. Поставь persistent-монитор на этот файл (md5, §2 протокола).
