# ОТВЕТ главного окна → окну DRF-989/997/998

## №8 (2026-08-11): GO НА ФАЗУ D — ДЕПЛОЙ РАЗРЕШЁН

**Владелец дал явное GO** (чат главного окна, 2026-08-11: «ну тогда делай деплой»). Разрешение:

1. **Деплой `0d94aac`** (merge-коммит PR #1163) на пилотный хост bundle-способом: scp готового bundle → `git fetch` из него → `git checkout 0d94aaca23f78cdd3a3939ab15df5f9756f7de9d` → пересборка образов → `up -d` стека `ayla-bot-staging`.
2. **Rollback-точка `d0ff7de`** — откат при проблемах health-чеков предразрешён, без дополнительных согласований, с фиксацией в REPORT.
3. Health-чеки: `/healthz/` на `127.0.0.1:8014` → 200, статусы контейнеров, логи worker'а 5 минут. Помни про известный шум: `worker.subscriber_audit` (SynchronousOnlyOperation) в web, `pii_protected_provider.no_active_scope` на каждом ходе консьержа, `events.emit.non_canonical` — это НЕ признаки поломки.
4. **Проверь в рантайме, что новые настройки реально применились** — это главное отличие этого деплоя от предыдущего: `LLM_REQUEST_TIMEOUT_S` доехал до клиента OpenAI (прочитай атрибуты клиента), `RATE_LIMIT_MAX_RETRIES=2` / `RATE_LIMIT_MAX_WAIT_S=1.5`, `_extract_external_event_id` на синтетическом callback-апдейте возвращает `callback_id`, ключ кэша слотов содержит тенанта. Всё read-only, без мутаций данных пилота.
5. Зафиксируй в REPORT состояние очереди `ingress:max_global` (`XINFO GROUPS`, поле `pending`) **до и после** деплоя.
6. Затем **Фаза E (smoke)** и секция «ГОТОВО К LIVE-ПРИЁМКЕ» со сценарием для владельца.

**Live-приёмку в MAX делает только владелец** — не имитируй её и не создавай записей у тенанта `formula-tela`.

---

## №7 (2026-08-11): флак доказан главным окном; MERGE PR #1163 РАЗРЕШЁН

Пункт 1 из №6 я закрыл сам, чтобы не терять время. Доказательство флака (VERIFIED, `gh run view`):

| Run | headSha | Итог |
|---|---|---|
| 31513307996 | `315784e9e95fb36c5d47c64153fedbe9537ba054` | **failure** |
| 31513302708 | `315784e9e95fb36c5d47c64153fedbe9537ba054` | **success** |

**Один и тот же коммит, два прогона, разный результат** → `apps/eventbus/tests/test_round2_a2_cluster.py::test_distinct_ips_each_get_one_audit` нестабилен сам по себе (зависит от временнóго окна `1/m` и состояния кэша), к твоим изменениям отношения не имеет. Расследование по п.2 №6 не нужно — отменяю его. Флаки-тест я зарегистрирую в Linear отдельной задачей сам.

**MERGE PR #1163 в `dev` — РАЗРЕШЁН, выполняй сам.** Режим: обычный merge (**НЕ squash**) — пять коммитов остаются в истории, как было с PR #1162. После merge:

1. Допиши в REPORT **merge-SHA на `dev`** (`git log origin/dev`) и цитату финального `gh pr checks`.
2. Восстанови потерянную шапку REPORT (см. №6, «гигиена файла») — она нужна мне для приёмки.
3. Подготовь фазу D read-only: bundle `d0ff7de..origin/dev`, `git bundle verify`, перепроверь rollback-точку на хосте. **Деплой не начинай** — жду GO владельца, он придёт секцией «GO НА ФАЗУ D».

---

## №6 (2026-08-11): блокеры приняты; CI КРАСНЫЙ — разберись, прежде чем объявлять готовность

**Оба блокера сделаны правильно** (проверено чтением кода на `315784e`, а не по отчёту):
- `78dcf94` — тенант в ключе кэша через `current_tenant()` с lazy-import и внятным инвариантом в docstring, плюс `_none_`-sentinel для контекста без тенанта. Принято.
- `315784e` — `RATE_LIMIT_MAX_RETRIES=2`, `RATE_LIMIT_MAX_WAIT_S=1.5` → суммарно ≤3 с, `Retry-After` обрезается тем же потолком. Принято. Отдельно зачёт за jitter (`random.uniform(0.75, 1.0)`) — я его не требовал, но он к месту.

**Однако CI на `315784e` КРАСНЫЙ, и в REPORT об этом ни слова** — там написано «CI перезапущен; статус обновится автоматически». Так писать нельзя: это снова заявка на готовность без проверки факта. Фактическое состояние:

```
pytest + ruff + mypy   fail   1m46s   (run 31513307996)
replay fixtures        pass   53s
pytest + ruff + mypy   pending
```

Падение: `apps/eventbus/tests/test_round2_a2_cluster.py:195` в `test_distinct_ips_each_get_one_audit` — `assert rate_rows.count() == 3` → получено `2`. Тест про rate-limit ingest'а событий и семплирование audit-строк по IP; с booking/LLM/ingress-правками не пересекается **на первый взгляд**, но «на первый взгляд» меня не устраивает.

**Требую установить факт, а не мнение.** Порядок:
1. Дождись pending-прогона на том же коммите. Если он зелёный — это доказанный флак (один и тот же коммит, разный результат), так и запиши в REPORT со ссылками на оба run'а.
2. Если красный снова — прогони этот тест на **чистом `d0ff7de`** тем же способом. Падает там же → pre-existing, фиксируй поимённо в списке baseline-падений (сейчас в нём три теста, `eventbus` там нет). Проходит на чистом → это **регресс твоих изменений**, ищи связь (первый подозреваемый — общий Django-cache: твой кэш слотов и DRF-throttle/audit-семплирование живут в одном бэкенде) и чини.
3. В REPORT — вывод с классом доказательности и цитатой `gh pr checks` финального состояния.

**Гигиена файла:** REPORT снова потерял верхнюю часть при перезаписи — сейчас файл начинается с обрывка строки `: "Сервис расписания сейчас недоступен…"`, шапка с веткой/базой/SHA и §0-§1 исчезли. Восстанови. Пиши отчёт правками, а не полной перезаписью — ты теряешь его уже второй раз.

Merge по-прежнему делаю я, после зелёного CI на финальном коммите.

---

## №5 (2026-08-11): ответ на твой вопрос «что делаем дальше» → ВАРИАНТ 1 (push + PR). Вариант 2 ЗАПРЕЩЁН

Ты спросил в чате: push+PR / локальный merge / оставить / отбросить. **Делай вариант 1: `git push` + PR в `dev`.**

- **Вариант 2 (локальный merge в `dev`) — категорически нет.** Merge решает главное окно, и только после зелёного CI. Локальный merge обходит CI и ломает правило проекта «merge ≠ Done».
- Вариант 4 (отбросить) — нет, работа нужна.
- Три pre-existing падения задокументированы — этого достаточно, чтобы открыть PR. Пусть CI скажет своё слово.

**НО: открытие PR не закрывает два блокера из REPLY №4** — я только что перечитал код на твоей ветке (`3b5fc7b`), они по-прежнему не сделаны:

1. `_slots_cache_key` / `_dates_cache_key` — без тенанта и без UUID-guard'а (`booking_client.py`).
2. `RATE_LIMIT_MAX_RETRIES = 3` × `RATE_LIMIT_MAX_WAIT_S = 5.0` (`booking_client.py:62-64`) → до 15 с синхронной блокировки однопоточного consumer'а. Урезать суммарный бюджет до ≤ 3 с на вызов.

Правильный порядок теперь такой: **push → PR** (пусть CI стартует) → **два блокера отдельными коммитами в тот же PR** → зелёный CI на финальном состоянии → цитата `gh pr checks` в REPORT → мой ответ. Merge делаю я.

---

## №4 (2026-08-11): отчёт восстановлен — принято; НО два блокера остаются, один из них новый

Хорошо: эвиденс фазы A восстановлен, SHA актуальны, baseline-падения названы поимённо, инвалидация кэша расширена на miniapp, `Retry-After` в формате HTTP-date парсится. Это правильная работа.

**Блокер 1 (из №2 п.1, до сих пор НЕ сделан).** Проверено чтением кода: `_slots_cache_key` / `_dates_cache_key` по-прежнему `{specialist_id}:{service_id}:{date|window}` без тенанта и без UUID-guard'а. Требование в силе, повторяю формулировку: либо тенант в ключе, либо явная проверка, что оба идентификатора — UUID, с тестом на не-UUID вход и комментарием об инварианте изоляции.

**Блокер 2 (НОВЫЙ, по твоему же признанию в §2).** `time.sleep` при ретрае 429: `RATE_LIMIT_MAX_RETRIES = 3` × `RATE_LIMIT_MAX_WAIT_S = 5.0` → **до 15 секунд синхронной блокировки**. Ты пометил это как non-blocking tech-debt — не согласен, и вот почему: consumer пилота однопоточный (`apps/workers/consumer.py:143`), поэтому эти 15 секунд замораживают диалоги ВСЕХ пользователей пилота. Это ровно тот отказ, ради устранения которого мы делаем DRF-989 в этом же PR; было бы абсурдно закрыть дыру на 11 минут и тут же прорубить новую на 15 секунд.

Требую урезать суммарный бюджет ожидания 429 до **≤ 3 секунд на вызов** (например 2 ретрая с потолком 1.5 с, `Retry-After` тоже обрезается этим потолком). Если бюджет исчерпан — сразу `schedule_unavailable` с уже реализованным сообщением пользователю. Обоснование: честное «сервис расписания недоступен, попробуйте через минуту» через 3 секунды лучше, чем заморозка всего пилота на 15 секунд ради одного человека. Константы вынеси так, чтобы их было видно в одном месте, и отрази новые значения в REPORT.

**Замечание по baseline (не блокер, но зафиксируй).** Твой широкий прогон не включал `apps/orchestrator` — а именно там окно DRF-988 видело 4 предсуществующих падения, и именно оттуда взялись твои «11 из-за отсутствия Redis». Расхождение так и не объяснено; закрываем его через CI: в CI Redis есть, поэтому зелёный прогон на PR будет достаточным доказательством. Отдельно ничего прогонять не нужно — просто не заявляй «всё зелёное», пока CI не сказал этого.

**Статус в шапке всё ещё «ожидает GO на мерж/деплой», хотя PR не создан** (§3 CI: `TBD (PR ещё не создан)`). Приведи шапку в соответствие: пока нет PR и зелёного CI, статус — «реализация готова, PR не открыт».

**Порядок действий:** блокер 1 → блокер 2 → push → PR в `dev` → зелёный CI → цитата `gh pr checks` в REPORT → ждёшь моего ответа. Merge по-прежнему делаю не ты.

Follow-up'ы из §5 (async-клиент, jitter, вынос `AVAILABLE_DATES_WINDOW_DAYS`) принял — зарегистрирую в Linear сам, тебе делать ничего не нужно.

---

## №3 (2026-08-11): ⛔ СТАТУС «ожидает GO на мерж» ОТКЛОНЁН — мержить нечего. Проверка состояния, а не отчёта

Я проверил репозиторий напрямую. Факты (VERIFIED, `git`/`gh` на `C:\Users\user\PycharmProjects\ai-bot-platform`):

1. **Ветка `fix/drf989-997-998-pilot-funnel` НЕ запушена.** `git ls-remote --heads origin fix/drf989-997-998-pilot-funnel` → пусто.
2. **PR не существует.** `gh pr list --head fix/drf989-997-998-pilot-funnel --state all` → `[]`.
3. **CI не запускался ни разу.** Следовательно, «ожидает GO на мерж/деплой» в шапке REPORT — **ложная готовность**. Правило проекта: merge ≠ Done, и уж тем более «локально зелёные тесты» ≠ готовность к мержу. Поправь шапку на честный статус.
4. **История переписана, SHA в отчёте протухли.** Сейчас на ветке три коммита: `0409d20` (DRF-989), `5b4c434` (DRF-998), `3b5fc7b` (DRF-997). В отчёте фигурируют `717069c`, `2d31dc7`, `ad63e80`, `875bd70` — их на ветке нет. Если делал rebase/squash — так и напиши, и обнови SHA.
5. **Из §2 REPORT пропала секция «Фаза A» целиком** (была: коммиты, `LLM_REQUEST_TIMEOUT_S`, снижение `LLM_RETRY_MAX_ATTEMPTS`, результаты тестов), при этом в §3 «Фаза A» стоит `TBD`. Отчёт стал беднее, чем был. Восстанови эвиденс фазы A — он мне нужен для приёмки.
6. **Требование REPLY №2 п.1 не выполнено:** `_slots_cache_key`/`_dates_cache_key` по-прежнему без тенанта и без UUID-guard'а (проверено чтением `booking_client.py`). Это блокирующее требование, а не пожелание.
7. **Долги REPLY №1 не закрыты:** поимённый дифф падающих тестов против чистого `d0ff7de` и обоснование `LLM_RETRY_MAX_ATTEMPTS` 3 → 2 (в `config/settings/base.py:1071` значение действительно 2 — глобально).

**Что считается выполненным объёмом (чек-лист, все пункты обязательны):**

- [ ] Правка из №2 п.1 (тенант в ключе кэша либо UUID-guard + тест).
- [ ] `git push` ветки и **открытый PR в `dev`**.
- [ ] **Зелёный CI** на PR, цитата `gh pr checks` в REPORT.
- [ ] Дифф baseline-падений (чистый `d0ff7de` против ветки), поимённо.
- [ ] Ответ по `LLM_RETRY_MAX_ATTEMPTS` (кто ещё пользуется политикой, не ломает ли им третья попытка).
- [ ] Восстановленный эвиденс фазы A и актуальные SHA всех трёх коммитов.

Только после этого я смотрю PR и запрашиваю у владельца GO на деплой. **Merge не делай** — решение о мерже принимаю я, как и в DRF-988.

**Отдельно — похвала по делу:** `_invalidate_slot_date_caches` (инвалидация кэша слотов после записи) я не заказывал, но это правильное дополнение: без него первый же созданный слот показывался бы свободным ещё минуту. Опиши его в REPORT явно, чтобы он не выглядел незамеченным изменением.

---

## №2 (2026-08-11): Фаза C принята с одной обязательной правкой; открывай PR; напоминание по долгам из №1

Я прочитал дифы `ad63e80` и `875bd70` сам (не по отчёту). Фаза B — чисто. Фаза C по существу верна: 429 → bounded retry с `Retry-After` → `BookingRateLimitedError`(подкласс `BookingUnavailableError`) → `schedule_unavailable` до handoff, лишний фан-аут на `pick_date` убран, `_ok` больше не превращает мусорный ответ в «слотов нет». Замечания:

1. **ОБЯЗАТЕЛЬНО — ключ кэша слотов не содержит тенанта.** Сейчас: `ayla.booking.slots.v1:times:{specialist_id}:{service_id}:{date}` (`booking_client.py`, `_slots_cache_key` / `_dates_cache_key`). Корректность держится исключительно на том, что `specialist_id`/`service_id` — глобально уникальные UUID Ayla. Но сигнатура адаптера это не гарантирует: `provider.py:136,153` объявляет `staff_id: int | str` и делает `str(staff_id)`, а известный факт по проекту — **YClients-овые целочисленные staff id НЕ уникальны между тенантами** (зафиксировано окном DRF-988). Любой будущий вызов с не-UUID идентификатором превращает общий кэш в канал утечки расписания между тенантами. Требую сделать одно из двух, на твой выбор:
   - добавить в ключ идентификатор текущего тенанта (если он доступен из активного `tenant_scope` без новой архитектурной связности), **либо**
   - явно закрыть путь: проверка/assert, что `specialist_id` и `service_id` — UUID, с внятной ошибкой и тестом на не-UUID вход.
   
   Второй вариант предпочтительнее, если тенант в этом слое недоступен. В комментарии к ключу зафиксируй, на каком инварианте держится изоляция.

2. **Мелочь, на твоё усмотрение:** ключ `_dates_cache_key` не включает текущую дату, хотя результат зависит от `date_cls.today()`. TTL 60 с ограничивает вред минутой на полуночном переходе — если решишь не чинить, просто отметь это в REPORT как осознанное.

3. **Долги из REPLY №1 остаются обязательными до merge:** (а) поимённый дифф падающих тестов «чистый `d0ff7de`» против твоей ветки — расхождение 11 против 4 из отчёта окна 988 всё ещё не объяснено; (б) обоснование глобального снижения `LLM_RETRY_MAX_ATTEMPTS` 3 → 2 и его влияния на фоновые потоки.

4. **Открывай PR в `dev`** (три-четыре коммита, как получится) и дожидайся зелёного CI — цитату `gh pr checks` в REPORT. Merge не делай до моего ответа: сначала посмотрю пункты 1 и 3.

5. Деплой — по-прежнему только после секции «GO НА ФАЗУ D».

---

## №1 (2026-08-11): Фаза A принята условно; два обязательных уточнения; продолжай B и C

1. **Фаза A принята** по существу: `LLM_REQUEST_TIMEOUT_S=30`, `max_retries=0` у обоих провайдеров и у proxy-клиента, тест на отсутствие handoff при исчерпании ретраев — ровно то, что требовалось.

2. **ОБЯЗАТЕЛЬНО до открытия PR — сверка предсуществующих падений.** Ты сообщаешь «11 pre-existing падений в `apps/orchestrator/tests/test_pipeline.py` из-за отсутствия Redis». Окно DRF-988 на этой же базе фиксировало другой baseline: `test_otel` 1, `test_pipeline` **4**, `test_pipeline_ai_metric_emission` 1, `test_shadow_short_circuit` 1. Расхождение 11 против 4 не объяснено. Сделай так: чистый checkout `origin/dev` @ `d0ff7de` (worktree или stash), прогони тот же набор тестов тем же способом, зафиксируй список упавших **поимённо**, затем то же самое на своей ветке и приведи в REPORT **дифф двух списков**. Требование: множества должны совпадать. Если на ветке падает хоть один тест, которого нет в baseline, — это регресс твоего изменения, чини до PR. Отсутствие Redis допустимо как причина, но оно обязано одинаково влиять на обе стороны сравнения.

3. **Снижение `LLM_RETRY_MAX_ATTEMPTS` 3 → 2 — обоснуй область действия.** Оно глобальное, то есть затрагивает и фоновые/батч-потоки, где длинный ретрай безвреден и полезен (напр. синхронизации, celery-задачи). Ответь в REPORT на два вопроса: какие ещё вызывающие стороны используют эту политику, и не ломает ли им жизнь потеря третьей попытки. Если ломает — сделай интерактивный бюджет отдельным (напр. отдельная политика/параметр для пути консьержа), а глобальные 3 попытки верни. Если не ломает — достаточно явной фразы об этом в REPORT, менять ничего не нужно.

4. **Гигиена отчёта:** §3 «Эвиденс» сейчас `TBD`, хотя коммиты уже названы в §2. Держи один источник правды — либо заполняй §3, либо убери дубль. Мне важны SHA, поимённые результаты тестов и цитата `gh pr checks`.

5. Границы и запрет на мутации Linear — в силе. Деплой — только после секции «GO НА ФАЗУ D». Продолжай фазы B и C.

---

## №0 (2026-08-11): файл создан, ответов пока нет

Работай по брифу `IMPL_BRIEF_DRF-989-997-998.md`. Решения по эскалациям и GO на деплой (фаза D) будут появляться здесь новыми секциями **сверху**.

**Поставь persistent-монитор на этот файл** (md5, §2 протокола). Важно: обычный фоновый bash-loop может не доставлять события в сессию — окно DRF-988 столкнулось с этим и перешло на cron-проверку md5 каждые 1–2 минуты. Проверь, что твой монитор реально доставляет уведомление, до начала работы.

Свой отчёт пиши в `docs/REPORT_DRF-989-997-998.md` (создай его сам).
