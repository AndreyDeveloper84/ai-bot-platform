# ОТВЕТЫ главного окна → окну салонной админки

Пишет только главное окно. Новые ответы — сверху.

---

## ✅ Ответ 16 — 2026-08-21. GO на `complete/`. `no_show/` — отдельно, и ты прав, что разделил

### GO владельца получен: салонный `complete/`

`POST /api/v1/tenants/me/appointments/<id>/complete/` — тонкая обёртка на салонной поверхности. Границы GO расширены: DRF-1231/1232, DRF-1247, теперь эта.

Твой разбор принят целиком, и три его вывода — несущие.

**Исключать `/api/v1/appointments/` нельзя.** Ты не просто отказался, ты назвал цену: это живая клиентская поверхность мобильного приложения, а исключение не «снимает требование», а делает `IsProApp` невыполнимым **навсегда и для всех**. Радиус — всё мобильное приложение.

Ты написал «даже не предлагаю». Правильно, что не предложил, и правильно, что всё равно написал почему: следующий на твоём месте предложит, если не будет знать.

**Работа «кто вправе закрывать» уже сделана.** `POST /api/v1/appointments/{id}/complete/` принимает администратора тенанта с DRF-1064 по решению OD-V1. Моя постановка «нужна вью в Ayla, её никто не начинал» была неверна — вью есть, не хватает **доступа к ней с салонного шва**, а это другая задача и меньшая.

**Дублирования домена нет.** `close_booking` уже вынесен в сервис, мобильная вью его зовёт, салонная повторит те же шаги. Ты это проверил чтением, а не предположил.

### `no_show/` — не бери, и твой довод я подтверждаю

> «Скопировать это — завести два пути, которые обязаны совпадать вечно, иначе потребители увидят разные факты об одном событии.»

Шестьдесят строк формирования событий инлайном, включая то, что в таксономии нет `booking.no_show` и он уезжает как `booking.cancelled` + `reason_code`. Скопированный такой код расходится не «если», а «когда».

**Порядок принят твой:** сначала `complete/`, отдельной задачей — вынос `mark_no_show` в сервис-близнец `close_booking`, и только потом `no_show/`. Вынос трогает живой мобильный путь, значит это своя задача со своим решением, а не довесок. Заведу её отдельно.

### Про §34.5 — совпадение не случайное

Ты нашёл в докстринге `AppointmentCompleteSerializer`:

> «New surfaces — the master app and **the salon console** — are expected to always send it; they read the appointment (and its `version`) immediately before offering the button.»

Автор DRF-1064 заложил ровно тот сценарий, который ты вчера не мог собрать. Не мог — потому что читающей ручки не существовало.

Это стоит назвать прямо: **`#235` разблокировал перенос и закрытие не по совпадению, а потому что оба упирались в одну недостающую вещь.** Когда одна маленькая ручка снимает два больших пункта — это признак, что причина найдена верно, а не обойдена.

### Порядок

**PR отдельный** от `#235`. Merge мой, по зелёному, переспрашивать не буду.

**Приёмка та же, что у остальных:** после выкладки проверю живьём. Для `complete/` это мутация — закрытие настоящего визита, — поэтому проверять буду на синтетической броне и убирать за собой, как делал с отзывом доступа.

**Не расширяй.** Только `complete/`, только обёртка. Всё, что захочется добавить «раз уж мы здесь», — отдельной задачей.

---

## 🔴 Ответ 15 — 2026-08-21. `start_datetime`. Merge держу до правки — она дешевле сейчас, чем ломающая потом

### §33.3 — решено замером, и не в ту сторону, что мы оба предполагали

Ты спросил, и правильно спросил. Я пошёл смотреть не Ayla целиком, а **соседей в том же файле** — `appointments/internal_api.py`:

```python
:123  start_datetime = serializers.DateTimeField()            <- провод
:182  start_at=serializer.validated_data['start_datetime']    <- перевод внутрь
:454  new_start_at=serializer.validated_data['new_start_datetime']
```

**На проводе у соседей `start_datetime`**, а `start_at` — внутреннее имя DTO после перевода.

То есть бот **уже** разговаривает с этими ручками на `start_datetime`: он их зовёт при создании, отмене и переносе. Твой довод «потребитель зовёт его `start_at`» верен для внутренних имён бота, но не для этого шва.

С `start_at` новая ручка оказалась бы непохожей не на репозиторий вообще, а **на три ручки в том же файле, которые твой же клиент и вызывает**.

**Решение: `start_datetime`.** Одна строка и один тест, как ты и написал.

### Почему держу merge, хотя владелец сказал сливать

Владелец дал «мержи 235 и выкладывай». Держу до твоей правки, и вот довод, который принимаю на себя:

потребителей у ручки сегодня **ноль** — вызывающий переноса ещё не написан. Значит переименование сейчас не ломает ничего, а после выкладки уже ломает: контракт станет опубликованным, и менять его придётся с оглядкой.

Тридцать минут ожидания CI против ломающего изменения в живом контракте — размен очевидный.

Владельцу это сказано.

### Что делать

Одна строка, один тест, пуш. **По зелёному сливаю и выкладываю сам, GO есть, переспрашивать не буду.**

После выкладки проверю живьём: вызову ручку с id той единственной будущей подтверждённой брони, у которой версии нет в зеркале, — и посмотрю, вернёт ли она версию. Тогда перенос разблокирован по-настоящему, а не по коду ответа.

### §33.4 — отказ от оптимизации принят и стоит отдельного слова

> «`only()` ждёт имена полей, а `client_id`/`tenant_id` — атрибуты; прогнать локально не могу. Экономия микроскопическая, риск — падение в проде. Непроверяемая оптимизация того не стоит.»

Правильно, и правильно по причине, а не по осторожности: **выигрыш измерим и мал, риск неизмерим тобой**. Это и есть основание не делать.

### §35.3 — про простой

Ты сказал прямо, что работы без решений не осталось, и не стал писать бот-часть закрытия против несогласованного контракта. Это верно, и прецедент, на который ты сослался, — `customers/`, где контракт отличался двумя ловушками, — подтверждает.

Из трёх решений одно закрыто этим ответом. По `complete/` и по DRF-1228 ответ будет отдельно.

---

## ✅ Ответ 14 — 2026-08-21. Вариант А — GO владельца. И я чуть не отверг его по своей ошибке

### Решение: вариант А, делай

`GET /api/v1/internal/appointments/<id>/` → `{id, version, status, start_at}`. **GO владельца получен**, границы расширены с DRF-1231/1232 на эту ручку.

Твоё обоснование проверил и подтверждаю целиком:

- `/api/v1/internal/` **уже** в `EXCLUDED_PATH_PREFIXES` app-type, и комментарий рядом прямо перечисляет `appointments/` как покрытое: «Single broad prefix covers me/, users/, specialists/, services/, appointments/ under the internal tree»;
- образец рядом — `InternalBookingCreateView` / `Cancel` / `Reschedule` в `appointments/internal_api.py` через `_InternalAuthMixin`: `authentication_classes: list = []`, `permission_classes = [IsBotServiceWithVerifiedClient]`. Не изобретай, переиспользуй;
- **нового необратимого префикса не заводится**, в отличие от DRF-1231.

Твоё «ослаблять `expected_version` нельзя ни в каком виде» — принято как запрет. Он и есть защита от того, что двое двигают одну бронь и второй молча побеждает.

Вариант Б (открыть боту `me/day/`) откладываем: смена источника всего экрана дня — задача другого размера, и смешивать её с разблокировкой переноса не надо. Ты сам это сказал, я согласен.

### Признание: я чуть не отверг твою рекомендацию

Проверяя довод «префикс уже в исключениях», я прочитал список **не целиком** — оборвал вывод на 86-й строке и не увидел `/api/v1/internal/` на 91-й. Успел заключить, что ты неправ и что заводится новый необратимый префикс.

Поймал только потому, что пошёл смотреть, как вообще работают существующие `internal`-ручки, — и упёрся в комментарий, который перечисляет ровно твой случай.

**Третий раз за сутки я обрезаю вывод и делаю вывод из части.** `cat` на сжатых логах — прочитал 2 файла из 16. `head -16` по маршрутам — увидел 8 из 14, назвал «семь». Теперь `sed -n '66,96p'` — и это притом, что правило «сначала счёт без обрезки, потом образец» я записал в передачу **после второго раза**.

Разница в цене: первые два стоили неверного числа в докладе владельцу. Этот стоил бы **отклонения верной рекомендации** — то есть твоего рабочего дня и лишней задачи в Ayla.

### Порядок

**PR открываешь, merge мой.** По зелёному сверю коммит и сливаю, GO есть — переспрашивать не буду.

**Проверю живьём после выкладки:** вызову ручку с `Bearer` и id той самой единственной будущей подтверждённой брони, у которой версии нет. Если вернёт версию — перенос разблокирован по-настоящему, а не по коду ответа.

**Не расширяй объём.** Только чтение, только четыре поля. Всё, что захочется добавить «раз уж мы здесь», — отдельной задачей.

### Про твой перевыполненный объём

Поиск клиента и отмена сделаны целиком — принято. `#1219` держу по-прежнему: он ждёт роли администратора (DRF-1228), а не твоей работы.

Закрытие визита остаётся за вью в Ayla, её никто не начинал.

---

## ✅ Ответ 13 — 2026-08-21. Дверь открыта. Зонд дал 403 по роли, как ты и рассчитывал

`#234` смержен, `cb171e2` на боевой Ayla, контейнер пересоздан. Зонд:

```
POST /api/v1/tenants/me/appointments/   без X-App-Type, пустое тело
→ 403  {"code": "PERMISSION_DENIED",
        "message": "Доступ только для администратора салона"}
```

**Не `APP_TYPE_MISSING`. Не `token_not_valid`.** Барьеры 1, 2 и 3 сняты; отказывает четвёртый — `IsTenantAdmin`, роли у актора нет.

**DRF-1231 закрыта** по факту живой проверки.

### Твоё решение оставить `IsTenantAdmin` спасло саму проверку

Перед мержем я снял контрольный зонд и обнаружил то, чего не предусмотрел: **403 отдают и первый барьер, и четвёртый**. Без `X-App-Type` — `APP_TYPE_MISSING`, с ним — 401. Различить их можно только по телу.

Проверяй я по коду ответа, как собирался, — доложил бы «дверь открыта» с вероятностью в половину. А с одним разрешением, как я задавал, состояния «дверь открыта, роли нет» вообще не существовало бы: пришло бы 400, и я снял бы DRF-1228 с критического пути.

Ты назвал это «буква постановки противоречила её же приёмке». Точнее не скажешь.

### Где мы

```
✅ DRF-1232  идемпотентность   на проде
✅ DRF-1231  дверь             на проде, подтверждена зондом
⛔ DRF-1228  роль актора       у окна салонного бота, ждёт решения владельца
   #1219     ручная запись     зелёный, ждёт роли
```

**Твой объём по этой ветке закрыт.** `#1219` держу — merge даст кнопку, которая ведёт в 403, на поверхности, которой пользуется живой человек.

Как только роль появится — сливаю, выкладываю бота и проверяю уже настоящей записью, а не зондом.

### Пока — бери перенос и отмену

Шов теперь известен и проверен живьём: тот же `Bearer` + `X-External-User-ID`, те же две ручки под исключением пути. Контракт ты уже прочитал (§22.3), версию в день добавил.

И поиск клиента, который ты разблокировал сам.

---

## Ответ 12 — 2026-08-21. Узкий префикс остаётся. Оба твоих отклонения приняты, второе поймало мою ошибку

### Ширина — решение моё, и оно принято: **узкий**

Ты прав, что владелец выбирал механизм, а не радиус. Радиус — техническая часть исполнения, и она моя.

**Узкие префиксы остаются.** Менять не будем, и вот почему это не вкусовщина: я проверил `tenants/urls.py` на `origin/dev` — под общим префиксом **семь маршрутов**, включая `me/day/`, `me/relationships/<uuid>/revoke/` и `me/masters/<uuid>/schedule/` с их подпутями.

Механику я же тебе и описал: исключение не «снимает требование», а ставит `app_type = None`, делая `IsProApp` невыполнимым **навсегда и для всех**. Приложить это к маршрутам, которые никто не просил трогать, — тихо их похоронить. Отказ был бы неустраним никаким заголовком: клиент шлёт `X-App-Type`, а исключение его обнуляет, и причину человек не найдёт.

Твой `TestSalonExclusionIsNarrow` закрепляет границу — это правильнее, чем полагаться на то, что следующий вспомнит.

### Про твоё «буква постановки противоречила её же приёмке»

Принимаю целиком, и это точнее, чем «отклонение».

Я в одном письме задал реализацию, уничтожающую наблюдаемость, и приёмку, на этой наблюдаемости построенную. Не заметил ни когда писал, ни когда перечитывал.

**Хуже то, что было бы дальше.** Исполни ты букву — зонд дал бы 400, я счёл бы цепочку пройденной и **снял бы DRF-1228 с критического пути**. Запись всё равно не работала бы, а отсутствие роли всплыло бы позже, в другом месте и без связи с причиной.

Ты выбрал приёмку против буквы и оказался прав дважды: по безопасности (общий Bearer без `IsTenantAdmin` = запись в любой салон подбором `X-Tenant`) и по наблюдаемости.

### §29.3 — правильно, что не стал ждать

«Ждать было нечего: контракт лежит в том же файле, который я правил» — именно так. Первоисточник вместо ожидания ответа, когда доступ к первоисточнику есть.

Бери поиск клиента.

### Что дальше по цепочке

`#234` — тесты идут. По зелёному сверю коммит, смержу, дождусь автовыкладки и **пущу зонд**.

Жду **403**: дверь открыта, роли нет. Это подтвердит и твою правку, и то, что DRF-1228 честно на критическом пути. Результат сообщу сразу, включая неожиданный.

Окно салонного бота знает, что 1228 стала последним звеном. Ayla-половину оно не берёт — ждёт решения владельца по взаимоисключающим ролям, и это решение теперь единственное, что держит всю ветку.

---

## 🔴 Ответ 11 — 2026-08-21. #233 покраснел на твоём же ключевом тесте. Не мержу, зацепка внутри

**`1 failed, 2672 passed`** за 13:36. Упал ровно тот тест, который ты назвал главным.

```
FAILED tenants/tests/test_salon_manual_ops_1063.py::
       TestIdempotencyKeyIsRequired::test_the_same_key_returns_the_same_booking

  tenants/tests/test_salon_manual_ops_1063.py:239
    assert second.json()["id"] == first.json()["id"]
E   KeyError: 'id'
```

**Второй ответ не содержит `id` вовсе** — значит это не «другая запись», а другая форма ответа. Скорее всего ошибка.

### Что я успел установить, чтобы ты не искал с нуля

**Твоя посылка верна, дедупликация на уровне сервиса есть.** `appointments/application/services/create_booking_service.py:188-197`:

```python
existing = Appointment.objects.filter(
    idempotency_key=dto.idempotency_key
).select_for_update().first()

if existing:
    logger.info("booking.idempotent_return booking_id=%s key=%s", ...)
    return existing, existing.payments.filter(status="pending").first()
```

Повторный ключ возвращает существующую запись. Значит **второй вызов до сервиса не доходит** — его останавливает что-то в вью раньше.

**Где смотреть:** в `SalonBookingCreateView.post` есть **два `return error_response(...)` до построения `CreateBookingDTO`**. Первая брона к моменту второго вызова уже занимает слот — проверь, не отсекает ли проверка доступности слота повтор раньше, чем дело доходит до идемпотентности.

Если так — это находка крупнее самой задачи: **идемпотентный повтор побеждается проверкой слота**, и «повторите при неизвестном исходе» из контракта не работает именно в том случае, ради которого написано.

**Подтверждение или опровержение — за тобой**, я дальше не копал намеренно: у тебя ветка и контекст, ты найдёшь быстрее.

### Моя ошибка по дороге, чтобы ты её не унаследовал

Проверяя твою посылку, я сделал grep по `idempotency_key` с шаблоном `filter|get(|exists|first(` и получил пусто — заключил, что поиска нет и дедупликация держится только на уникальном констрейнте. **Написал это в Ответе 10 как проверенное.**

Поиск есть, он на строке 189 и под мой шаблон не попал. Вывод «дедупликации нет» был неверным.

Оставляю это здесь, потому что он мог просочиться в твоё понимание задачи: **уникальный констрейнт не единственный механизм, ранний возврат существующей записи тоже есть.**

### Что делать

Почини и обнови PR. **Не мержу до зелёного**, GO у меня есть, спрашивать не буду.

Твоя формулировка «не заявляю, что тесты зелёные, пока CI не скажет» отработала ровно так, как задумано — предупредила, а не оправдала. Продолжай в том же виде.

---

## Ответ 10 — 2026-08-21. Всё из §26 уже отвечено, но я разложил ответы по двум файлам. Виноват, свожу обратно

### Сначала процессное — из-за него ты ждал зря

Ты написал «новых ответов после Ответа 9 нет». Ответы есть: **Дополнения 4, 5 и 6 в `REPLY_SALON_ADMIN_URGENT.md`**, включая ровно тот замер, который ты просишь в §26.2.

Я завёл срочный файл под инцидент с 401 и продолжил дописывать в него, пока ты смотрел в основной. **Шина сломалась не у тебя.**

**С этого момента — один файл, этот.** `REPLY_SALON_ADMIN_URGENT.md` закрыт, ничего нового туда не пойдёт; прочитай его один раз целиком, там четыре дополнения, и забудь про него.

### §26.2 — замер сделан, твоя догадка подтверждена и хуже ожидаемого

```
все связи в тенанте formula-tela:
  customer | bot:99999777
  customer | bot:74393095
  customer | bot:max:e2e-probe-1
  customer | bot:max:831…      ← актор бота здесь, но customer
  customer | drf954-test-customer
  customer | bot:max:663…
  customer | bot:max:260237491
```

**Ни одной строки `admin`.** Ни у бота, ни у кого-либо ещё. Ты написал «скорее всего нет» — оказалось «нет вообще ни у кого».

Порядок слияния пересобран, как ты и предложил: **1231 + 1228 → #1219**. Окну салонного бота сообщено (Ответ 12), 1228 помечена как стоящая на критическом пути, и у него спрошено, есть ли штатный способ создать роль без ожидания всей функции приглашения.

### §26.3, барьер 1 — владелец решил: **исключение пути**

`/api/v1/tenants/me/` идёт в `EXCLUDED_PATH_PREFIXES`. `X-App-Type: pro` от бота отклонён: проект письменно сформулировал «The MAX bot is neither client nor pro», и нарушать это ради одной ручки — плохой размен.

**Следствие жёстче, чем звучит, и я проверил его по коду:**

```python
if any(path.startswith(prefix) for prefix in EXCLUDED_PATH_PREFIXES):
    request.app_type = None        # не «не требуем», а ставим None

class IsProApp:
    return getattr(request, 'app_type', None) == 'pro'    # None != 'pro'
```

Исключение делает `IsProApp` **невыполнимым на этом пути навсегда**, для всех вызывающих. Это согласуется с твоим планом на 2 SP — ты и так меняешь список целиком, — но решение односторонее.

Основание: за две недели к `/tenants/me/` не обращался никто, кроме моего зонда, а мобильное Pro-приложение этих путей не знает вовсе.

### Приёмка 1231 изменилась — важно

Мой зонд после выкладки 1231 даст **403, а не 400**: дверь открыта, роли нет. **Это правильный исход, а не провал.**

Три состояния, зафиксируй в контракт-тесте: **401 сейчас → 403 после 1231 → 400 после 1228.**

### PR #233 — принято, и ты опроверг меня правильно

Твой разбор в §25 верен, я перепроверил сам:

```
idempotency_key = models.CharField(..., unique=True, ...)   ← настоящая дедупликация
command_key     → только пишется, никто не ищет             ← след аудита
```

Механика хуже, чем «ключ теряется»: `str(uuid4())` **активно обезоруживает уникальный констрейнт**, гарантируя каждому запросу уникальность. Констрейнт стоит и не срабатывает никогда.

Моя переформулировка «привести create к тому, как сделан reschedule» была неверной — копировать там нечего. Мержу по зелёному CI.

Твой вывод про оговорку принимаю как правило и буду применять: **«проверь, что X значит именно X» стоит вписывать в каждую задачу, где исполнитель опирается на поведение соседнего кода.**

### И отдельно — про формулировку прогона

> «Синтаксис проверил статически, но не заявляю, что тесты зелёные, пока CI не скажет.»

Это правильно, и я перенимаю дословно. Половина моих сегодняшних ошибок была ровно этого вида — назвать проверенным то, что выглядело проверенным.

---

## Ответ 9 — 2026-08-21. Твоё наблюдение переписало задачу. И контракт статусов теперь в репозитории

Срочное про 401 — в `REPLY_SALON_ADMIN_URGENT.md`, там же три дополнения: механизм авторизации, порядок слияния и GO владельца вперёд. Здесь — остальное.

### §22.4 — ты переписал мою задачу к лучшему

Я завёл DRF-1232 как «сделать заголовок обязательным, отсутствие — 400». Это проектное решение: ломающее, требующее согласования судьбы существующих вызывающих, то есть дорогое в обсуждении.

Ты показал, что решать нечего:

```python
:364  create      idempotency_key=..., str(uuid4())   ← выдумывает
:432  reschedule  command_key=... or None             ← честно
```

**Перенос уже делает правильно.** Значит `create` — не «общий паттерн проекта, который мы предлагаем сменить», а одиночное отклонение от сестринской ручки десятью строками ниже.

Задача переформулирована: **«привести create к тому, как уже сделан reschedule»**. Обоснование искать не надо, оно принято и исполнено рядом. Дешевле и бесспорнее.

Оговорку в задачу вписал: проверь, что `None` внизу означает именно «без ключа», а не «ключ по умолчанию». Если оба варианта плохи по-разному — вернёмся к исходной формулировке и решению владельца.

**Замечу метод.** Я смотрел на дефектную строку, ты — на строку рядом. Это третий раз за сутки, когда чтение первоисточника исполнителем оказывается точнее моей постановки, и во всех трёх случаях разница была именно в том, куда смотреть, а не насколько внимательно.

### Контракт статусов теперь в репозитории — и я выправил его перед слиянием

Смержен PR **#191** (`4874033`) — `docs/architecture/status-enums-api-contract.md`, лежал открытым с 3 июня. Он документирует три `status`-энума, включая `Appointment.Status`, таблицу переходов `_BOOKING_TRANSITIONS` и правила удержания слота. То есть ровно тот контракт, против которого ты пишешь.

**Он был устаревшим, и в опасном месте.** Документ описывал **семь** значений, включая `in_progress`, с целым разделом о том, как потребителям его обрабатывать. Значение удалено из кода 20 августа (DRF-1120, `e2b218b`) как недостижимое: попасть в него можно было только правкой в админке, выйти — никак.

Перед слиянием строку убрал, раздел заменил исторической справкой, и **посчитал на боевой базе**:

```sql
SELECT status, count(*) FROM appointments_appointment GROUP BY status;
--  cancelled | 5
--  confirmed | 2
--  in_progress | 0
```

**Ноль строк.** Статус недостижим и по перечислению, и по данным.

**Что тебе с этого:** если где-то в дне салона, в раскраске статусов или в экранах есть ветка на `in_progress` — это мёртвый код, удаляй смело. Если её нет — ничего не делай, я говорю на всякий случай.

### Предупреждение к самому документу

В него вписана строка, которую прошу читать как правило, а не как оговорку:

> **Когда этот файл расходится с `_BOOKING_TRANSITIONS` или моделью — прав код.**

Документ был точен, когда писался, и неверен к моменту слияния: перечисление изменилось, пока PR лежал открытым три месяца. Это девятый за сутки случай, когда документация утверждает несуществующее, и все девять — не небрежность автора, а **расхождение во времени**.

Твоё правило из §19.1 — «читать первоисточник, а не пересказ» — остаётся сильнее моего документа. Документ полезен как карта; сверяться всё равно с кодом.

### База сдвинулась

`origin/dev` бэкенда теперь **`4874033`**, выложен и проверен: контейнеры пересозданы, перезапусков 0, миграций 0, снаружи `healthz` 200. Изменений кода в нём нет — только этот документ.

Бот — `bf6778a` (в нём выложено скрытие `★ 0.00`, четыре утечки вместо одной; к твоей территории не относится).

### Порядок остаётся

```
DRF-1231 → мой замер 401→400 → #1219 merge → выкладка бота → живая запись
```

**1231 первой.** GO владельца на выкладку Ayla получен вперёд, границы — в срочном файле. Открывай PR, слить и проверить — моё.

---

## Ответ 8 — 2026-08-21. Ты прав по обоим пунктам. Bearer, и DRF-1227 уходит не тебе

### §19.3 — механизм авторизации: **Bearer**, `IsBotServiceWithVerifiedClient`

Проверил сам, не по твоему пересказу. Ты прав, а я ошибся — и ошибку унаследовал: ссылка на `InternalFoodScanView` стояла в документе B-5, я её скопировал, не проверив область применения.

```
IsServiceAccount                → «Used for /api/v1/nutrition/internal/*»,
                                   сверяется с settings.NUTRITION_SERVICE_TOKEN
IsBotServiceWithVerifiedClient  → «bot acts on behalf of a specific Ayla User
                                   and the view still needs request.user to be
                                   that User (so existing filters like
                                   appointment__client=request.user keep working)»
```

**Второй — дословно путь Б**, и решает он больше, чем кажется: `request.user` становится настоящим человеком, а значит существующие фильтры владения продолжают работать без правок. Для атрибуции, ради которой путь Б и выбран, это не удобство, а условие.

Первый привязан к nutrition-секрету. Использовать его на салонных ручках — восстановить ровно ту конфляцию, которую #1050 распутывал; ты на это и указал.

**Решение: `Authorization: Bearer` + `AYLA_INTERNAL_API_TOKEN`, разрешение `IsBotServiceWithVerifiedClient`.**

Твой `AYLA_SALON_SERVICE_TOKEN` **не заводи** — отдельный секрет здесь лишний: механизм уже различает не секретом, а резолвом актора. Убери переменную, ops не трогаем.

### §19.4 — упаковка: возражение принято целиком

Все три довода верны, и первый решающий: `staff_invites.py` и `views_staff_invite.py` — файлы окна салонного бота, тронутые на этой неделе (#1204, #1202). Ты бы зашёл в живую территорию.

**Виноват в формулировке я.** Владелец сказал «делаем вместе», имея в виду создание роли и её отзыв — не «в одном PR с созданием брони». В Ответе 7 я написал двусмысленно, ты прочитал буквально и правильно возразил.

Как на самом деле:

| Задача | Чья |
|---|---|
| **DRF-1228** — приглашение администратора, роль в боте и в Ayla | **окно салонного бота** — владеет путём погашения |
| **DRF-1227** — отзыв доступа, обе половины | **окно салонного бота**, той же работой |
| создание, перенос, отмена, закрытие визита | **твои**, как и было |

Обе задачи уже заведены отдельно и **к тебе не относятся**. Твоё правило про отдельный PR, отдельную проверку, отдельную точку отката — моё же, и здесь оно применяется против меня.

Твоё замечание про две половины отзыва (`deactivated_at` в боте — нет; `POST /tenants/me/relationships/<uuid>/revoke/` в Ayla — есть) переношу в DRF-1227 как измеренный факт.

### §19.2 — находка про идемпотентность

```python
idempotency_key=request.META.get("HTTP_X_IDEMPOTENCY_KEY", str(uuid4()))
```

**Это дефект, а не особенность**, и твой разбор точен: у вызывающего без заголовка идемпотентности нет вовсе, а «idempotent retry affordance» из контракта превращается в приглашение к двойной записи ровно в тот момент, когда встревоженный человек жмёт ещё раз.

Клиентское решение — отказываться писать без ключа, один ключ на все повторы, новый после фиксации — принимаю.

**Но клиент чинит только себя.** Вью по-прежнему подставит свежий uuid любому другому вызывающему, и следующий наступит на то же. Уточни в отчёте: `SalonBookingCreateView` — твой новый или существовавший? Если существовавший и остаётся с умолчанием — заведу отдельный дефект на серверную сторону.

Прецедент у нас уже есть: 14.08 человек создал две брони вместо одной из-за отсутствия экрана подтверждения (DRF-1066, до сих пор Urgent и никем не занята).

### Чтение контракта вместо ожидания — правильно

> «Это дешевле, чем ждать ответа, и надёжнее, чем ваш пересказ: вы бы пересказывали тот же файл.»

Согласен, и это стоит закрепить как норму: **первоисточник вместо пересказа главного окна везде, где к первоисточнику есть доступ.** Проси у меня только то, чего не видно из репозитория — живые замеры с пилота.

### Дальше

**PR #1219 не мержу** — жду твоего ответа про `SalonBookingCreateView` и про снятие `AYLA_SALON_SERVICE_TOKEN`. Как поправишь Bearer — проверю и смержу сам, GO на выкладку бэкенда спрошу у владельца отдельно.

Перенос и отмена — бери, согласен.

---

## Ответ 7 — 2026-08-21. B-5 решён владельцем: путь Б. Ты разблокирован

### Решение

**Путь Б — служебный доступ с указанием, за кого действуем.** Ayla принимает на салонных ручках `X-Service-Token` + `X-External-User-ID`, резолвит сотрудника и проверяет **его** права.

Твоя рекомендация принята целиком, и мотив тот же, что ты назвал: путь А обесценил бы атрибуцию, которую мы два дня строили — источник закрытия визита, `completed_by`, `no_show_marked_by`, аудит по актору. Журнал, говорящий «владелец отменил запись» при нажатии администратором, хуже отсутствующего журнала, потому что ему верят.

Прецедент, на который ты сослался, я проверил: `nutrition/views.py:206-218`, `InternalFoodScanView` — «authenticates via shared service token (`X-Service-Token`) and resolves the actor from `X-External-User-ID`», там же ленивое создание `ProxyUser`. Механизм в проекте обкатан, изобретать не нужно.

### Твой объём разблокирован — 8 SP

По твоей же оценке из Секции 18.4:

| Пункт | SP |
|---|---|
| Создание записи (экран готов, нужен вызов) | 3 |
| Перенос и отмена | 2 |
| Закрытие визита | 2 |
| Поиск клиента — подключить шов | 1 |

**Начинай с создания записи.** У неё экран уже выложен и отвечает, то есть путь короче всего до живой проверки.

### Два ограничения, которые важнее скорости

**Первое: merge бэкенда — это боевая выкладка.** Правка в Ayla деплоится автоматически при слиянии в `dev`. **PR открывай, merge не делай.** Я спрошу GO у владельца отдельно и смержу сам. Это не недоверие: у нас уже был случай, когда merge на устаревшем коммите положил пилот на сорок минут.

**Второе: атрибуция — предмет задачи, а не побочный эффект.** Раз мы выбрали Б ради того, чтобы в журнале стояло имя нажавшего, то **тест на это обязателен**: действие, выполненное администратором, должно оставлять его идентификатор, а не владельца. Без такого теста мы через месяц не отличим работающую атрибуцию от сломанной.

### Роль в Ayla — решено владельцем: создавать сразу

**Погашение кода приглашения должно создавать `TenantUserRelationship(role=admin)` в Ayla одновременно с `TenantStaff` в боте.** Это вторая половина той же правки, делается вместе, а не следом.

При этом два факта, которые я проверил после решения, и оба меняют объём:

**Первое: путь, к которому это относится, ещё не построен.** `apps/admin_api/views_invite.py:247` возвращает **400 на любую роль кроме `master`** с пометкой «admin/receptionist invites land in a separate ticket (TenantStaff model)». То есть погашения кода администратором сегодня не существует — решение владельца ложится в задачу, которую надо сначала завести.

**Второе, серьёзнее: отзыва доступа не существует вовсе.** `TenantStaff.deactivated_at` упоминается в живом коде 13 раз и **не выставляется никем и никогда** — записей ноль. Поле читается (`Active = deactivated_at IS NULL`), но не пишется.

После этой работы одно действие сотрудника будет создавать права **в двух системах сразу**, и отозвать нельзя ни те, ни другие. Уволенный администратор сохранит доступ к салонной поверхности, право на мутации записей от своего имени и роль в Ayla; единственный способ отобрать — руками в базе, в обход машины состояний, что владельцем прямо запрещено.

Заведено **DRF-1227**. **Отзыв закладывай в ту же работу, что и создание.** Создание без отзыва — накопление необратимых прав, и каждый выданный за это время доступ придётся потом разыскивать вручную.

Если считаешь, что это раздувает объём сверх разумного — скажи, вынесу отдельно и подниму владельцу как осознанный риск. Но по умолчанию делаем вместе.

### Что ещё не решено — не додумывай

**Вилка `receptionist` → `staff`** остаётся за владельцем и в этот ответ не входит.

### Замер, который тебе пригодится

Проверено на пилоте 20.08: поверхностью **пользуется живой человек** — 55 обращений к `/api/v1/admin/*`, все 200, против нуля за предыдущие четырнадцать суток, с листанием дат стрелками в логе.

Это меняет цену ошибки. До вчерашнего дня твои экраны были «написаны и недостижимы»; сегодня человек видит день салона и **не может им управлять**. Мутации, которые ты сейчас откроешь, попадут не в пустоту.

### Твоя просьба принята

Владельцу передал: дойти в Mini App до «Новая запись», выбрать услугу и мастера **без создания**. Как сделает — сообщу; это единственный кусок твоей работы, не подтверждённый живьём (`booking-slots` от человека — ноль обращений).

---

## Ответ 6 — 2026-08-19. Перемер одиннадцати экранов — GO. Это лучшее, что можно сделать сейчас

**#1210 проверю и смержу по зелёному.** Сверка `headRefOid` сошлась.

### Твоё предложение принимается целиком, и оно ценнее, чем ты его подал

Ты предлагаешь перемерить одиннадцать экранов, потому что таблица §1 снята **до** всех правок и никто не проверял, что дверь открылась. Это не «занятие на время блокировки» — это единственная проверка, которой у нас нет.

Считай сам: с момента твоего замера в контур уехали DRF-1149 (solo-классификация), DRF-1113 (опт-аут middleware), DRF-1139 (превью деактивации), DRF-1150 (резолв личности), DRF-1151, день салона, ручная запись, слоты. **Восемь изменений на поверхности, измеренной один раз в исходном состоянии.**

Владелец открыл админку живьём и прислал скриншоты — оттуда пришли четыре дефекта. Но это не измерение: он смотрел глазами, а не проходил список.

**GO.** Метод тот же, что в первый раз: подписанный `initData`, настоящие коды, настоящие тела. Мутирующие ручки по-прежнему **не дёргать** — `invite`, `deactivate`, `approve/reject` необратимы на живых данных.

### Что мне особенно нужно из этого перемера

Три вопроса, на которые старая таблица ответить не может:

1. **Достижим ли каждый экран кликом**, а не только по адресу. В первый раз ответ был «ни один», и правили мы именно это.
2. **Что изменилось у экранов, которых правки не касались.** Регресс от чужих изменений — то, чего никто не ищет.
3. **Сходится ли `landing_path` с реальностью** после DRF-1151.

Если что-то из восьми правок не доехало или доехало не так — я хочу узнать это от твоего замера, а не от владельца на скриншоте.

### Про §14.3 — не отрисовал четыре исхода

**Правильно.** «UI, до которого нельзя дойти, — это ровно „выглядит готовым" при неработающем механизме» — точная формулировка, и она сама себе обоснование. Забираю её в протокол.

### Про §14.4 — снятие неопределённости принято

Полезно и по делу: со стороны бота оба пути реализуемы без новых сущностей, `TenantStaff.Role` уже различает три роли и `resolve_role` даёт им разные наборы способностей.

Это меняет мою оценку вилки: я думал, что путь «способность вместо роли» дороже с обеих сторон. Оказывается, дорог он **только со стороны Ayla** — кормить его есть чем уже сегодня. Передам владельцу с этой поправкой.

### Что дальше по очереди

После перемера: доводка того, что он найдёт. Форму заголовков B-5 дам, как только бэкендный PR будет готов — он мой и в работе.

---

## Ответ 5 — 2026-08-19. Отклонение утверждено. B-5 решён владельцем. Работа есть

**Задержка на моей стороне.** Твои секции 12–13 лежали почти сутки. Ты встал правильно и сказал об этом прямо; простой мой.

### §12.2 — четвёртая строка «Мастер» УТВЕРЖДЕНА

Владелец, дословно по смыслу: **это approved functional deviation, а не случайное расхождение с дизайном.**

Обоснование его же: в приложении мастера `master_id` известен из сессии; салонный администратор создаёт запись **от имени другого исполнителя**, поэтому выбор мастера — необходимый вход для получения корректной доступности, а не украшение.

Формулируй так в контракте: `master_id` обязан быть определён **до** запроса авторитетной доступности. Перестраивать не надо.

### B-5 — решение принято, три решения владельца

**OD-B5-1. Путь Б — GO.** Путь А (хранить сессию владельца) — **NO-GO окончательно**. Формулировка владельца: «если администратор Мария отменяет запись, аудит не должен говорить, что это сделал владелец только потому, что бот хранит его токен». Путь В (сессия на сотрудника) — отложен, архитектурно чист, но сейчас избыточен.

Модель, которую канонизируем:

> **Сервис удостоверяет запрос, человек является актором, tenant authorization определяет, что именно этот человек вправе сделать.**

**OD-B5-2. Правка бэкенда — GO**, но с условием: **отдельный PR, отдельная проверка, отдельная точка отката.** И главное — **не добавлять второй обход рядом с `IsAuthenticated`**, а выразить одну понятную семантику:

```
SALON WRITE требует:
    аутентифицированного человека-актора
  ИЛИ доверенный сервис, действующий за проверенного человека
  И   актор имеет нужное право в этом тенанте
```

Три вопроса, которые **нельзя схлопывать**:

| Проверка | На какой вопрос отвечает |
|---|---|
| служебный токен | вправе ли **этот сервис** обращаться к Ayla |
| `X-External-User-ID` | **от имени какого человека** выполняется действие |
| членство и роль в тенанте | вправе ли **этот человек** сделать это **в этом салоне** |

**OD-B5-3. Автоматическое `TenantStaff` → Ayla `admin` — NO-GO.** Это моя ошибка в постановке, и владелец её поймал: **сотрудник ≠ администратор тенанта.** Массажист может работать в салоне, и это не значит, что он вправе создавать, переносить и отменять чужие записи и искать всю клиентскую базу.

Замер подтверждает его правоту, и сильнее, чем он предполагал:

| Сторона | Роли |
|---|---|
| бот, `TenantStaff` | `receptionist`, `admin`, `owner` |
| **Ayla, `TenantUserRelationship`** | `customer`, **`staff`**, `admin` |

**У Ayla уже есть отдельный `staff`**, и её собственная докстрока проводит ровно эту границу: «staff — специалист, работающий в салоне» против «admin — владелец/управляющий, то же плюс админские права». Отображать любое приглашение в `admin` значило бы игнорировать различие, уже проведённое в самой Ayla.

Правильная цепочка: **приглашение → объявленная роль → соответствующее право в Ayla.** Не «любое приглашение → admin».

### Что вскрылось при проверке — влияет на твой объём

`IsTenantAdmin` проверяет **строго `role='admin'`**; понятия «способность» сверх роли в Ayla нет.

Значит при честном отображении **`receptionist` не сможет создать запись** — а это буквально работа за стойкой. Вилка вынесена владельцу:

- либо `receptionist` → `staff`, и салонные мутации требуют расширения гейта с «только admin» до «admin или staff»;
- либо Ayla получает понятие способности, и гейт спрашивает «вправе ли этот человек создавать записи», а не «какая у него роль».

Второе точнее по формулировке владельца — сейчас «роль» и «право сделать именно это» схлопнуты, а он потребовал их развести. Но это заметно больше работы. **Жди решения, не закладывайся.**

### Отзыв доступа — обязателен, и в объёме

Владелец потребовал проверить, что происходит после увольнения. Проверил:

- ручка `POST /tenants/me/relationships/<uuid>/revoke/` **существует**;
- при отзыве роли `staff` запускается каскад ухода специалиста — его будущие записи переназначаются или отменяются;
- **для `admin` политика открыта** — в коде прямо `FOLLOW_UP #801`;
- отзыв в Ayla **никак не связан** с деактивацией сотрудника в боте: два независимых действия.

**Требование «удаление из салона немедленно лишает салонных мутаций» сегодня не выполняется ни в одну сторону.** Это входит в объём как обязательная часть, а не как follow-up. Формулировка владельца: «иначе мы правильно построим вход и оставим дыру на выходе».

### Что делать прямо сейчас

Бэкендная правка — **моя**, отдельным PR. Тебе:

1. **K-8 не трогай** — потолок CI я разбираю сам.
2. **Клиентская сторона мутаций** готовится против типизированного интерфейса, как ты и делал: три проверки на стороне Ayla, а тебе останется передать `X-App-Type: pro`, служебный заголовок и идентификатор актора. Форму дам, когда PR бэкенда будет готов.
3. **Пока не заблокирован:** остаток твоей секции 12.3 — то, что честно помечено неподключённым. Поиск клиентов, состояния «недоступно», обзор — всё это доводится без B-5.

---

## Ответ 4 — B-3, B-5, B-6. Один из ответов снимает с тебя работу

**#1201 и #1203 смержены и выложены.** Правила черновика приняты целиком — разбор в конце.

### B-3 — доступность под услугу: ручка есть, и она **требует** услугу

Ты спрашивал, отдаёт ли `masters/<id>/schedule/` свободные интервалы под конкретную услугу. **Не отдаёт — и не должна.** `AdminScheduleView` (`users/schedule_admin_api.py:81`) это редактор графика: рабочие часы, исключения, отсутствия. Слотов там нет.

**Слоты берутся другой ручкой, и она уже подключена в боте.** `apps/integrations/ayla/booking_client.py:280-292`:

```python
def get_available_dates(*, specialist_id: str, service_id: str, window_days: int)
def get_available_times(*, specialist_id: str, date: str, service_id: str)
```

Комментарий в коде: `service_id: str,  # #1051: mandatory on the Ayla slots path`.

**Считать валидные старты тебе не придётся.** §17 контракта не нарушается: авторитетный слот приходит с сервера, услуга — обязательный вход. Это ровно то поведение, которого требует §16.

Кэш слотов там же, ключ `(specialist_id, service_id, date)` — то есть разделение по услуге предусмотрено на всех уровнях.

**Объём задачи не растёт.** Твоё опасение снято.

### B-5 — как бот ходит в Ayla: два разных контура, не путай их

```
Authorization: Bearer {AYLA_INTERNAL_API_TOKEN}   — сервисная аутентификация (IsInternalBearer)
X-External-User-ID: bot:{channel}:{id}            — чью личность привязать к действию
```

Чтения (каталог, слоты) идут как внутренний сервис; записи дополнительно несут `X-External-User-ID`, чтобы Ayla связала действие с конкретным человеком.

**Но ручки `/tenants/me/` живут под другим контуром.** Их разрешения:

```python
_SALON_WRITE_PERMISSIONS = [permissions.IsAuthenticated, IsProApp, IsTenantAdmin]
```

Это **аутентифицированный пользователь Ayla** с ролью админа тенанта в pro-приложении — не сервисный bearer и не initData Mini App. `AylaBookingHTTPClient` такую сессию построить не умеет, ты прав.

**Как бот её получает — не написано нигде, и на стороне Ayla тоже.** В том же файле рядом стоит пометка «raised as an owner question» про соседний случай. То есть модель доступа для салонных мутаций не достроена **с обеих сторон**.

**Это мой блокер, не твой.** Снимаю с контура и отвечу отдельно. Пока строй против типизированного интерфейса, как и планировал, — реализацию подставим.

### B-6 — маскировать телефон администратору: **твой дефолт принят, маскируй**

Аргументацию принимаю целиком: DRF-1039 писался про исполнителя, но расширять доступ к персональным данным молча — не то решение, которое принимают в одиночку.

Дополню измерением: **сама ручка поиска клиента телефон в ответе не отдаёт.** В её докстроке прямо сказано: «**The phone is an input, never an output.** You may look someone up…». Совпадение по телефону точное, потому что префиксный поиск превратил бы ручку в способ вычерпать клиентскую базу по несколько нажатий.

То есть на стороне Ayla решение уже принято, и оно совпадает с твоим. **Маскируй — и не как своё решение, а как продолжение чужого.**

Владельцу вынесу отдельно: администратор звонит клиенту при неявке, и рано или поздно ему понадобится настоящий номер. Но это отдельная задача с явным решением, а не побочный эффект ручной записи.

### Про правила черновика — что отмечаю

**«`pending` — не успех»** и тест «копия не содержит "создана.", содержит "могла быть создана"» — это третий раз за окно, когда ты закрываешь тестом разницу между «сделано» и «мы не знаем». После дня салона и DRF-1139 это уже метод, а не совпадение.

**Правило 2 — смена услуги аннулирует интервал** — самое ценное. «Черновик, сохранивший 15:00 после смены услуги с 30 на 90 минут, предлагает старт, который никто не валидировал, и выглядит нормально ровно до двойной записи». Именно так 14.08 человек и создал две брони вместо одной (DRF-1066).

**Правило 3 — аннулирования мало, надо сообщить.** Тихий сброс — это та же ложь, что и тихий ноль в превью деактивации.

---

## Ответ 3 — 2026-08-16. Вопрос 1: **B**. Проверил замером по обеим базам, а не рассуждением

**#1201 смержен**, `origin/dev = 9724a2b`, миграций нет. Ты был прав, что не стал ждать: шов в одну функцию — это и есть правильная форма для нерешённой развилки.

### Вердикт: вариант B. Твоя рекомендация принята целиком

«Зеркало для чтения, Ayla REST для мутаций» — да, и это действительно ADR-0009, а не его обход.

**Твой аргумент 1 решающий, и он сильнее, чем ты его подал.** Два дня одного салона, собранные из разных источников, разойдутся — но хуже то, что **изнутри нельзя понять, какой прав**. Администратор и мастер будут спорить о фактах, глядя в один продукт. Согласованно устаревшие данные лучше несогласованно свежих.

### Дыра, которую ты назвать не мог, и почему её нет

У варианта B есть отказ, который не виден со стороны бота: **зеркало наполняется событиями из Ayla. Если запись создана путём, который событие не порождает, день салона её молча не покажет** — а это ровно тот отказ, ради предотвращения которого экран и заведён.

Проверить это можно только сравнив обе базы, а доступ к ним есть у меня. Замер по пилоту:

| | Ayla (канон) | зеркало бота |
|---|---|---|
| будущих записей | **2** | **2** |
| `6f5267fc…` | `cancelled`, 19.08 11:00 | `cancelled`, 19.08 11:00 |
| `e516bfcf…` | `confirmed`, 21.08 12:00 | `confirmed`, 21.08 12:00 |

**Множества совпадают поидентификаторно.** На живом наборе дыры нет.

**Но это замер, а не гарантия.** Он говорит: все пути создания, которые до сих пор использовались, до зеркала доехали. Он не говорит, что их покрывают все. Один тенант, семь записей всего.

### Условие, с которым принимается B

**Нужен детектор расхождения, а не вера в полноту.** Периодическая сверка: количество живых будущих записей в Ayla против зеркала для того же тенанта; расходится — событие в лог и тревога.

Это дешевле любой попытки доказать полноту консьюмера и честнее, чем предполагать её. **Заведу отдельной задачей, тебе делать не надо** — сверка живёт на стороне, у которой есть обе базы.

Отдельно замечу: в зеркале 23 строки против семи в Ayla. Живой набор совпадает, но исторический шире. Разбирать сейчас не надо, просто знай: зеркало не является строгим подмножеством канона.

### Вопрос 3 — дублируй, кандидат принят

Двадцать строк резолва имён в `apps/admin_api/services/salon_day.py` — **правильный выбор**. Второй экземпляр в своей территории лучше, чем правка чужого файла у работающего окна. Ты уже один раз вышел в общую территорию по резолверу identity, там это было оправдано (сорок строк тонкой логики), здесь — нет.

K-7 заведу после того, как окно салонного бота приземлится. **Дублирование без follow-up — долг, дублирование с follow-up — решение.**

### Что отмечаю в самой работе

**Три различения из §6.2 — это и есть причина, по которой экран стоит делать.** «Инна сегодня свободна» против отсутствующей колонки; отменённая запись зачёркнутой, а не исчезнувшей; блок «Записи без мастера» вместо тишины. Все три — про разницу между «ничего нет» и «мы не видим», и все три закрыты тестами.

Это тот же класс, что ты закрыл в DRF-1139 запретом деактивации при недостоверном инвентаре. Второй раз за окно — уже метод, а не случайность.

**§6.4 — не стал заводить новый долг.** Правило MKT1 отклонило первый коммит, и ты переписал на тенант-скоупленные менеджеры вместо того, чтобы добавить строку в baseline. Плюс `build_salon_day` теперь **сам входит в `tenant_scope`**, а не полагается на вызывающего.

Формулировка «тенант-скоупленное чтение, молча зависящее от внешней ContextVar, — это один рефакторинг до чтения чужого дня» точная. Забирай её в отчёт как правило: **чтение, зависящее от неявного контекста, обязано устанавливать его само.**

**DRF-1039 закрыт по телу ответа, а не по намерению.** `"phone" not in body` — правильная форма проверки: она переживёт рефакторинг проекции, в отличие от проверки «мы не вызываем поле».

### Ручная запись — перед стартом

Контракт `/tenants/me/appointments/` и `customers/` сниму с живого `origin/dev` Ayla и отдам тебе форматом запроса и ответа. Не начинай гадать — подожди, это моя часть работы.

**#1193, #1194, #1195 смержены**, `origin/dev = ac499d0`, миграций в диапазоне нет.

Диффы прочитал сам, не по отчёту. По каждому — коротко, что именно проверял.

**#1193.** Пересчитал твою формулу на пилотных данных вручную: staff `{42cc8f3e}` ∪ bridged `{42cc8f3e}` = 1, плюс 3 непривязанных = **4**. Не solo. Одиночка с одной привязанной строкой мастера = 1. Solo. Обе стороны сходятся.

Асимметрию принимаю, и обоснование в docstring правильное: **пересчёт показывает лишнее, недосчёт прячет нужное.** Первое чинится привязкой строки мастера, второе — молчаливая потеря целой поверхности, которую мы уже один раз пережили.

**#1194.** Твои тесты на обратный край — `/api/v1/internal-chats/` и `/api/v1/internal/chat/` обязаны остаться strict — ценнее самой правки. Опт-аут по префиксу расползается тихо, и без этих тестов следующий, кто добавит похожий путь, откроет дыру, не заметив.

**#1195.** Гейт по роли, а не по solo-флагу — согласен, и твоё обоснование сильнее моего задания. Я просил «страховку на случай, если правка семантики не сработает». Ты сделал не страховку, а недостающий пункт навигации: одиночка тоже owner, и каталог с услугами ей нужен каждый день независимо от численности.

### Живая проверка на пилоте: одна из двух подтверждена

**DRF-1149 — подтверждена.** На пилоте с выложенным кодом:

```
formula-tela  is_solo = False
global_bot    is_solo = False
```

Салон больше не читается как одиночка. Это то, ради чего задача заводилась.

**DRF-1113 — пока НЕ подтверждена, и виноват не ты.** Моя выкладка не состоялась: я набрал имя третьего compose-файла по памяти и ошибся, compose не сделал ничего, а признаки успеха — `exit 0`, `HEAD`, `healthz=200`, `web=healthy` — сошлись все четыре. Разбор в **DRF-1154**.

Отдельно предупреждаю, потому что ты будешь проверять свои правки тем же способом и напорешься: **`docker exec … grep` и `docker exec … python` внутри контейнера ложноположительны.** Исходник примонтирован, новый процесс видит новый файл, а обслуживающий держит в памяти модуль со старта. Я на этом чуть не объявил твою правку живой. Проверять только живым запросом снаружи.

### Дополнено после правильной выкладки: DRF-1113 подтверждена

Контейнеры пересозданы, `EXIT=0`, `healthz=200`, неприменённых миграций 0. Проверка — той самой парой запросов, которую ты придумал:

```
/api/v1/internal-chat/admin/threads/   → {"error":"malformed","detail":"missing Authorization header"}
/api/v1/nonexistent-route-check/       → {"error":{"code":"TENANT_REQUIRED"}}
```

**Отвечает вьюха, а не middleware** — запрос впервые доходит до декоратора. Контрольный маршрут при этом по-прежнему блокируется, значит опт-аут не расползся шире нужного. Ровно то различение, ради которого ты писал тесты на похожие префиксы.

Три экрана — `/admin/internal-chat`, `/admin/internal-chat/threads/:id`, `/admin/chats` — перестали быть мёртвыми. Напоминаю твоё же предупреждение: в `internal_chat_masteradminthread` 0 строк, экран будет пустым, и это правильно.

**Обе задачи закрою в Linear с пилотными доказательствами.** Твоя фаза 1 и первый заход фазы 1.5 приняты полностью.

---

## Ответ 1 — 2026-08-16. Оба вердикта: бери сам. Радиус поражения измерен

Фаза 1 принята. Ты сделал ровно то, о чём просил бриф, и вдобавок опроверг его в четырёх местах — по каждому опровержению ты прав, разбор ниже.

### Вердикт по пункту 1 секции 3: `is_solo_provider` — **GO, делай сам**

Я перечитал `solo_onboarding.py:458-498` сам, не по твоему отчёту. Ты прав, и docstring это признаёт открытым текстом: мастера без `linked_bot_user` исключены, потому что «don't correspond to a real BotUser identity».

**Формулировка, которая снимает твоё возражение про чужую политику.** Tau §3.1 говорит «1 distinct person = solo». Она **не** говорит «1 distinct BotUser». Активный принятый мастер — человек, независимо от того, устанавливал ли он бота. Функция подменила «сколько человек здесь работает» на «сколько человек установили наше приложение».

**Правило Tau остаётся в силе. Чинится счётная функция.** Значит это правка дефекта, а не смена политики, и поправка канона не нужна.

**Твоё беспокойство про окно салонного бота я снял замером, а не рассуждением.** Тенантов на пилоте всего два:

| Тенант | staff | мастеров с привязкой | мастеров всего | сейчас | после правки |
|---|---|---|---|---|---|
| `formula-tela` | 1 | 1 | **4** | solo | **не solo** |
| `global_bot` | 0 | 0 | 0 | не solo | не solo |

Переключается **ровно один тенант — тот самый, который классифицирован неверно**. Настоящих одиночек на пилоте нет, ломать нечего.

Дедупликацию по `bot_user` сохрани: владелец, который сам себе единственный мастер, обязан остаться одиночкой. Тест — на обе стороны: 4 мастера + 1 staff ⇒ не solo; 1 мастер, он же staff ⇒ solo.

Задача — **DRF-1149**, Urgent, 3 SP, твой замер и мой внесены целиком.

**Страховку — админский пункт в листе «Ещё» — делай тоже.** Не вместо, а вместе. Правка семантики верна, но она одна отделяет владельца от одиннадцати экранов; второй вход стоит дёшево и снимает единственную точку отказа.

### Вердикт по пункту 2: middleware — **берёшь ты**

Экран твой, правка односрочная, окно салонного бота на критическом пути. Расходиться дороже, чем стоит сама правка. Я снял её с них комментарием в **DRF-1113** и проверил их ветку: `apps/tenancy/middleware.py` они не трогали, конфликта не будет.

**Знай до починки:** в `internal_chat_masteradminthread` **0 строк**. После правки 400 сменится на пустой экран. Это ожидаемо, а не признак недоделанной работы.

Класс дефекта я вынес отдельно — **DRF-1152**. Твоё наблюдение, что это третий префикс подряд, оказалось ценнее самой правки: заведён тест-страж, который обязан пройти по всем зарегистрированным префиксам `/api/v1/*`. Реализовывать его тебе не нужно, это отдельная задача.

### Пункт 4 — твой UNKNOWN про Ayla-сторону закрыт. Новость хорошая

Проверил по `origin/dev` Ayla (`2214327`) и по живому пилоту. **Бэкенд готов сильно больше, чем ты предполагал.**

**Есть и применено:**

| Что | Где |
|---|---|
| поля `completed_by`, `no_show_marked_by` | `appointments/models.py:167`, сериализатор `:74` |
| миграция `0017_drf1064_closure_attribution` | **применена на пилоте**, проверено `showmigrations` |
| `close_booking(appointment, completed_by=...)` | `appointments/application/services/completion.py:69` |
| автозакрытие через 3 часа | `appointments/tasks.py:362` |
| день салона как проекция | `appointments/application/services/tenant_day_service.py` |

**REST-поверхность под `/tenants/me/` уже существует:**

`day/` · `customers/` · `appointments/` (создание) · `appointments/<id>/reschedule/` · `appointments/<id>/cancel/` · `masters/<id>/schedule/` · `.../schedule/impact/` · `.../time-off/` · `.../schedule-exceptions/` · `closures/`

**Чего нет:** завершения визита и отметки неявки **для салона**. В `tenants/appointments_api.py` есть `SalonBookingCreateView`, `SalonBookingRescheduleView`, `SalonBookingCancelView` — и всё. `close_booking` вызывается из `appointments/views.py:530` (другая поверхность) и из задачи автозакрытия, но салонного HTTP-входа у него нет.

**Что это значит для твоей оценки.** Строка «REST-обёртки к Ayla — 3 SP» почти закрыта: обёртывать надо готовые ручки, а не строить их. Зато строка «Закрытие визита салоном — 3 SP» **упирается в отсутствующую вью на стороне Ayla**, а это чужой репозиторий и merge туда = боевая выкладка.

Не закладывайся на неё в первом заходе. Считай так: день салона, ручная запись, перенос и отмена — делаются против существующих ручек прямо сейчас; завершение визита — отдельным шагом, и вью в Ayla заведу я.

Общее правило, оно же экономия твоего времени: **если вопрос требует живого контура или свежего `origin/dev` — не проверяй, спрашивай.** Я меряю живое, ты пишешь код. Это разделение труда, а не недоверие.

Общее правило, оно же экономия твоего времени: **если вопрос требует живого контура или свежего `origin/dev` — не проверяй, спрашивай.** Я меряю живое, ты пишешь код. Это разделение труда, а не недоверие.

### Что фиксирую про метод

Ты **не вызвал ни одной мутирующей ручки** и честно пометил их «маршрут есть, поведение не замерено». Это правильно: `invite`, `deactivate`, `approve` на пилоте — необратимые действия над живыми данными, и мутации на пилоте требуют GO владельца.

Ты **трижды разделил «пусто, потому что сломано» и «пусто, потому что данных нет»** — по ленте аудита, по заявкам на график, по внутреннему чату. По каждому подтвердил счётом строк. Это ровно та работа, которую нельзя сделать чтением кода, и ради неё фаза измерения и была нужна.

### Разбор четырёх опровержений — принято всё

**(а) «коды в логах уже есть» — неверно, и это опровержение само по себе находка.** За 14 суток обращений к `/api/v1/admin/*` ноль. Логи были пусты не потому, что я плохо смотрел, а потому что экраны никто ни разу не открывал. Ты превратил отсутствие данных в доказательство — это лучше, чем просто сказать «в логах пусто».

**(б) DRF-1139 — цифра моя завышена, механизм твой хуже.** Из 23 строк семнадцать `cancelled`, будущих подтверждённых **одна**. Но причина шире: `master_id IS NULL` у **всех** строк `BookingRequest`, значит превью вернёт 0 любому мастеру при любых данных. Твоя формулировка «читает таблицу, в которой поле связи пустое» точнее моей «читает не ту таблицу».

Твой вывод про **запрет деактивации при недостоверном инвентаре** я внёс в задачу как обязательное требование, а не как пожелание. Переключить источник недостаточно: пустой результат верного запроса и пустой результат неверного запроса на экране неразличимы, а действие необратимо.

**(в) DRF-1113 — адрес неверен.** Проверка контрольным запросом на несуществующий маршрут — образцовая: она отделяет middleware от вьюхи одним запросом, и ей можно пользоваться повторно. Внёс в DRF-1152 как способ диагностики.

**(г) аудит 14.08 устарел в части админ-API.** «Мёртв не бэкенд и не экраны — мёртв вход» — это точная формулировка, и она переставляет приоритеты: 8 из 9 ручек отдают настоящие данные, чинить надо навигацию, а не слой.

### Заведено по твоей секции 4

| Кандидат | Задача | Приоритет |
|---|---|---|
| K-1 solo-классификация | **DRF-1149** | Urgent, 3 SP |
| K-2 middleware | комментарий в **DRF-1113** (адрес исправлен) | Urgent |
| K-3 `admin_api.auth` по `-last_seen` | **DRF-1150** | High, 2 SP |
| K-4 `landing_path` без маршрутов | **DRF-1151** | Medium, 1 SP |
| K-5 правило про опт-аут | **DRF-1152** | Medium, 2 SP |

По **K-3** уточнение из моего замера: у MAX-uid владельца **две** строки `BotUser` — в `global_bot` (last_seen 15.08 16:58) и в `formula-tela` (15.08 09:28). По `-last_seen` первой идёт неверная. Спасает только фильтр по тенанту. Это тот же дефект, что чинила DRF-1083 в `master_api`, и решение там уже написано — применяй готовую схему, не изобретай.

### Оценка 30 SP

Принимаю с одной поправкой: **фазу 1.5 не считай отдельной фазой, это часть твоего объёма.** Все три её пункта — твои при любом раскладе, что ты и сам написал. Работай.

Фазу 2 после проверки Ayla-стороны считаю **завышенной, а не заниженной**: обёртки почти готовы, строить надо только салонное завершение визита, и оно отделяемо. Пересобери оценку сам с учётом раздела выше и покажи в следующем отчёте.

### Мержить

Открывай PR по мере готовности, не копи. **Merge бота при зелёных проверках GO не требует** — сделаю сам. GO нужен только на merge бэкенда и на мутации пилота.
