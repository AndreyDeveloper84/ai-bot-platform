# СРОЧНО → окну салонной админки. 2026-08-21

**Прочитай до того, как продолжишь перенос и отмену. Шов, на котором ты строишь, сегодня не работает.**

---

## Замер

Я вызвал `POST /api/v1/tenants/me/appointments/` на живой Ayla ровно тем способом, который ты реализовал: `Authorization: Bearer <AYLA_INTERNAL_API_TOKEN>` + `X-External-User-ID` + `X-App-Type: pro`. Тело — пустой объект, чтобы ничего не создалось.

```
HTTP=401
{"error":{"code":"AUTHENTICATION_FAILED",
          "message":"Given token not valid for any token type",
          "code":"token_not_valid"}}
```

**Отказ на аутентификации, а не на разрешениях.** JWT-аутентификатор отвергает служебный токен раньше, чем `permission_classes` вообще начинают проверяться. До `IsProApp` и `IsTenantAdmin` дело не доходит.

Приди 400 на валидации — это значило бы, что авторизация прошла и не хватает только полей. Пришёл 401.

## Что это значит

**PR #1219 не может работать против сегодняшней Ayla.** Клиент написан правильно, шов исправен со стороны бота — принимающая сторона его не принимает.

Причина в двух слоях, и оба на стороне Ayla:

```python
# tenants/appointments_api.py:95
_SALON_WRITE_PERMISSIONS = [IsAuthenticated, IsProApp, IsTenantAdmin]

# tenants/appointments_api.py:176-177
class _SalonBookingBase(APIView):
    permission_classes = _SALON_WRITE_PERMISSIONS
```

`IsBotServiceWithVerifiedClient` в списке **нет**, а он обязан быть в `permission_classes`, чтобы отработать: именно он подменяет `request.user` резолвнутым человеком. Не будучи вызванным, он ничего не подменяет, и `IsAuthenticated` видит анонима.

Но даже добавление его в список недостаточно: **401 приходит с уровня аутентификации**, а не разрешений. Значит нужен ещё и аутентификационный класс, либо `authentication_classes`, не отвергающий не-JWT токен.

Все три ручки — create, reschedule, cancel — наследуют `_SalonBookingBase`. **Перенос и отмена упрутся в то же самое**, поэтому и пишу срочно.

## Чего я от тебя НЕ жду

**Не ходи в репозиторий Ayla.** Правка там — боевая выкладка, и объём её больше, чем «добавить класс в список»: надо решить, как салонная поверхность проходит аутентификацию, не открыв при этом дверь шире, чем нужно.

Это моя работа — поставить задачу и получить GO владельца. Твой PR остаётся открытым и корректным со своей стороны.

## Что делать сейчас

**Вариант 1, предпочтительный:** доведи перенос и отмену по контракту, как планировал. Клиент и тесты уровня бота от этого не зависят — они проверяют, что ты шлёшь правильно, а не что тебе отвечают. Когда Ayla будет доработана, все три ручки заработают разом.

**Вариант 2:** если считаешь, что писать вслепую против непроверенного шва неразумно — скажи, и возьми вместо этого поиск клиента (1 SP) либо подготовку закрытия визита. Я не настаиваю: ты лучше видишь, где риск переписывания выше.

**В обоих случаях добавь тест-контракт на 401.** Сегодняшнее поведение принимающей стороны — факт, и он должен быть зафиксирован: когда Ayla починят, тест покраснеет и скажет нам, что мир изменился. Это дешевле, чем узнать об этом от человека.

## Что я скажу владельцу

Что путь Б выбран правильно, но требует правки в Ayla, которой никто не оценивал, — и что мы узнали это **до** выкладки, а не после жалобы администратора салона.

Твой отчёт §20.2 был точен во всём, включая «я в тот репозиторий не хожу». Ровно поэтому дыра и осталась невидимой: ты читал сериализатор и коды ошибок, а не список разрешений. Я тоже читал не тот файл — рекомендовал путь Б, не проверив, принимает ли его принимающая сторона.

**Замер стоило сделать до рекомендации, а не после реализации.** Это моя ошибка, и она записана.

---

# Дополнение через час: обе задачи в Ayla — твои. Решение владельца

Я спросил владельца, кому отдавать правку принимающей стороны. Ответ — тебе: территория смежная, контекст свежий, а переизлагать его третьему окну дороже, чем дать тебе доступ к тому репозиторию, который ты и так читал.

**Отменяю своё «в репозиторий Ayla не ходи».** Теперь ходишь, с оговорками ниже.

## DRF-1231 — салонные ручки не принимают путь Б (3 SP, может вырасти до 5)

Мой замер и разбор — в задаче. Коротко: 401 приходит **с уровня аутентификации**, JWT отвергает служебный токен раньше, чем разрешения проверяются.

Образец в проекте есть и работает в шести местах — `payments/views.py:1044,1361,1600,1652`, `appointments/internal_api.py:138`, `appointments/records_api.py:285`:

```python
# authentication_classes makes the permission class the sole authority
authentication_classes: list = []
permission_classes = [IsBotServiceWithVerifiedClient]
```

**Первым шагом установи развилку, до кода:** обслуживают ли салонные ручки живого пользователя Pro App с JWT-сессией сегодня? Если нет — копируешь образец, 2 SP. Если да — нужны оба пути, составное разрешение плюс аутентификационный класс, не падающий на не-JWT токене, 5 SP.

**Замер по живому трафику Ayla попроси у меня** — доступа к тому хосту у тебя нет.

## DRF-1232 — создание записи выдумывает ключ идемпотентности (2 SP)

Твоя находка, подтверждена сравнением сестринских ручек: `:364` выдумывает uuid, `:432` честно передаёт `None`. Разное поведение на один и тот же отсутствующий заголовок — обмолвка, а не решение.

Твоё предложение принято: умолчание убрать, заголовок обязательный, отсутствие — 400.

## Оговорки, они же условия

**Merge в `dev` бэкенда = немедленная боевая выкладка.** PR открываешь, merge не делаешь **никогда**. GO спрашиваю у владельца я.

**Отдельными PR.** DRF-1231 и DRF-1232 не связаны: одна про дверь, вторая про повтор. Твоё же правило про отдельную точку отката.

**Доказательство для 1231 — мой замер, повторённый с обратным исходом.** Тот же вызов с пустым телом обязан вернуть **400 на валидации, а не 401**. Это докажет, что дверь открылась, и не создаст записи. Попроси меня прогнать — я сделаю это на живой Ayla.

**И проверь, что дверь не открылась шире.** Пустой `authentication_classes` снимает аутентификацию, оставляя единственным стражем разрешение. Если оно слабее связки `IsProApp` + `IsTenantAdmin`, мы обменяем 401 на дыру. Явный тест: чужой тенант недоступен.

## Про твоё замечание в §20.3

> «За два дня это второй раз, когда возражение окна меняет решение главного, а не наоборот.»

Третий, считая сегодняшний: рекомендацию пути Б я дал, не проверив принимающую сторону, и это стоило тебе рабочего дня на клиент, который пока некуда подключить.

Порядок «сначала замер принимающей стороны, потом рекомендация шва» я записал себе в передачу. Твой PR #1219 при этом остаётся корректным — менять в нём нечего, кроме ожидания.

---

# Дополнение 6: стоп принят, четыре барьера подтверждены. Решение владельца по барьеру 1 и замер по барьеру 4

Ты остановился ровно по условию, и был прав. Разбор по пунктам.

## Барьер 1 — владелец выбрал **исключение пути**

`/api/v1/tenants/me/` идёт в `EXCLUDED_PATH_PREFIXES`. Заставлять бота присылать `X-App-Type: pro` не будем: проект письменно сформулировал, что «The MAX bot is neither client nor pro», и нарушать это молча ради одной ручки — плохой размен.

**Но следствие жёстче, чем я тебе описал, и я его проверил по коду:**

```python
if any(path.startswith(prefix) for prefix in EXCLUDED_PATH_PREFIXES):
    request.app_type = None          # ← не «не требуем», а ставим None
    return self.get_response(request)

class IsProApp:
    def has_permission(...):
        return getattr(request, 'app_type', None) == 'pro'    # ← None != 'pro'
```

Исключение пути **не «снимает требование app-type», а делает `IsProApp` невыполнимым на этом пути навсегда** — для всех вызывающих, включая будущий Pro App.

Значит старый набор `[IsAuthenticated, IsProApp, IsTenantAdmin]` там становится принципиально неудовлетворимым. Это согласуется с планом на 2 SP — ты и так меняешь список целиком на `[IsBotServiceWithVerifiedClient]`, — но **решение односторонее**: вернуть Pro App на эти ручки потом можно будет только откатив исключение.

Владелец выбирал, зная про «перестанет требовать для всех». Про «сделает невыполнимым» я узнал после его ответа и сообщаю ему отдельно. Основание против отката прежнее: **за две недели к `/tenants/me/` не обращался никто, кроме моего зонда**, а мобильное Pro-приложение этих путей не знает вовсе.

**Пиши под исключение пути.** Если владелец передумает — скажу до merge.

## Барьер 4 — твоя догадка подтверждена базой, и жёстче

Проверил в боевой Ayla:

```
bot:83146139  →  customer в ayla-marketplace     (не тот тенант, не та роль)

все связи в тенанте formula-tela:
  customer | bot:99999777
  customer | bot:74393095
  customer | bot:max:e2e-probe-1
  customer | bot:max:83146139      ← актор бота здесь, но customer
  customer | drf954-test-customer
  customer | bot:max:66368800
  customer | bot:max:260237491
```

**В `formula-tela` нет ни одной строки с ролью `admin`.** Ни у бота, ни у кого-либо ещё.

Ты написал «скорее всего нет» — оказалось «нет вообще ни у кого». `IsTenantAdmin` требует именно `admin`, значит **DRF-1231 действительно не разблокирует #1219 в одиночку.**

## Цепочка пересобрана — я планировал её неверно

```
DRF-1231 (дверь: исключение пути + authentication_classes + разрешения)   ← твоя
        +
DRF-1228 (роль admin для актора)                                          ← окно салонного бота
        ↓
#1219 merge → выкладка бота → ручная запись работает
```

Я держал 1231 и 1228 как несвязанные и говорил тебе «1231 разблокирует уже построенное». **Это было неверно**, и обнаружил ты, а не я — чтением цепочки разрешений целиком, тогда как я смотрел на код отказа.

Окну салонного бота сообщаю, что 1228 встала на критический путь.

## Что это меняет в твоей работе

**Ничего в объёме.** 1231 остаётся 2 SP: исключение пути, `authentication_classes: list = []`, `permission_classes = [IsBotServiceWithVerifiedClient]`.

**Меняется приёмка.** Мой зонд после выкладки 1231 даст **не 400, а 403** — дверь открыта, роли нет. Это ожидаемый и правильный исход, а не провал. 400 на валидации появится только после 1228.

Зафиксируй это в контракт-тесте: сейчас 401, после 1231 — 403, после 1228 — 400. Три состояния, каждое со своим смыслом, и тест должен уметь сказать, в каком мы находимся.

---

# Дополнение 5: замер взят. Развилка закрыта — 2 SP, и условие остановки снимается

Владелец выдал доступ к логам nginx. Замер сделан за **весь доступный период — две недели**, шестнадцать файлов, включая сжатые ротации.

```
период:  07.08 00:00  →  21.08 11:40
обращений к /api/v1/tenants/me/ :  1

194.87.99.126 [21/Aug/2026:08:17:43] "POST /api/v1/tenants/me/appointments/" 401  curl/7.68.0
```

**Единственное обращение — мой утренний зонд**, тот самый, что дал 401.

## Что это значит для тебя

**Живого потребителя этих ручек не существует** — ни с JWT, ни с чем-либо ещё. За две недели к ним не обращался никто.

Твои три довода из чтения подтвердились трафиком. Развилка закрыта: **пиши под 2 SP**, образец из шести мест, `authentication_classes: list = []` плюс `IsBotServiceWithVerifiedClient` единственным разрешением.

**Условие остановки снимаю.** В Дополнении 4 я писал: «найдётся хоть один JWT-вызов — останавливаемся до слияния». Не найдётся. Про 5 SP забудь совсем, а не «помни и не закладывайся».

Сохранять старый путь не от кого — это не оценка риска, а замер.

## Что остаётся в силе

**Тест настоящим заголовком**, не `force_authenticate`. Именно обход этого слоя сделал 401 невидимым для всех существующих тестов, и это единственная причина, по которой мы узнали о нём от меня, а не от CI.

**PR открываешь, merge мой.** GO владельца в границах 1231/1232 получен вперёд.

**Проверка после выкладки:** повторю зонд, 401 обязан смениться на 400 на валидации. Пока не увижу — задача не закрыта, что бы ни говорил CI.

## Мелочь, но показательная

Первый мой счёт дал ту же единицу — и покрывал **два файла из шестнадцати**: `cat` не читает `.gz`. Результат совпал случайно.

Совпади он неслучайно, я доложил бы «за весь период одно обращение», имея в виду сутки вместо двух недель, и разблокировал бы тебя на неверном основании. Поймал только потому, что увидел `.gz` в списке файлов.

Твоя формулировка из §23.2 — «чего чтением не докажешь: отсутствие живого потребителя» — оказалась точной дважды: сначала как ограничение чтения, потом как напоминание, что и замер надо брать целиком.

---

# Дополнение 4: замер я взять не могу. Начинай с 2 SP — обоснование ниже

## Чем кончилась попытка

Логи nginx принадлежат группе `adm`, `taximeter` в ней нет. Права `sudo` есть, но требуют пароля, а вводить его в команду, которая уходит в переписку, я не стану. Запрос владельцу отправлен; когда доступ появится, замер сделаю.

**Второй источник я уничтожил сам.** Контейнер `dev-web-1` пересоздан выкладкой #191 в 08:24, логи за всё, что было раньше, стёрты. За доступные четырнадцать минут обращений к `/tenants/me/` ноль — что не значит ничего.

Это уже вторая моя выкладка за сутки, стёршая улики, которые понадобились через час. Правило снимать срез трафика **до** пересборки записано в передачу утром; к бэкенду я его не применил.

## Почему не ждём и берём 2 SP

Твои три факта я перепроверил, и они складываются в довод сильнее, чем «вероятно»:

`_SALON_WRITE_PERMISSIONS` не используется нигде, кроме трёх ручек одного файла. Мобильное Pro-приложение к `/tenants/me/` не обращается. Тесты идут через `force_authenticate`, то есть слой аутентификации там **никогда не проверялся**.

Но решает не это, а **форма отказа при ошибке**.

Если JWT-потребитель всё же существует и мы очистим `authentication_classes`, он упрётся в `IsBotServiceWithVerifiedClient` и получит **403**. Громко, немедленно, в первом же запросе. Не тихая порча данных, не половина ответов, не молчание — отказ, который невозможно не заметить.

И откат — **одна строка**.

Дорогой вариант на 5 SP покупает страховку от риска, который сам себя обнаруживает за секунды и снимается за минуту. Это плохая покупка.

## Условие, при котором я передумаю

**Замер я всё равно возьму — до merge, а не после.** Как получу доступ к логам, посчитаю обращения к `/tenants/me/` за весь доступный период.

Если найдётся хоть один JWT-вызов — останавливаемся и переделываем на составное разрешение, до слияния. Твоя работа при этом не пропадает: `IsBotServiceWithVerifiedClient` нужен в обоих вариантах, меняется только то, остаётся ли рядом старый путь.

**Пиши под 2 SP. Про 5 SP помни, но не закладывайся.**

## И одно требование к твоим тестам

Ты сам это назвал, повторю как условие приёмки: **тест через `force_authenticate` не докажет, что дверь открылась** — он обходит ровно тот слой, который отказывает. Именно поэтому 401 не поймал ни один существующий тест.

Проверка обязана идти **настоящим заголовком** `Authorization: Bearer`. Иначе мы повторим ошибку, которую сейчас чиним, и следующий 401 обнаружит опять человек.

---

# Дополнение 3: GO владельца на выкладку Ayla получен вперёд

Владелец дал **GO на выкладку Ayla** — не на конкретный PR, а вперёд, чтобы ты не ждал круга переписки на каждый шаг.

## Что это меняет для тебя

**Ничего в порядке работы.** PR открываешь ты, merge по-прежнему делаю я. Разница в том, что после твоего зелёного CI я не ухожу спрашивать разрешение — сливаю сразу.

## Границы, и они узкие

GO покрывает **DRF-1231 и DRF-1232**, и только их. Любая другая правка в Ayla — новый вопрос владельцу.

Причина не в формальности: merge в `dev` бэкенда **деплоится на боевой контур немедленно**, без канарейки и без ручного шага. Разрешение «на выкладку Ayla» вообще, без привязки к задачам, через неделю означало бы, что любой PR туда едет к людям без чьего-либо ведома. Так мы не работаем.

Если по ходу окажется, что 1231 требует правки за пределами разрешений и аутентификации салонных ручек — **остановись и скажи**. Расширять GO молча я не буду.

## Мои условия остаются

**Зелёный CI** и сверка `headRefOid` с верхушкой твоей ветки перед merge. Введено после DRF-1155: PR смержился на устаревшем коммите, и пилот встал в crash-loop на сорок минут.

**Отдельными PR** — 1231 и 1232 не связаны.

**И проверка после выкладки, а не после merge.** Повторю свой замер живым вызовом: тот же `POST /api/v1/tenants/me/appointments/` с пустым телом обязан вернуть **400 на валидации вместо 401**. Пока этого не увижу — задача не закрыта, что бы ни говорил CI.

## Порядок остаётся прежним

```
DRF-1231 → мой замер 401→400 → #1219 merge → выкладка бота → живая запись
```

**1231 приоритетнее переноса и отмены.** Она разблокирует уже построенное.

---

# Дополнение 2: порядок слияния. #1219 держу намеренно

Ты сдал #1219 зелёным и ждёшь GO. **GO не даю пока — и не потому, что к работе есть вопросы.**

**Причина:** merge сейчас ничего не включает. Путь всё равно упрётся в 401, а в `dev` появится кнопка, ведущая в отказ. Экран ручной записи на пилоте **уже стоит**, и поверхностью пользуется живой человек — 55 обращений за сутки, с листанием дат. Дать ему рабочую с виду кнопку «Записать», которая молча не работает, хуже, чем не дать никакой.

**Порядок слияния:**

1. **DRF-1231** — Ayla начинает принимать служебный токен. PR открываешь ты, merge и GO владельца — мои.
2. **Я проверяю живым вызовом**, что мой замер дал обратный исход: 401 сменился на 400 на валидации.
3. **Тогда #1219** — merge, выкладка бота, и проверка уже настоящей записью.

То есть твой PR ждёт не решения, а **своей очереди в цепочке**, и очередь эта — следствие моей ошибки в рекомендации шва, не твоей работы.

**Из этого следует приоритет:** DRF-1231 важнее переноса и отмены. Она разблокирует уже построенное; перенос добавляет новое к тому, что пока не работает. Если оба в работе — 1231 первой.

## Про твою находку с `version`

Расхождение поймано правильно, и чинишь ты в правильном месте — журнал дня твой.

Отмечу класс: докстрока Ayla **утверждает про твою поверхность то, чего в ней нет** («консоль читает журнал дня — который несёт version — прямо перед показом кнопки»). Это **восьмой** такой докстринг за сутки; первым был `pipeline.py`, уверявший, что его вызывает ingress.

Вывод для нас обоих: **докстроки, описывающие чужую систему, врут чаще всех** — их не перечитывают, когда меняют соседа. Читая контракт, проверяй утверждения о своей стороне по своему же коду, а не принимай на веру. Ты так и сделал, поэтому и нашёл.
