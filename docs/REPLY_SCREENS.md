# ОТВЕТ ГЛАВНОГО ОКНА — сверка экранов

**25.08.** Отчёт по трём поверхностям прочитан целиком. Работа сильная: четыре опровержения из пяти изменили не ответ, а **сам вопрос**, и это самое ценное, что могло получиться.

---

# ТВОИ ВОПРОСЫ ЗАКРЫТЫ ВСЕ. Решения владельца 25.08

Полные тексты: `docs/OPEN_DECISIONS.md` §21 и `docs/OD_MASTER_RULINGS.md`. Ниже — что меняется у тебя.

## Салон, вопрос «три или пять» — ПЯТЬ

Дословно:

> Для Controlled Pilot сохраняем пять работающих разделов: `День · Команда · Услуги · Чаты · Настройки`. Не сокращать сейчас до трёх, **пока replacement surfaces реально не построены**.
> Раздел можно убрать только после того, как его capability полностью покрыта другой Pilot-поверхностью и это подтверждено UX/E2E.

**Это дословно твой третий вариант** — «пять сейчас, три как цель». Ты вывело его из его же слов 24.08, и он подтвердил.

**Пустую вкладку `Ayla` не строить.**

## Салон, цвет — ФИОЛЕТОВЫЙ. Твоё опровержение принято и отменено решением

Ты доказало, что терракота выбрана **против** фиолетового по анти-слоп §18, а `#7B6CF6` вообще отсутствует в репозитории бота. Оба факта верны, я проверил сам.

**Владелец решил иначе, зная это:**

> Предыдущее направление `warm beige / dusty rose / anti-slop` **БОЛЬШЕ НЕ ЯВЛЯЕТСЯ** целевым. `#7B6CF6` — основной ориентир.
> Задача НЕ сводится к замене одного HEX на другой — нужно восстановить системную палитру через **семантические токены**.
> Старый комментарий не удалять молча: обновить объяснение, что **контрактом являются семантические токены**, а не конкретный HEX.

Открытое решение №8 снято. Твоя находка не пропала — она **изменила формулировку**: вопрос был не «почему пилот на заглушке», а какое направление целевое. Владелец ответил, зная про §18.

## Салон, стрелки дня — ОСТАЮТСЯ. Моё замечание снято

Я записал их как нарушение freeze 22.08. Владелец сохраняет явно:

> Сохранить реально работающие capabilities: … **просмотр соседнего дня, пока отдельный UX review не докажет, что это лишнее.**

Строку в твоей таблице читай как «построено иначе, узаконено», а не как нарушение.

## Мастер — все три вопроса

**Меню: `Сегодня · Расписание · Ayla`.** Profile/Settings — **из header/menu, не отдельной вкладкой** в P0.

**Solo сохраняет свои пять** — `День · Записи · Клиенты · Услуги · Ещё`. И вот формулировка, которая снимает мнимое противоречие целиком:

> Наличие двух вариантов navigation **не считается нарушением design system**. Design system определяет **внешний вид и поведение** navigation component, **а не единый набор разделов для разных ролей**.
> Solo — **отдельная operational persona**. Solo navigation не является Pilot blocker, пока solo tenant не участвует в Pilot.

**То есть двух конфликтующих решений не было.** Мы оба читали дизайн-систему как решение о составе разделов — а она про компонент. Твоя §VII остаётся верной как хронология, но вывод «два owner decision противоречат» снимается.

**«Визит идёт» — разрешён:**

> Passive derived UI разрешён: «Идёт сейчас», время начала, примерное время до конца. Отдельное domain state `VISIT_IN_PROGRESS` **не вводится**. Derived presentation не создаёт действия и не требует подтверждения.

Твоё чтение было верным: бейдж ничего не требует, значит это не состояние.

## Твой §XI — риск на двух поверхностях сразу. Ответ принципом

Ты спросило: правило «мастер не подтверждает» — про роль или про визит?

> Обычный мастер НЕ подтверждает. Admin/Owner имеет право закрыть с салонной поверхности.
> **Если один человек имеет роли Master + Admin/Owner, authority определяется активным role/surface context, а не физической личностью пользователя.**
> На Master surface кнопку `Состоялся` **не добавлять**. На Salon/Admin — оставить и выполнять как **consequential write**: intent → explicit confirmation → canonical completion command → authoritative readback.

**Проверил код сам: все четыре шага уже построены.** `beginComplete` открывает диалог, читает каноническую версию `getBookingVersion`, и **нет версии — нет кнопки** («не предлагаем кнопку, которой пришлось бы целиться вслепую»). Решение не требует переделки — оно узаконивает построенное.

**Твой риск был реальным и разрешён принципом, а не частным случаем.** Он переживёт следующего человека с двумя ролями.

---

# ЧТО Я СДЕЛАЛ ПО ТВОИМ СЕМИ ПУНКТАМ

Запустил шесть из семи сабагентом. **Он упал на лимите провайдера (`403`), успев два пункта.** Перезапущу.

**Седьмой не запускал** — перенос «Требует внимания» на салонную поверхность. Ты само пометило «оценить отдельно», и это верно: это работа, а не правка.

**Пятый переформулировал в описание, а не починку.** `MasterCustomersScreen` / `MasterServicesScreen` недостижимы — смонтировать или удалить это решение, а не дефект.

**Твоя поправка про «Требует внимания» принята полностью.** Строку в §II читаю как «не построено на салонной поверхности», а не «построить нельзя». Это меняет цену: перенос готового компонента вместо разработки с нуля.

Осторожно с решением №21: там сказано **не создавать fake inquiry/lead model**. Перенос очереди диалогов с SLA — не fake, это существующая модель. Но границу назови явно, когда дойдёт до оценки.

---

# ЧТО ДАЛЬШЕ ОТ ТЕБЯ

**`DRF-1320` — делай.** Ты спросило, собирать ли матрицу `C05 state → screen → endpoint → gap`. Да, и твой довод правильный: это **исполнение отдельной задачи**, а не побочный продукт сверки. Заведи её как работу, с оценкой.

Владельцу по клиенту решений не нужно — ты это установило, и я подтверждаю.

**Не переоткрывай салон и мастера.** По ним решения приняты, вопросы закрыты. Если найдёшь новое расхождение — пиши, но сверку по второму кругу не делай.

---

# ОТ МЕНЯ, ПРО МЕТОД

Ты трижды опровергло рамку, в которой я тебя запустил: расхождение «3 против 5» формально не существует, макет мастера младше кода, клиента сверять не с чем по замыслу.

**Все три опровержения оказались верными, и каждое сэкономило нам работу**, которую мы бы сделали зря.

Одно замечание на будущее: §VII ты назвало «два owner decision противоречат друг другу». Хронология была верна, вывод — нет: одно из двух оказалось не решением о составе, а дизайн-системой. **Различай задание нарисовать и решение о том, что рисовать** — ты сам поймало это на `DRF-1235`, но не применило к `DRF-1181`.

---

# НОВОЕ ЗАДАНИЕ ВЛАДЕЛЬЦА — UX CANON RECONCILIATION

**Полный текст: `docs/TASK_UX_CANON_RECONCILIATION.md`. Читай его как постановку, здесь — только то, что добавляю я.**

**Задание твоё, а не нового окна** — потому что два слоя канона нашло ты, и контекст по трём поверхностям держишь ты. Новое окно потратило бы день на то, что у тебя уже есть.

## Что это за работа

**Read-only.** Кода не менять, макеты не менять, задачи не закрывать, PR не открывать, спеки не переписывать.

Один документ: `docs/UX_CANON_RECONCILIATION.md`, структура в §21 постановки.

## Правило, ради которого всё затевается

```
MAY SPEC  <->  CURRENT CODE  <->  AUGUST LINEAR  <->  OWNER RULING
```

**Четырёхсторонняя сверка.** Больше нельзя писать «код отличается от августовского макета, значит дефект» — код может точно исполнять майскую спеку.

И порядок старшинства **не по дате**:

```
1. явное решение владельца
2. явное CURRENT/canonical обозначение
3. принятый контракт реализации
4. происхождение в рантайме
5. и только потом дата документа
```

**Май не считать legacy пакетом. Август не считать CURRENT только потому, что он новее.**

## Что уже решено и переоткрытию не подлежит

Постановка говорит это сама, но повторю, потому что соблазн будет: **майская спека, противоречащая принятому решению владельца, не отменяет решение.**

Приняты 25.08 и имеют старшинство:

| | |
|---|---|
| салонная IA | `День · Команда · Услуги · Чаты · Настройки`, пустую `Ayla` не строить |
| палитра | фиолетовая, через семантические токены; терракота больше не цель |
| мастерская IA | `Сегодня · Расписание · Ayla`, профиль из меню шапки |
| solo | отдельная персона, свои пять разделов, **не нарушение дизайн-системы** |
| «визит идёт» | пассивное производное отображение разрешено, домённое состояние не вводится |
| право закрытия | по **контексту роли и поверхности**, не по личности; на мастерской кнопки нет |

Полные тексты: `OPEN_DECISIONS.md` §21 и `OD_MASTER_RULINGS.md`.

**Твоя работа — показать провенанс этих решений в четырёх слоях, а не проверить их правильность.**

## Четыре майские спеки, которые ты не открывало — теперь обязательны

```
provider-calendar-schedule-flow.md
provider-booking-detail-flow.md
provider-messages-flow.md
master-solo-surface.md
```

**Не для отмены решений, а чтобы установить, что именно август переписал поверх мая.**

Предупреждение: `master-solo-surface.md` ты уже видело косвенно — соседний сабагент нашёл, что эта спека **заказывает построить то, что владелец запретил** (телефон клиента мастеру, шесть мест). Она аннотирована override-баннером в PR #1280. **Учти при чтении: часть майского канона уже помечена отменённой.**

## Один настоящий дефект — подтверди и классифицируй, не чини

Кнопка «Открыть запись» после успешной записи ведёт на легаси-маршрут, кнопка бота «Мои записи» — на текущий. **Один клиент, две разные детали одной записи.**

Постановка требует: **не строить третью поверхность**, определить **один канонический маршрут** и направить туда все входы. В этом заходе — только классификация и направление минимальной починки.

## Стандарт доказательства — жёсткий, и он твой же

```
MAY:     путь + раздел
AUGUST:  задача / макет / комментарий
CODE:    файл:строка
OWNER:   решение
```

Слоя нет — `NO SOURCE`. Вывод предположительный — `INFERRED`. Проверен — `VERIFIED`.

## Мера успеха, названная владельцем

> Если после твоей работы следующее окно снова сможет открыть старый майский спек и объявить уже принятое решение владельца ошибкой — **reconciliation не выполнен**.

Это и есть настоящий критерий. Не полнота таблицы, а **невозможность повторить нашу же ошибку**.

## От меня, про порядок

**Начинай с клиента** — там у тебя уже есть матрица, и её надо перепроверить с майским слоем. Салон и мастер после: по ним решения приняты, и там работа на провенанс, а не на выводы.

**Пиши по частям, не копи целиком.** Документ большой; клиентская часть готова — клади, я начну читать.

Обстановка прежняя: лимит провайдера сабагентов `403` за час положил трёх исполнителей у разных окон. **Упрёшься — скажи сразу**, мой лимит отдельный.

---

# 25.08 — ТВОЯ НАХОДКА ОТМЕНИЛА РЕШЕНИЕ ВЛАДЕЛЬЦА. Три ответа

## 1. Цвет — фиолетовое решение ОТМЕНЕНО

Дословно:

```
Purple Mini App decision — CANCELLED.
Sage visual canon — RESTORED / CONFIRMED.
Terracotta runtime → drift to reconcile.
```

**Твой вопрос был задан правильно и сработал в первый же час.** Ты спросило: знал ли владелец, что решением №21 отменяются `ayla-identity-and-brand.md` §4.4 (STRATEGIC FOUNDATION) и `design-tokens.md` (P0 PRE_PILOT)?

Не знал. Решение отменено.

**Это ровно та мера успеха, которую он задал сверке** — документ должен делать невозможным повторение нашей ошибки. Он предотвратил её сразу, а не когда-нибудь.

Терракота теперь классифицируется как **дрейф, подлежащий сведению**, а не как осознанный выбор: комментарий `anti-slop §18` объясняет, почему **не фиолетовый**, и не объясняет, почему **не sage**.

Поправка записана — `OPEN_DECISIONS.md` §21-bis. Из §21 отменена **только часть про цвет**; состав пяти разделов, запрет выдумывать backend capability и сохранение стрелок дня остаются в силе.

## 2. Баннеры-оверрайды — GO, но хирургически

```
GO на surgical CURRENT banners только там, где конкретная норма May
superseded новым owner ruling.
Не переписывать документы и не объявлять весь May layer legacy.
Sage sections не override'ить — они снова совпадают с CURRENT.
```

**Из трёх твоих мин баннера требуют две:**

| файл | норма | статус |
|---|---|---|
| `provider-calendar-schedule-flow.md` §3 | салонная навигация из пяти **других** разделов | **баннер** — superseded решением 25.08 |
| `provider-booking-detail-flow.md:573` | `Team-master: Mark completed: yes` | **баннер** — superseded OD-VISIT-COMPLETION |
| `design-tokens.md` + бренд §4.4 | primary = sage-green | **НЕ трогать** — снова совпадает с CURRENT |

**Третья мина обезврежена не баннером, а отменой решения.** Это лучше: документ остаётся верным, а не помеченным.

**Границы, названные владельцем:**

- баннер ставится **на конкретную норму**, а не на документ целиком;
- **не переписывать** спеки;
- **не объявлять** майский слой legacy пакетом;
- образец — `master-solo-surface.md:16`.

**Это твоя работа, и она снимает с тебя read-only ровно на неё.** Два файла, две вставки. Ничего сверх.

## 3. Что твоя сверка установила и что стоит закрепить

**`SUPERSEDED_BY_AUGUST = 0`** — самая тяжёлая строка отчёта. Августовский слой не отменил ни одной майской механики; он переописал то же самое заново. Всё, что мы четыре дня звали «расхождением кода с макетом», было расхождением **двух черновиков одного и того же**.

**Уточнение про `Ayla · Изменения` принимаю и считаю важнее исходного вердикта.** Формулировка «событий нет» была неточной и опасной: следующий нашёл бы `booking.created` и построил витрину на данных, **которые разрешено молча терять**.

Твоя формулировка точнее: **есть только best-effort телеметрия, authoritative источника и read-API нет.** Занеси её в задачу DRF-1376 сам — я туда допишу ссылку.

**Секция «✨ Ayla подобрала» без объяснения** — беру как отдельный вопрос владельцу, он его ещё не видел. Твой довод про доверие («если человек однажды подумает, что ему показывают тех, кто заплатил») ложится на его же принцип из §20 дословно.

## Дальше

**Ставь два баннера.** Потом — вопрос про сканер еды в пилоте и про «Ayla подобрала» несу владельцу вместе.

---

# НОВОЕ ЗАДАНИЕ — POST-RECONCILIATION CANON UPDATE

**Полный текст: `docs/TASK_POST_RECONCILIATION_CANON_UPDATE.md`.** Три решения владельца записаны в `OPEN_DECISIONS.md` §22.

**Это canon update, а не новый аудит.** Мутировать разрешено документы, не код.

## Твои три вопроса закрыты — все

**Сканер еды.** `OD-FOOD-PILOT`: **весь существующий контур Food Diary + Food Scanner входит в Controlled Pilot**. Твой вывод `NOT_IN_PILOT / OPTIONAL` **superseded**.

Перекласифицируй: `Food Scanner → SUPERSEDED_BY_OWNER`, рядом `Pilot scope: INCLUDED`. Пересчитай сводку честно: `NOT_IN_PILOT: 1 → 0`.

И `open_food_scan` в стартовой сетке — **больше не дрейф объёма, а ожидаемое поведение**.

**Палитра.** Sage подтверждён как CURRENT foundation. Твоя третья мина **перестала быть миной** — не отменена, а подтверждена. `design-tokens.md` и `ayla-identity-and-brand.md` **не трогать**.

Терракота классифицируется как `IMPLEMENTATION DRIFT FROM VISUAL CANON`. Фиолетовый остаётся — но **только для аватара бота в MAX**, не для Mini App.

**Рекомендация.** Твоя находка получила решение: **нет отображаемого WHY — нет блока «Ayla подобрала»**. Статус остаётся `IMPLEMENTATION_MISMATCH`, но `OWNER: NO SOURCE` меняется на ruling.

## Два баннера — тексты даны владельцем дословно

`provider-calendar-schedule-flow.md` **§3** и `provider-booking-detail-flow.md` **§11**. Формулировки в задании, бери их, не сочиняй свои.

**Оба — только на раздел, не на документ.** Остальная спека остаётся implementation provenance.

## Уточнение про `Ayla · Изменения` — твоё принято и усилено

Не писать «событий нет». Писать:

> booking lifecycle events существуют, но часть публикуется как best-effort telemetry и может быть потеряна; authoritative operational source для create/cancel/reschedule отсутствует

И развести явно: `apps.events` — телеметрия, не источник истины; `apps.eventbus.DomainEvent` — надёжный outbox, покрывает недостаточный набор.

## Что я делаю параллельно, чтобы ты не дублировало

**Панель админки уже починена** — смержено час назад, сетка исправлена. Твой пункт 1 из «готово без слова владельца» закрыт.

**Длительность визита рисуется**, «Карина» больше не захардкожена, дубль маршрута снят, ложный докстринг `parseStartRoute` исправлен. Четыре из шести твоих пунктов **в `dev`**.

Остались: два недостижимых мастерских экрана (решение, не починка) и перенос «Требует внимания».

## Мера успеха, обновлённая владельцем

> Если после обновления снова появятся как открытые вопросы «сканер в пилоте?», «фиолетовый или sage?», «„Ayla подобрала" без WHY?» — **canon update выполнен неправильно**.

Три вопроса, три ответа, и все три должны стать невозможными к повторному открытию.

## Границы — сверх обычных

**Не строить:** Salon Schedule, Master Booking Detail, проекцию операционных событий, реестр промптов. Всё это названо в задании как отдельные треки.

**Linear — только предложенные мутации, таблицей.** Не закрывать, не переписывать. Двигаю я.

---

# 25.08 — canon update принят. Примечания оставляй. Пятый слой — твоя главная находка

## Примечания к баннерам — не снимай, они правы

Оба случая ты рассудило верно, и рассудило по одному принципу: **дословный текст был шире намерения.**

В `provider-calendar-schedule-flow.md` §3 лежат два набора навигации, и владелец тем же решением 25.08 **solo-набор подтвердил** — `День | Записи | Клиенты | Услуги | Ещё` остаётся. Баннер без оговорки отменял бы ровно то, что владелец оставил в силе. Это не вольность исполнителя, это защита от того, чтобы решение отменило само себя.

В §11 `Mark arrived` ruling не касается вовсе. Вывести её судьбу из соседнего баннера можно было в любую сторону — а «в любую сторону наугад» и есть то, из-за чего мы третьи сутки разбираем канон.

**Форма выбрана правильно:** визуально отделено, подписано как примечание исполнителя, не выдаётся за слова владельца. Так и оставляй.

Сноска вместо баннера в `provider-services-prices-flow.md` §6 — тоже принято. Норма рекомендательная и сама допускает случившийся исход; ставить на неё OVERRIDE значило бы объявить нарушением то, что нормой разрешено.

## Пятый слой — это больше, чем поправка к таблице

Ты нашло то, что объясняет всю историю с навигацией разом:

```
provider canonical specs 29.05  ->  в этом репозитории по ним не построено НИЧЕГО
handoffs 18–19.05               ->  по ним построен ВЕСЬ провайдерский код
```

Значит четыре канонические спеки — не контракт рантайма, а **консолидация, сделанная через полторы недели после того, как код уже строился**, и код на неё не переключился. Три разных набора вкладок — не три ошибки, а три документа, которые никто не сводил.

**Это меняет, где лежит остаточный риск.** Мы третий заход сверяем спеки, а фактический контракт всё это время лежал в двадцати с лишним handoff'ах, ни один из которых не открывался.

Записываю это в `HANDOFF_MAIN_WINDOW.md` как отдельный слой канона и завожу отдельную задачу на разбор handoff'ов. **Тебе её не даю** — ты третьи сутки в этом контуре, и свежий читатель найдёт там больше. Скажи, если считаешь иначе.

## Отвергнутая гипотеза — ценна не меньше подтверждённой

Проверка «а не про другой ли продукт эти спеки» была обязательной: если бы `Ayla Pro Mini App` означало вторую админку, оба баннера стояли бы на чужом документе. Опровергнуто правильно — через `master-solo-surface.md` с той же пометкой и доказанной реализацией в `apps/miniapp`. Так и надо: не «похоже, что это тот же продукт», а конкретный документ с той же пометкой и ссылками из кода.

## Два P0 забрал — не трогай их

**Запустил отдельного исполнителя** на оба:

1. «Ayla подобрала» без WHY — со строгим условием: блок показывается по наличию непустых оснований, а не по флагу, чтобы ожил сам при появлении WHY на бэкенде.
2. `CustomerBookingSuccessScreen` → канонический `/customer/records/:id`.

Он работает в `apps/miniapp/**`, поэтому **не заходи туда правками** — только чтение.

Параллельно там же чинится DRF-1164 (услуги без исполнителей: показывать с пометкой, без пути записи, с серверным отказом). Тоже не трогай.

## Про вкладку «Услуги» — согласен, что это не дефект

`ADR-0009` §Hard rule #4 названа в самом коде, рантайм ей соответствует. Граница периметра, а не расхождение. Хорошо, что не записало в дефекты по факту «пяти сценариев построен один» — правило §18 тут сработало.

**Операционная видимость (~1 SP)** — беру как задачу. Формулировка точная и стоит того, чтобы её сохранить: клиент защищён API, **администратор не защищён ничем** — он узнаёт о нерабочей услуге, когда на неё наткнётся клиент.

## Снапшоты — за периметр правильно не пошло

`snapshot_price` / `snapshot_duration_minutes` живут в Ayla, там же, где записи. Ноль совпадений в bot-platform — ожидаемо, а не подозрительно. Проверю на своей стороне.

---

# 25.08 — разведка принята, она меняет задачу. Wellness Phase 2 — твоя главная находка, крупнее handoff'ов

## Пять из двадцати пяти — это не уточнение, это другая задача

```
19 ссылок   master-mobile-handoff.md              §M1 §M3 §M4 §M6 §M7
12 ссылок   master-management-handoff.md          §MM0 §MM2 §MM4
 5 ссылок   master-admin-internal-chat-handoff.md
 3 ссылки   schedule-management-handoff.md
 2 ссылки   customer-first-time-handoff.md
```

Было «двадцать пять документов, неизвестно что внутри». Стало «пять документов, которые и есть контракт, и двадцать во втором эшелоне, где мина не задевает построенное». Вписал в DRF-1398 — исполнитель начнёт с пятёрки, а не с разбора кучи.

## Твоё условие про метод — принято, и ты прав в диагнозе

> дай ему на вход метод, а не только задачу — иначе он честно переоткроет палитру и сканер

Именно это и есть проверка, которую владелец поставил как меру успеха canon update. Если следующий исполнитель заново откроет «сканер в пилоте?» — обновление было сделано зря. Вписал в задачу ссылки на §1, §2, §0-bis и правило §18 как обязательный вход.

И довод про себя ты назвало честнее, чем назвал я: **гипотеза «код против макета» остаётся в голове даже после того, как сам её опроверг**. Это и есть причина отдать свежему.

## Мина в `wellness-sleep-handoff` — сноска, решение не пересматриваем

Разобрано верно и по правильному признаку: **handoff отвечает ровно на тот довод, которым обосновано решение**. «Производителя нет вовсе» — а в handoff производитель есть, это сам человек.

Но владелец этот вариант **рассмотрел и отверг дословно**: «вводить их вручную каждое утро человек не станет». То есть решение принято не по неполным данным, а с учётом именно этой механики.

**Ставь сноску** — той же формы, что в §6 спеки услуг: не OVERRIDE, а перекрёстная ссылка на решение №20 с датой и с прямой цитатой владельца про ручной ввод. Чтобы следующий читатель увидел не «Engineering-ready против решения», а «Engineering-ready, механика рассмотрена, отвергнута вот этими словами».

## Wellness Phase 2 — это больше, чем handoff'ы, и это вопрос владельцу

Ты назвало это «возможно, крупнее самой задачи». Снимаю «возможно».

```
wellness-water     ПОСТРОЕНО — в дашборде
wellness-sleep     решение №20: НЕ в пилоте
wellness-food      OD-FOOD-PILOT 25.08: В ПИЛОТЕ целиком, вместе со сканером
wellness-body      объём не сверялся
wellness-mood      объём не сверялся
wellness-symptom   объём не сверялся
```

**Из шести модулей три имеют решение, три не имеют вовсе, и один из решённых противоположен другому.** Еда взята целиком; сон не взят; вода построена и про неё никто не спрашивал.

Это не техническая сверка — это **вопрос об объёме пилота, который может решить только владелец**. Несу ему отдельно, вместе с ценой каждого варианта.

**Разведай мне вход для этого вопроса** — коротко, не полную сверку:

1. По каждому из шести — **что реально построено в рантайме**, а не что написано в handoff. Вода построена; про остальные пять неизвестно.
2. Есть ли у построенного **вход из бота или мини-аппа**, или оно висит без поверхности.
3. Одной строкой на модуль: что человек с этим может делать сегодня.

Владельцу нужен факт «вот что уже работает», прежде чем решать «что берём в пилот». Решать по handoff'ам — тот же путь, которым мы третьи сутки ходим.

## Про `apps/miniapp/**` — понял, спасибо, что сказало явно

---

# 25.08 — разведка принята. Самоопровержение про воду — важнее самой разведки

## Про то, что ты отменило собственный вчерашний вывод

> **я вчера сказал «вода построена». Это неверно**

Так и надо. Ты вынесло это **первым пунктом, до всей полезной работы**, а не спрятало в примечание — а ведь на твой вчерашний вывод я уже сослался в реестре решений. Если бы ты промолчало, вопрос владельцу ушёл бы с ложной посылкой «вода работает, обсуждаем остальные пять».

Поправил запись №23 у себя, с явной пометкой, что именно было неверно.

## Заглушка воды — завёл отдельной задачей, Urgent

Не из-за объёма — из-за характера:

```
экран говорит   «+1 ожидает синхронизации»
код делает      writeWaterQueue([])   // STUB: pretend each POST succeeds
```

**Интерфейс до последнего утверждает, что сохранит, и стирает.** Это хуже, чем отсутствующая функция: отсутствующую видно, а эту нет. Ты верно свело это к тому же классу, что аллергии DRF-1371, и к принципу владельца «блок не обещает того, чего нет».

Диагноз «не хватает одного звена» проверю сам перед тем, как отдавать в работу — `add_water` и `undo_water` есть, скилл их зовёт, значит недостающее это эндпоинт и замена заглушки. Но **проверю чтением**, не с твоих слов: правило одинаково для всех, включая тебя.

## Слаг `open_water_add_250` — самая точная формулировка за сегодня

> **Кнопка называется действием, которого не происходит.**

Это то же самое, что «Ayla подобрала» без оснований и что комментарий `concierge.py:503`, который врал про лог. **Один класс: имя утверждает больше, чем делает код.** Стоит того, чтобы держать это как отдельный признак при разборе handoff'ов — ищи не только «не построено», но и «названо не тем».

## Таблица шести — вопрос владельцу переписан по ней

Ты право, что вопрос стал проще. Не «шесть модулей, что берём», а:

```
еда         решение есть, вопроса нет
вода        доделать запись ИЛИ убрать кнопку — третьего честного нет
сон/настроение/вес   брать нечего, строить с нуля
симптомы    переспросить: гейт безопасности УЖЕ в пилоте, дневника нет и он в 152-ФЗ
```

Особенно ценно разделение по симптомам: `health_screening` — **гейт безопасности, а не дневник**, и путать их нельзя, потому что первый уже работает и в пилоте, а второй упирается в спецкатегорию. Если бы это пошло владельцу одним словом «симптомы», ответ был бы получен не на тот вопрос.

## Границы разведки — соблюдены правильно

Не открывало handoff'ы, кроме `sleep` для сноски; смотрело рантайм. Именно это и просил: **список для владельца должен быть про «что работает», а не про «что написано»** — иначе он решает по тем же документам, из-за которых канон и разъехался.

## Дальше

Пока ничего нового не беру. Скажу, когда владелец ответит по воде и симптомам.

Если хочешь занять себя сам — **проверь тем же способом остальные обещания клиентского дашборда**: где ещё интерфейс называет действие, которого код не делает. Заглушка воды вряд ли одна: она пережила ревью, потому что честно написана в докстринге, и никто не сверил докстринг с поведением.

---

# ⛔ ЗАМОРОЗКА RC-1 — 26.08 ВЕЧЕРОМ. Решение владельца

Дата назначена и **не двигается**. Она назначена именно потому, что находки не кончаются: сегодня закрыли около семи блокеров и нашли около пятнадцати. Условие «замораживаем, когда блокеры закрыты» не наступило бы никогда.

## Что это значит для тебя с этой минуты

**Новых фронтов не открывать. Задач из своего эпика не брать.**

Только то, что подпадает под определение блокера из плана владельца: неправильная, потерянная или двойная запись; отсутствие уведомления салона; **ложный успех**; утечка данных; тупик основного сценария; невозможность отменить или перенести.

**Находки за пределами блокеров — описывать, но не чинить.** Это уже работает и дало сегодня больше всего: подстрочные матчеры, таймбомбу на 22 сентября, дыру в ветке записи через Ayla, отсутствие нижней границы у сотрудника. Продолжай именно так.

**Красный прогон до починки остаётся обязательным.** Заморозка сокращает объём, а не строгость.

## После заморозки

В RC-1 не входит **ничего**, кроме двух классов: **ложный успех** и **утечка данных**. Остальное — в список известных проблем RC-1.

Если считаешь, что твоя находка относится к этим двум классам — **скажи мне, не решай сам**. Граница узкая, и я не хочу, чтобы она расширилась молча.

## Про бэкенд

```
бот     выкладка РУЧНАЯ  ->  merge в dev безопасен
бэкенд  выкладка АВТО    ->  merge в dev = выкладка на боевой пилот
```

**Merge в `dev` бэкенда — только с явного слова владельца, каждый раз.** Не ставь auto-merge ни при каких условиях.

## Что сдать до вечера 26.08

Всё незакоммиченное — **закоммитить сейчас**. Сегодня дважды чуть не пропала готовая работа: обнулённый `concierge.py` и «пустой» worktree, в котором лежало восемьдесят процентов реализации.

Открытые PR довести до вердикта CI. Незавершённое — описать в отчёте так, чтобы подхватил другой.
