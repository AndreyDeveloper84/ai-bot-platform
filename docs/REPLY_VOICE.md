# ОТВЕТ ГЛАВНОГО ОКНА

---

# СРОЧНО, ДО ПЕРВОГО ПРОГОНА ТЕСТОВ — окружение сломано почти везде

**Замерено мной 25.08. Твои сабагенты упрутся в это на первом же прогоне, если не предупредить.**

```
worktree бота всего        41
из них с .venv             23
из них РАБОЧИХ              1
```

Единственный рабочий: **`C:\Users\user\PycharmProjects\ai-bot-platform-drf1355\.venv`**

Остальные падают на:
```
ImportError: cannot import name 'SOURCE_INFERRED' from 'ayla_ai_core'
```

Системный Python падает раньше: Django **5.0.6** при нужных **5.2**, `CheckConstraint(condition=...)` не импортируется.

## Причина моя

За ночь `ayla-ai-core` перепинивался дважды: `2adf54b` → `af620ba` → `ee6425a`. Каждое окружение, собранное раньше последнего пина, несёт старый пакет.

**Это не поломка кода.** Отказ выглядит как дефект, но им не является — сабагент потратил на это время дважды, прежде чем понял.

## Что делать твоим сабагентам

**Предупреди каждого в брифе.** Заимствовать `drf1355/.venv` можно **только на чтение**: один `pip install` в нём — и он сломается у всех, включая меня.

Правильный путь — собрать своё из `uv.lock` свежего worktree. Заведено задачей **DRF-1384**, чинится отдельно; до починки это ручная работа.

**Если сабагент сообщает про `SOURCE_INFERRED` — это окружение, не его код.** Не пускай его чинить импорт.

---

# Обстановка на утро 25.08

**Потолок CI поднят до 60 минут** — по тобит-подгонке, не на глаз: старые 45 стояли в 0.32σ от медианы, то есть были подбрасыванием монеты. Порог перепроверки записан в сам комментарий: **если хоть один прогон умрёт о 60 в следующие двадцать — цифра неверна**.

**Один PR — один прогон.** До этой ночи каждый PR давал два, и они душили друг друга; хуже — **каждый мерж в `dev` глушил проверку предыдущего мержа**, и три четверти слитого за двадцать пушей приехало **без вердикта CI**.

**`replay` проходит за ~1:10.** До этой ночи он отменялся по потолку на каждом прогоне репозитория — сигнала регрессии промптов не было сутки.

**Джоб идёт 41–44 минуты**, из них 88% — `pytest apps/` одним процессом: `pytest-xdist` в `uv.lock` отсутствует вовсе (DRF-1383, открыта).

## Известные лотереи — не твои, не чини

| | частота |
|---|---|
| `test_tampered_token_rejected` | 1 из 65 (DRF-1379, чинится) |
| `test_suppress_log_contains_draft_id…` | 1 из 530 (DRF-1380, чинится) |
| редактор трасс `apps/replay/redactor.py` | 3.15% (DRF-1382, открыта) |

## Правила, которые я не буду повторять

**Linear устаревает.** За сутки 32 задачи из 61 смерженных числились открытыми. **Перед каждым брифом сабагенту:** `git log origin/dev --grep DRF-XXXX`. Тридцать секунд против часа переделывания.

**PR против `dev`. Мержу я**, после проверки. Сам не мержи никогда.

**Не запускай двух сабагентов в один worktree.**

**Требуй коммитить промежуточное**, не копить.

**Секреты не печатать.**

---

# 25.08 — доказательство принято, и две твои находки важнее его

**Проверил лог пилота сам, не по твоему отчёту.** Одна строка `orchestrator.intent_resolution.ok`, трасса `5536c26e` — та самая. **DRF-1310 закрыт живьём**, долг висел с вечера.

Ты сделало это **первым действием**, как просил бриф. Это правильный порядок: сначала закрыть то, что доказывается за минуту, потом браться за то, что требует дней.

## Находка первая — она меняет методику проверки, не только твою

> Резолвер стреляет только на глобальном клиентском пути. На tenant-пути крюка нет вовсе.

**Это надо записать в передачу, и я запишу.** Потому что следующий, кто пойдёт проверять резолвер на tenant-боте и не найдёт `intent_resolution.ok`, сделает вывод «сломалось» — и полезет чинить работающее.

Твоя формулировка точная: **«не сломалось, а не подключено»**. Разница в том, что первое чинят, а второе решают.

И отдельно ценно, что ты это нашло **своей ошибкой** — два хода ушли не туда. Ошибка, разобранная до причины, стоит дороже, чем ход, сразу попавший в цель.

## Находка вторая — про боль, и она задевает соседей

> Ход со словами про боль уходит в детерминированный уточняющий сценарий без консьержа — ~1 с, без резолвера.

Значит **часть ходов не доходит ни до консьержа, ни до блока памяти, ни до резолвера**. Для тебя это про чтение логов. Но следствие шире:

**Для окна целей** — их Gate D не увидит такие ходы вовсе.
**Для проверки исходящего** (DRF-1210) — надо понимать, покрыт ли этот путь; он про боль, то есть ровно тот класс, ради которого проверка и строилась.

Я передам обоим. Тебе — не отвлекаться на это, просто знать.

## Твой порядок дальше принимаю без правок

Третий путь → сведение голоса → шесть шагов. Именно в этом порядке.

**Одно уточнение по третьему пути.** Когда будешь разведывать, где консьерж выбирает инструмент, — **проверь, сохраняется ли этот выбор хоть где-то сейчас**. Если он уже пишется в метрику или лог, задача сжимается до «читать оттуда». Если нигде — это объясняет, почему резолвер спрашивают заново, и цена третьего пути выше, чем я оценил.

Скажи цифру, когда узнаешь. Я оценил его как «бесплатно», и **эта оценка моя, непроверенная** — опровергни, если она неверна.

## Обстановка

Прочти `REPLY_VOICE.md` выше про окружение: **рабочий venv один из двадцати трёх**, остальные падают на `SOURCE_INFERRED` — это не дефект кода, это устаревший пин.

Потолок CI поднят до 60 минут. Один PR — один прогон. `replay` жив, проходит за ~1:10.

---

# 25.08 — РЕШЕНИЕ ВЛАДЕЛЬЦА ПО ОБЪЁМУ. Вариант (а)

Дословно:

```
GO на вариант (а).
Prompt Registry runtime integration → отдельный track, сейчас NO-GO.
```

**Реестр промптов не трогай.** Ни `apps/promptreg/`, ни `apps/voice/`, ни `persona.BrandVoiceConfig`. Твоя находка про **tenant-less зазор** — `get_prompt()` требует тенанта, а глобальный клиентский бот его не имеет — и есть причина, по которой это отдельный трек: подключать реестр, не решив зазор, значит строить на песке.

## Что входит в (а)

```
discovery.py:393   читает voice['assistant_name'] из сырого dict мимо assistant_identity()
handler.py:198     _WELCOME_TEXT
faq/skill.py:75    «Алина, администратор салона»
booking/skill.py:162  ДОСЛОВНЫЙ дубликат той же Алины
```

## Про границу с окном записи — снимаю, узко

Ты назвало конфликт честно: свести голос полностью, не заходя в `apps/skills/booking/**`, нельзя.

**Разрешаю тронуть `booking/skill.py:162-171` — и только эти строки.** Это дословный дубликат константы, не логика записи. Ничего другого в этом каталоге.

**Предупреждаю окно записи отдельно**, чтобы не удивилось конфликту.

**Моя ошибка:** я поставил границу по каталогу, не проверив, что цель лежит по обе её стороны. Границы надо ставить по работе, а не по дереву файлов.

## Про твоё опровержение брифа — принято целиком

Замер 22.08 устарел: коммит `99824a6` того же дня уже свёл имена в `persona/voice.py`, и хардкода в `ai_drafts.py:368` больше нет.

**Ты написало это сразу, до начала работы** — ровно как просит протокол. Час не потрачен зря.

И нашло больше, чем было в замере: **неподключённых механизмов три, а не один**. Это отдельная находка, и она пойдёт в трек реестра, когда владелец его откроет.

## Про третий путь — моя оценка была неверна, и я это записал

Я сказал «бесплатно, просто читай выбор консьержа». Ты доказало обратное:

```
имя инструмента    только ПОСЛЕДНЕЕ за ход, без аргументов
аргументы          нигде — record_global_message их дропает сознательно
concierge.py:503   утверждает, что выбор в логе — ЛОЖЬ, там только отказы
```

**Двенадцатый ложный комментарий за сутки.**

Верна была только половина: детерминированный путь **не создаёт LLM-клиента вовсе**. Трассу пришлось пронести — и ты его пронесло через двадцать точек возврата.

**Ветку пушь, PR открывай.** Смержу по проверке. Флаг на пилоте — отдельно, это GO владельца.

## Про обнулённый `concierge.py`

Решение восстановить из git без спроса **правильное**, и обоснование верное: ноль байт — потеря, а не правка.

**Но это второй за сутки случай, когда незакоммиченная работа едва не пропала** — первый был в каноне, где решение владельца лежало пять суток. Я записал это в передачу как класс.

Твои тесты-спецификации оказались **единственным описанием утраченного**, и по ним ты восстановило реализацию. Это лучший довод за то, чтобы писать их первыми, какой я видел.

---

# 25.08 — что дальше по голосу, и один вопрос, который больше твоего

## Вариант (а) — работай, GO подтверждён

Четыре точки, включая разрешённую строку в `booking/skill.py`. Окно записи предупреждено.

## Ветку третьего пути пушь и открывай PR

Смержу по проверке. **Флаг `INTENT_RESOLUTION_FROM_TOOL_CHOICE_ENABLED` на пилоте — отдельный GO владельца**, я его подниму вместе с другими выкладками.

## Вопрос, который стоит поднять сейчас, а не после (а)

Ты нашло три построенных и неподключённых механизма голоса и **архитектурную причину**, по которой реестр не подключается: `get_prompt()` требует тенанта, глобальный клиентский бот tenant-less.

Владелец закрыл реестр как отдельный трек — **NO-GO сейчас**. Правильно.

**Но вопрос, который из этого следует, ещё не задан:** нужен ли реестр промптов **вообще**?

Сегодня три механизма стоят построенными и неиспользуемыми. Это не «надо доделать» — это **соблазн для следующего**, который найдёт `promptreg`, решит, что его забыли подключить, и потратит неделю на tenant-less зазор.

**Когда закончишь (а) — сформулируй этот вопрос владельцу одним пунктом**, с ценой обоих ответов:

- **нужен** — что именно требуется решить (зазор, сиды, восемь точек потребления) и сколько это стоит;
- **не нужен** — что удалить и что при этом теряется.

**Не решай сам и не начинай ни то, ни другое.** Просто поставь вопрос так, чтобы на него можно было ответить.

## Про обнулённый `concierge.py` — я записал это в передачу как класс

Второй случай за сутки, когда незакоммиченная работа едва не пропала. Первый — канон, где решение владельца пролежало **пять суток**.

**Твои тесты-спецификации оказались единственным описанием утраченного**, и по ним ты восстановило реализацию. Это лучший довод за то, чтобы писать их первыми, какой я встречал.

**Требование на будущее, и оно теперь общее для всех окон: коммить промежуточное перед любой рискованной операцией.** Не «когда работа готова», а «когда есть что терять».

## Обстановка

CI: потолок 60 минут, но **порог сработал** — один прогон умер и о него. Джобы 41–50 минут. Красное теперь настоящее.

Лимит провайдера `403` за сутки положил **четырёх** сабагентов у разных окон. Упрёшься — говори сразу.

---

# `#1296` упал на форматировании — два файла, оба твои

```
упал шаг: ruff format check   (2м21с, до тестов не дошло)

Would reformat: apps/orchestrator/intent_resolution.py
Would reformat: apps/orchestrator/tests/test_intent_resolution_tool_choice.py
```

`ruff format` и пуш. Логика не проверялась — прогон умер на втором шаге.

**Обстановка изменилась за последний час:** в `dev` уехало восемь PR, включая `DRF-1310` (сторож подделки резолвера) — он трогает `apps/orchestrator/tests/`. **Перебазируйся перед пушем**, иначе получишь конфликт как соседнее окно.

---

# 25.08 — #1296 всё ещё красный по формату. Повторяю, потому что вижу тот же прогон

Голова `3c635c2`, прогон `ci` → **FAILURE**, шаг `ruff format check`:

```
2 files would be reformatted, 1346 files already formatted
```

Остальное зелёное: replay-фикстуры зелёные, miniapp зелёный, `mergeable = MERGEABLE`. **Мержу сразу, как формат станет зелёным** — больше ничто не держит.

Оба файла твои, я это проверил диффом относительно `origin/dev`, а не по знакомым именам: тот урок в этом контуре уже был.

Сделай у себя в worktree:

```
uv run ruff format apps/
git add -A && git commit -m "style: ruff format"
git push
```

**Не ребейзи перед этим.** Ветка `MERGEABLE`, конфликта нет, а лишний пуш во время идущего прогона снова его отменит.

## Про третий путь — жду только формат

Код готов, тесты зелёные, живое доказательство упирается в merge и флаг `INTENT_RESOLUTION_FROM_TOOL_CHOICE_ENABLED=1` на пилоте. Флаг ставлю я после merge, доказательство снимем вместе.

Твоя находка про `concierge.py:503` («комментарий врёт про общий лог выбора инструмента») — отдельная ценность, она объясняет, почему моя оценка «бесплатно, просто читай» была неверной. Я её признал в отчёте. После формата заведи её отдельной строкой, чтобы комментарий поправили: враньё в комментарии дороже отсутствия комментария.

---

# 25.08 — #1297 принял. #1296 не сдвинулся: голова та же, формат не запушен

## Сначала про то, что почти наверняка вводит в заблуждение

Ты пишешь про #1297: «ruff чисто». Верю. Но у #1296 падает **не тот ruff**:

```
шаг:  ruff format check
итог: 2 files would be reformatted, 1346 files already formatted
```

`ruff check` (линтер) и `ruff format --check` (форматтер) — **две разные проверки**, и первая проходит при упавшей второй. Если ты гоняешь `ruff check`, ты не увидишь этого падения никогда.

Проверяй так:

```
uv run ruff format --check apps/
```

Голова #1296 — по-прежнему `3c635c2`, ровно та, на которой прогон и упал. Пуша не было.

## #1296 — ничто, кроме формата, его не держит

```
mergeable                              MERGEABLE
replay fixtures (golden + adversarial) SUCCESS
miniapp (typecheck + vitest)           SUCCESS
pytest + ruff + mypy                   FAILURE  <- только ruff format
```

Отформатируй, запушь — мержу сразу и ставлю флаг на пилоте.

## #1297 — состав принят, отклонение по `_WELCOME_TEXT` тоже

Прогон `ci` ещё идёт, вердикта нет. Мержу по `conclusion`, не по цвету в списке.

**Отклонение по приветствию разобрано правильно.** «Пенза» и описательная часть — не identity-поля ни в `assistant_identity`, ни в зеркале ai-core; собрать их «полностью» значило бы либо изменить видимый пользователю текст, либо завести новые поля под задачу, которая про сведение, а не про расширение. Частичная привязка + честная запись в коде — верный выбор.

**Комментарий 160–161 в `booking/skill.py` правильно не тронут.** Он не лжёт: tone и forbidden там по-прежнему inline. Это ровно та проверка, которой не хватило в истории с `concierge.py:503` — там комментарий врал, и никто не сверил.

**`is`-проверка через f-string** — хорошо. Литерал бы интернировался, и тест зеленел бы при двух разных источниках. Это тест, который умеет падать.

## Про границу в `booking/**` — импорт засчитан

Вынужденное дополнение к разрешённым строкам, без него `DEFAULT_SALON_PERSONA` не достать. Границу я ставил по работе, а не по дереву файлов — импорт в неё попадает.

## Дальше — да, шесть шагов DRF-1209, но с оговоркой

Не останавливаю. Одно условие: **разведку первого шага делай чтением, ветку от `origin/dev` заводи после мержа #1297** — иначе будешь строить на голове, которой в `dev` не станет.

И держи в уме: `origin/dev` ушёл на +10 не случайно, там сейчас параллельно четыре исполнителя. `apps/miniapp/**`, `apps/miniapp_api/**`, `apps/master_api/**`, `apps/skills/health_screening/**` заняты — не заходи туда шагами DRF-1209 без разговора со мной.

---

# 25.08 — порядок подтверждаю. Шаг 12 решаю: расширять `evaluate_outbound`, post_check не портировать

## Сначала про метод

**Ты сверила «что уже сделано» с кодом, а не с аудитом, и снова оказалась права.** Это третий раз за сутки, когда основание врёт: сначала `concierge.py:503` про лог выбора инструмента, потом замер 22.08 про сведённые имена, теперь аудит DRF-1209, который **спорит сам с собой** — строка 74 против собственной таблицы на строке 213.

И ты разрешила спор правильно: не «в аудите два утверждения, беру новое», а **по коду**. `handler.py:1620-1622` говорит прямо «deliberately NOT done here» — значит верна таблица. То, что дал S1-C, это handoff-половина шага 10.5, а не шаг 9.

## Порядок — принят твой, без изменений

```
1. шаг 10.5 confidence-floor          1 день, изолирован
2. AIRequestMetric на не-консьержных   1–2 дня
3. шаг 18 replay-recorder на живом     2–3 дня
4. шаг 12 — решение ниже
5. шаг 5 memory-snapshot               2–4 дня, самый дорогой
6. шаги 9-строгий и 13                 после п.4
```

Дешёвое и обособленное первым — правильно, и правильно, что ты не начала, пока не подтвердил: переделка дороже ожидания.

## Шаг 12 — решаю: расширять `evaluate_outbound`, post_check НЕ портировать

Основание простое: **`guard_outbound` уже стоит на обоих путях, а `post_check` не стоит нигде.** Портировать второй сторож туда, где уже работает первый, значит завести два места, где решается один вопрос — и следующий читатель не будет знать, какое из них настоящее. Мы этот класс сегодня разбираем весь день.

Поэтому:

**Расширить `evaluate_outbound` до недостающих паттернов** — PII, ключи, brand-voice. Не переносить код post_check как есть, а взять из него именно те проверки, которых не хватает.

**PII — первым и отдельно от остальных двух.** Сегодня в этом же контуре найдена живая утечка: `conversations_list` отдавал четыре цифры телефона клиента, и поймал это не сторож, а тест PII-границы. Решение владельца DRF-1039 не оставляет исключения для частичного номера. Сторож исходящего, который не умеет PII, — это дыра ровно того размера.

**После расширения — `post_check` не оставлять живым мёртвым кодом.** Либо удалить, либо пометить в самом коде: «не вызывается, проверки перенесены в `evaluate_outbound`, не подключать». Иначе через месяц кто-то найдёт его, решит, что забыли подключить, и заведёт второго сторожа — ровно так, как это случилось с `promptreg`.

## Тринадцатый ложный комментарий — завожу это отдельным классом

`concierge.py:181` утверждает, что рекордер пишет `ReplayTrace` на живом пути. На живом пути рекордер не зовётся вообще.

Тринадцать — это уже не случайность, а свойство кодовой базы. **Веди список дальше**: файл, строка, что утверждает, чем опровергается. Когда закончишь разведку — отдай списком, заведу одной задачей.

Формулировка, к которой мы пришли сегодня в двух местах независимо: **имя или описание обещает больше, чем делает код**. У неё есть подвид дороже — **заглушка, которая не признаётся в этом на своём уровне** и полагается, что прикроет кто-то этажом выше.

## Про оба PR

**#1296 — по-прежнему только `ruff format --check`**, голова не сдвинулась. Напоминаю: это не `ruff check`, который у тебя зелёный. Отформатируй и запушь — мержу сразу.

**#1297 — прогон `ci` ещё идёт.** Как добежит с `conclusion: success` — мержу.

Начинать шаг 10.5 можешь, не дожидаясь мержей: он изолирован, ветку бери от `origin/dev`.

---

# 25.08 — про голову #1296 ты права, я ошибся. И про `test_shadow_turn` — проверил, это не «настоящий красный»

## Голова

Проверил сейчас: `556211d`, `MERGEABLE`. Ты права — разошлись на минуту, я прочитал PR до того, как пуш дошёл. Ошибка моя, не твоя.

**#1297 смержен** (`96eae9d`), вместе с ним `9d7d653` (остаток «бол») и `99bc62d` (подборка с обоснованием). Голос в `dev`.

## `test_shadow_turn.py` — 12 падений: это окружение, а не дефект

Проверил три вещи, прежде чем отвечать:

```
файл лежит в  apps/orchestrator/tests/test_shadow_turn.py   -> входит в «pytest apps/ (full suite)»
deselect на него                                            -> НЕТ
прогон ci.yml на a6e5287 в dev                              -> success
```

То есть **на том самом коммите, где ты видишь 12 падений, CI зелёный, и файл в прогоне участвует.**

Наиболее вероятная причина — **база**. Соседний исполнитель сегодня гонял на SQLite, потому что нативный Postgres на `:5432` не его и пароль не подошёл. CI гоняет на Postgres. **Скажи, на чём гоняла ты** — если на SQLite, вопрос закрыт и гоняться за этим не надо.

Если на Postgres — тогда это интересно, и я разберу отдельно.

**Не трать на это время до ответа.** Ты правильно вынесла находку как «пре-существующее, не моё» и правильно проверила подстановкой base-версии файла — метод верный, вывод «чьё-то настоящее красное» просто не подтвердился.

## Шаг 10.5 — принят, и находка про шов ценнее самого шага

> шов `turn_seam.py` обрезал `confidence`

**Без этого шаг был бы не «сложным», а невозможным** — и обнаружилось это только потому, что ты написала красный roundtrip-тест до реализации. `None != 0.42` — ровно то падение, которое доказывает, что чинишь существующее, а не воображаемое.

Это второй раз, когда шов молча терял поле: первый — `tool_trace` в #1296. **Шов теряет всё, чего в нём не перечислили, и делает это беззвучно.** Стоит подумать, нельзя ли сделать так, чтобы новое поле `DiscoveryReply` не доезжало до `TurnReply` **шумно**, а не тихо. Не сейчас — но запиши, я заведу.

**Мутационная проверка — то, чего обычно не делают.** Инверсия гейта → 5 failed, `>= → ≤` → 5 failed. Тест, который умеет убить мутацию, отличается от теста, который просто зелёный. Так и продолжай.

**Механику AdminTask не продублировала** — правильно, и по той же причине, по которой я решил шаг 12 в пользу `evaluate_outbound`: два места, решающих один вопрос, хуже одного несовершенного.

## Telegram-handler — не переноси

Ты права, что helper изолирован и перенос тривиален. Но **тривиальность переноса не основание для переноса**. Telegram сейчас не в пилоте, а каждое место, куда floor доедет, придётся потом проверять живьём. Запиши как известное расхождение, оставь.

## Реестр ложных комментариев — веди дальше

Два записаны. Формат хороший: утверждает / опровергается. Когда наберётся, заведу одной задачей — тринадцать штук по счёту окна сверки, значит вы двое ведёте один список с разных концов, надо будет свести.

## Шаг 2 — начинай

Порядок подтверждён (запись выше по файлу). Ветку от свежего `origin/dev` — он ушёл на три коммита вперёд с момента твоей.

---

# 25.08 — #1296: формат зелёный, вскрылся mypy. Это настоящее, чинить

Голова `556211d`, прогон `ci` → `failure`, шаг **`mypy`**:

```
apps/orchestrator/intent_resolution.py:754: error: Argument 1 to "build_draft_from_tool_choice"
    has incompatible type "Any | None"; expected "str"   [arg-type]
apps/orchestrator/intent_resolution.py:754: error: Argument 2 to "build_draft_from_tool_choice"
    has incompatible type "Any | None"; expected "dict[Any, Any]"   [arg-type]
apps/orchestrator/intent_resolution.py:760: error: Argument 1 ...  expected "str"
apps/orchestrator/intent_resolution.py:760: error: Argument 2 ...  expected "dict[Any, Any]"
```

## Проверил по своему же правилу — это не ложное срабатывание

`intent_resolution.py` — **твой файл, твоя ветка**. Каталог, где упало, ветка трогает. Значит дефект настоящий, а не пятая причина ложного красного.

## Почему вскрылось только сейчас

Шаг `pytest + ruff + mypy` идёт последовательно и обрывается на первом отказе. Раньше он падал на `ruff format --check` **до** mypy — и mypy просто не выполнялся.

**Починка одной проверки открыла следующую.** Это не регресс от твоего формат-коммита; это то, что было под ним всё время.

Стоит запомнить как приём: **зелёная проверка после красной не значит «стало лучше» — она значит «дошли дальше».** До первого зелёного прогона неизвестно, сколько там ещё слоёв.

## Что чинить

`build_draft_from_tool_choice` объявлен с `str` и `dict`, а на строках 754 и 760 ему приходит `Any | None`. То есть **вызывающий код допускает `None` там, где принимающий его не ждёт**.

Тебе решать, где правда:

* если `None` там **возможен** — обработать его до вызова, явной проверкой. Молча привести типом нельзя: `None` доедет до тела функции и упадёт в рантайме уже на пилоте;
* если `None` там **невозможен** — докажи это сужением в коде (проверка + ранний возврат), а не `# type: ignore`. Игнор запишет предположение, которое некому будет проверить.

Учитывая, что вся эта ветка — про **пронос трассы через шов, который молча терял поля**, я бы поставил на первое: `None` там возможен, и именно поэтому шов и терял.

## Остальное по #1296 зелено

```
replay fixtures (golden + adversarial + voice)   SUCCESS
miniapp (typecheck + vitest)                     SUCCESS
mergeable                                        MERGEABLE
```

Держит только mypy. Почини, запушь — мержу и ставлю флаг на пилоте.

## Про #1304 (шаг 10.5) — прогон ещё идёт, вердикта нет

Мержу по `conclusion`, как добежит.

---

# ⛔ ЗАМОРОЗКА RC-1 — 26.08 ВЕЧЕРОМ. Решение владельца

Дата назначена и **не двигается**. Она назначена именно потому, что находки не кончаются: сегодня закрыли около семи блокеров и нашли около пятнадцати. Условие «замораживаем, когда блокеры закрыты» не наступило бы никогда.

## Что это значит для тебя с этой минуты

**Новых фронтов не открывать. Задач из своего эпика не брать.**

Только то, что подпадает под определение блокера из плана владельца: неправильная, потерянная или двойная запись; отсутствие уведомления салона; **ложный успех**; утечка данных; тупик основного сценария; невозможность отменить или перенести.

**Находки за пределами блокеров — описывать, но не чинить.** Это уже работает и дало сегодня больше всего: подстрочные матчеры, таймбомбу на 22 сентября, дыру в ветке записи через Ayla, отсутствие нижней границы у сотрудника. Продолжай именно так.

**Красный прогон до починки остаётся обязательным.** Заморозка сокращает объём, а не строгость.

## После заморозки

В RC-1 не входит **ничего**, кроме двух классов: **ложный успех** и **утечка данных**. Остальное — в список известных проблем RC-1.

Если считаешь, что твоя находка относится к этим двум классам — **скажи мне, не решай сам**. Граница узкая, и я не хочу, чтобы она расширилась молча.

## Про бэкенд

```
бот     выкладка РУЧНАЯ  ->  merge в dev безопасен
бэкенд  выкладка АВТО    ->  merge в dev = выкладка на боевой пилот
```

**Merge в `dev` бэкенда — только с явного слова владельца, каждый раз.** Не ставь auto-merge ни при каких условиях.

## Что сдать до вечера 26.08

Всё незакоммиченное — **закоммитить сейчас**. Сегодня дважды чуть не пропала готовая работа: обнулённый `concierge.py` и «пустой» worktree, в котором лежало восемьдесят процентов реализации.

Открытые PR довести до вердикта CI. Незавершённое — описать в отчёте так, чтобы подхватил другой.

---

# 25.08 — ответ на вопрос про класс. Тебе разрешение не нужно, но ответ важен для границы

## Короткий ответ: начинай сейчас, и это не исключение

**Заморозка завтра вечером.** Работа, начатая сейчас, приземляется **до** неё. Класс «утечка данных» — это про то, что впускается **после** заморозки, а ты ещё внутри срока.

Так что **бери первую часть шага 4 — PII-паттерны в `evaluate_outbound` — прямо сейчас**, без разрешения на исключение.

## Длинный ответ, и он важнее, потому что ты попросило границу

**Нет, дыра в стороже не считается «утечкой данных» в смысле плана.** И это не придирка.

```
утечка               данные УШЛИ наружу
отсутствие сторожа   данные могли бы уйти, если бы что-то ещё сломалось
```

Сегодняшняя утечка была настоящей: `conversations_list` **отдавал** четыре цифры телефона живому мастеру. Её починили. Сторож, не умеющий PII, — это **отсутствующая сеть, а не падение**.

**Почему различение стоит держать.** Если «отсутствие защиты от класса X» считать классом X, то граница расширяется до всего: любой недостающий тест, любой непокрытый путь, любая проверка, которой нет, — всё становится впускаемым после заморозки. Через неделю заморозки не останется.

Я сам писал, что сторож без PII — «дыра ровно того размера». Формулировка верная по **важности** и неверная по **классу**. Беру назад ту её часть, которую ты справедливо прочло как основание.

**Практический вывод для тебя:** успеешь до завтрашнего вечера — входит в RC-1. Не успеешь — **в известные проблемы, и не проси исключения**, я его не дам. Это не про ценность работы, а про то, чтобы граница осталась узкой.

## Инвентарь — принят, и он образцовый

Пять worktree, `git status` по каждому, «незакоммиченного нет нигде». **Сегодня это спасло бы три работы**: обнулённый `concierge.py`, «пустой» worktree с восемьюдесятью процентами реализации и — только что найденный мной — **весь канон сверки одним незапушенным коммитом на локальной ветке**.

Ты единственное окно, которое проверило это без напоминания.

## Твой пункт 5 сильнее, чем ты думаешь

> tenant-less зазор всплыл уже в ТРЁХ механизмах — это усиливает «нужен» или требует третьего варианта «реестр не нужен, но sentinel-паттерн узаконить»

**Третий вариант — настоящий, и я его добавил владельцу.**

Зазор перестал быть свойством `promptreg`. Он свойство **архитектуры**: глобальный клиентский путь tenant-less, а половина инфраструктуры требует тенанта. Три обхода одним паттерном — это уже не обход, это **нерасписанное соглашение**.

Узаконить sentinel-паттерн дешевле, чем решать зазор, и честнее, чем повторять обёртку в четвёртом месте молча.

## Что ставлю на пилоте после мержей

```
INTENT_RESOLUTION_FROM_TOOL_CHOICE_ENABLED   #1296, смержен
SKILL_CONFIDENCE_FLOOR_LIVE_ENABLED          #1304, смержен
LIVE_PATH_AI_METRIC_ENABLED                  #1309, смержен
REPLAY_LIVE_CAPTURE_ENABLED                  #1314, ждёт вердикта
```

Все умолчанием ВЫКЛ — сами не включатся. **Живое доказательство третьего пути снимем вместе**, как договаривались: один ход с инструментом, `source=tool_choice` без вызова модели.

Включать буду **по одному**, не пачкой: четыре флага разом дадут четыре одновременных изменения поведения на живом контуре, и если что-то пойдёт не так, причину придётся угадывать.

---

# 26.08 — #1314 смержен. Шаг 4.1 успел. 4.2 и 4.3 в RC-1 НЕ входят

## #1314 в `dev`

Вердикт сверил сам: `ci: success`, `replay: success`. Смержен.

Флаг `REPLAY_LIVE_CAPTURE_ENABLED` встаёт в очередь включений — **по одному, не пачкой**. Четыре одновременных изменения поведения на живом контуре сделали бы причину любого сбоя предметом угадывания.

## #1320 (PII) — прогон идёт, мержу по вердикту

**Ты успела до заморозки, и это ровно то, о чём мы договаривались:** разрешение на исключение не понадобилось, потому что работа приземляется внутри срока.

Три вещи в ней отмечу.

**Калибровка против ложных срабатываний по уроку DRF-1382.** Ты не просто добавила паттерн, а проверила его на классе, где родственный редктор уже обжёгся — резал группы UUID. Урок применён к соседнему коду, а не забыт вместе с задачей.

**«код 4521» сознательно не маркер.** Названо явно, а не оставлено на догадку читателя. Граница, о которой сказано вслух, переживает рефакторинг; граница, которая просто есть, — нет.

**«Ни один существующий тест не тронут», доказано диффом без удалённых assert'ов.** Это правильная форма доказательства: не «я ничего не ломал», а «вот чем это видно».

И проверила ты **сама, после гибели сабагента, а не с его слов**. Работа сабагента, которую никто не перепроверил, — это отчёт, а не результат.

## 4.2 и 4.3 — в известные проблемы, без исключения

**Ответ: нет, в RC-1 не входят.**

Основание то же, что я давал утром: **отсутствие защиты от класса X не есть класс X**. PII был срочен не потому, что сторож неполон, а потому что сегодня нашлась живая утечка того же размера — телефон клиента, доехавший до мастера. Ключи и brand-voice такой пары не имеют.

**Исключения не проси и не жди** — я его не дам, и это не про ценность работы. Граница узкая намеренно: расширь её один раз «по здравому смыслу», и заморозки не останется.

Записывай их в известные проблемы RC-1 своим текстом, с ценой.

## Про лимит — пятый случай, и твоё правило сработало

> резюм успел ровно на одно действие, закоммитить работу, и умер на следующем запросе

**Это лучшая иллюстрация правила за весь день.** Сегодня трижды чуть не пропала готовая работа — обнулённый файл, «пустой» worktree с восемью десятыми реализации, весь канон на локальной ветке без upstream. У тебя не пропало ничего, потому что коммит стоял раньше, чем понадобился.

Руками шаги 4.2/4.3 не начинай. **До вечера они не нужны**, а начатая и брошенная работа хуже неначатой.

## Что от тебя нужно до вечера

1. Ничего не начинать сверх #1320.
2. Незавершённое описать так, чтобы подхватил другой — ты это уже делаешь лучше всех окон.
3. **Реестр ложных комментариев** — отдай финальным списком. Две записи у тебя, окно сверки насчитало тринадцать; сведём в одну задачу после RC-1.

---

# 26.08 — окно закрыто. Шесть PR в `dev`, инвентарь чистый

```
#1296  резолвер записывает выбор консьержа вместо второго вызова модели
#1297  голос из одного места вместо четырёх
#1304  confidence-floor на живом пути за флагом
#1309  метрика живого пути за флагом
#1314  replay-трассы на живом пути за флагом
#1320  PII в стороже исходящего
```

Все шесть смержены, все вердикты сверены по `conclusion`, все мержи — по `mergedAt`.

## Что ты сделала лучше всех окон

**Инвентарь по пяти worktree с `git status` по каждому — без напоминания.** Сегодня трижды готовая работа чуть не пропала в других местах: обнулённый файл, «пустой» worktree с восемью десятыми реализации, весь канон на локальной ветке без upstream. У тебя не пропало ничего.

**Проверка работы сабагента своими руками после его гибели.** Отчёт сабагента, который никто не перечитал, — это отчёт, а не результат. Ты перепроверила и сказала, чем именно.

**Разделение подтверждённого и потенциального** в известных проблемах: «живой пары утечки того же размера на 26.08 не найдено — риск потенциальный, не подтверждённый». Это то, чего мне не хватало от задач, которые сегодня трижды оказались с устаревшей посылкой.

**И два раза ты нашла то, что нельзя увидеть чтением:** шов молча терял `tool_trace`, потом `confidence`. Оба раза — только красным тестом, написанным до реализации. Без него потеря выглядела бы как «функция почему-то не работает».

## Флаги — включаю по одному, после выкладки

```
INTENT_RESOLUTION_FROM_TOOL_CHOICE_ENABLED   живое доказательство снимаем вместе
SKILL_CONFIDENCE_FLOOR_LIVE_ENABLED
LIVE_PATH_AI_METRIC_ENABLED
REPLAY_LIVE_CAPTURE_ENABLED
```

Все умолчанием выключены. **По одному, не пачкой** — четыре одновременных изменения поведения на живом контуре сделали бы причину любого сбоя предметом угадывания.

Позову, когда дойдём до третьего пути: доказательство `source=tool_choice` без вызова модели снимаем вместе, как договаривались.

Монитор оставляй включённым.
