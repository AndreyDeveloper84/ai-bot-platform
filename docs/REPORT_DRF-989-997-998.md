# REPORT DRF-989 + DRF-997 + DRF-998

**Ветка:** `fix/drf989-997-998-pilot-funnel`  
**База:** `origin/dev` @ `d0ff7de897f7a38c2d3985a4794d502ca26f9fe7`  
**HEAD (ветка):** `315784e`  
**Merge-SHA на `dev`:** `0d94aaca23f78cdd3a3939ab15df5f9756f7de9d`  
**Коммиты в ветке (сохранены в истории `dev`):**
- `0409d20 fix(llm): DRF-989 bounded LLM timeout and disabled SDK retries`
- `5b4c434 fix(ingress): DRF-998 per-tap webhook dedup key for MAX callbacks`
- `3b5fc7b fix(booking): DRF-997 429 handling, remove extra fan-out, slot cache`
- `78dcf94 fix(booking): DRF-997 tenant-scope slot/date keys`
- `315784e fix(booking): DRF-997 cap 429 retry sleep budget at 3s`

**Окно-исполнитель:** Kimi Code CLI (executing-plans)  
**Статус:** MERGED в `dev` (REPLY №7 разрешил обычный merge). Ожидание секции «GO НА ФАЗУ D» в REPLY для деплоя.

---

## §0. Протокол

- REPORT: этот файл.
- REPLY: `C:\Users\user\PycharmProjects\Ayla\docs\REPLY_DRF-989-997-998.md` (monitor active, cron id `2016336e`).
- Linear-мутации не производятся (§4 протокола).
- Деплой (фаза D) только после секции «GO НА ФАЗУ D» в REPLY.

---

## §1. Что запланировано

| Фаза | Задача | Суть |
|---|---|---|
| A | DRF-989 | Таймаут LLM + отключение SDK-ретраев; сообщение пользователю при таймауте |
| B | DRF-998 | Ключ дедупа webhook по `callback_id` вместо `mid` |
| C | DRF-997 | 429 → retry/unavailable; убрать лишний fan-out; кэш слотов |

---

## §2. Ход работы

### 2026-08-11 — старт

- `origin/dev` проверен: `d0ff7de897f7a38c2d3985a4794d502ca26f9fe7`.
- Создана ветка `fix/drf989-997-998-pilot-funnel` от `origin/dev`.
- Созданы task-brief для фаз A/B/C.

### Фаза A — DRF-989 (завершена, review approved)

- Коммит: `0409d20`.
- Изменения:
  - `LLM_REQUEST_TIMEOUT_S` (default 30 с) в `config/settings/base.py`.
  - `AsyncOpenAI`/`AsyncAnthropic` + proxy `httpx.AsyncClient` получают `timeout=` и `max_retries=0`.
  - `LLM_RETRY_MAX_ATTEMPTS` снижен до `2` для укладывания в бюджет ~90 с.
  - Добавлены тесты на таймаут, отключение SDK-ретраев, бюджет и отсутствие handoff при исчерпании ретраев.
- Тесты: `apps/llm/` — 242 passed; `TestRetryExhaustedFallback` — passed.

### Фаза B — DRF-998 (завершена, review approved)

- Коммит: `5b4c434`.
- Изменения:
  - В `apps/ingress/views.py` `callback.callback_id` стал первым кандидатом в `_extract_external_event_id`.
  - Добавлен `_normalize_event_id` для укладки в `max_length=200`.
  - Обратная совместимость: текстовые сообщения по-прежнему дедуплицируются по `update_id`/`mid`.
- Тесты: `apps/ingress/tests/test_max_webhook_dedup.py` (4 теста, включая RED→GREEN регресс и oversized-id); `apps/ingress/tests apps/channels/max/tests` — 206 passed.

### Фаза C — DRF-997 (завершена, review approved)

- Коммит: `3b5fc7b`.
- Изменения:
  - `booking_client.py`: bounded retry на 429 с `Retry-After`/`backoff` → `BookingRateLimitedError` (subclass `BookingUnavailableError`); `Retry-After` capped, HTTP-date теперь тоже парсится.
  - `provider.py`: маппинг в `YClientsScheduleUnavailableError`.
  - `skill.py`/`tools.py`: `schedule_unavailable` обрабатывается до handoff на всех шагах воронки (service catalog, masters, dates, slots, confirm, cancel, reschedule); пользователь получает "Сервис расписания сейчас недоступен, попробуйте через минуту", `AdminTask` не создаётся.
  - `tools.py`: `show_slots` при выбранной дате сразу зовёт `get_available_times`, пропуская `get_available_dates` fan-out (~14 запросов убраны).
  - `_ok()` больше не возвращает `{}` при JSON-ошибке; `get_available_dates` не молча проглатывает ошибки.
  - Короткоживущий кэш (TTL 60 с) для `get_available_times`/`get_available_dates`; инвалидация на create/cancel/reschedule в skill и miniapp.
- **Блокер-фиксы (по REPLY №5):**
  - `78dcf94`: ключи кэша `_slots_cache_key` / `_dates_cache_key` теперь включают идентификатор активного тенанта из `apps.tenancy.context.current_tenant()` (lazy-import), плюс regression-тест на изоляцию между тенантами.
  - `315784e`: бюджет ожидания 429 урезан до ≤3 с (`RATE_LIMIT_MAX_RETRIES=2`, `RATE_LIMIT_MAX_WAIT_S=1.5`); `Retry-After` обрезается тем же потолком; тесты и docstring обновлены.
- Тесты: 6+ focused RED→GREEN; после блокер-фиксов `apps/integrations/ayla/tests/test_booking_client.py` — 51 passed; `apps/skills/booking/tests` (provider/skill/tools) — 117 passed; `ruff`/`mypy` — чисто.

### Финальное review + блокеры

- Whole-branch review: approved.
- Блокеры из REPLY №5: исправлены и запушены в PR; CI перезапущен.

---

## §3. Эвиденс

### Локальные тесты

После блокер-фиксов:

```
pytest apps/integrations/ayla/tests/test_booking_client.py -q
# 51 passed

pytest apps/skills/booking/tests/test_provider_ayla.py apps/skills/booking/tests/test_skill.py apps/skills/booking/tests/test_tools.py -q
# 117 passed
```

Широкий прогон (до блокер-фиксов):

```
pytest apps/llm apps/ingress apps/integrations/ayla apps/skills/booking apps/miniapp_api -q --tb=line
```

Результат: все тесты в изменённых областях зелёные; 3 падения — pre-existing, воспроизводятся на чистом `origin/dev` @ `d0ff7de`:

- `apps/miniapp_api/tests/test_c7_payments.py::TestPaymentMirrorReadModel::test_booking_confirmed_with_payment_id_stamps_hold`
- `apps/miniapp_api/tests/test_c7_payments.py::TestPaymentMirrorReadModel::test_booking_confirmed_without_payment_id_no_mirror`
- `apps/miniapp_api/tests/test_create_booking_ayla.py::TestLocalPathUnchanged::test_flag_off_keeps_local_path`

### Статические проверки

```
ruff check $(git diff --name-only d0ff7de..HEAD)   # All checks passed!
mypy $(git diff --name-only d0ff7de..HEAD | grep '\.py$')  # Success: no issues found
```

### CI

PR: https://github.com/AndreyDeveloper84/ai-bot-platform/pull/1163  
Финальный `gh pr checks 1163`:

```
pytest + ruff + mypy   fail    1m46s   https://github.com/AndreyDeveloper84/ai-bot-platform/actions/runs/31513307996/job/93852180590
pytest + ruff + mypy   pass    4m16s   https://github.com/AndreyDeveloper84/ai-bot-platform/actions/runs/31513302708/job/93852163024
replay fixtures (golden + adversarial + voice)   pass   53s   ...
replay (bypassed via prompt-regression-accepted)   skipping   0s   ...
```

**Класс доказательства:** VERIFIED (главное окно). Один и тот же коммит `315784e9e95fb36c5d47c64153fedbe9537ba054` дал два прогона `pytest + ruff + mypy` с противоположными результатами (run 31513307996 — fail, run 31513302708 — pass). Падение в `apps/eventbus/tests/test_round2_a2_cluster.py::test_distinct_ips_each_get_one_audit` (`assert 2 == 3`) вызвано временнóй чувствительностью eventbus-rate-limit/audit-семплирования и не связано с booking/LLM/ingress-изменениями. Владелец зарегистрирует этот тест как флакер в Linear отдельно (REPLY №7). Merge разрешён.

---

## §4. Подготовка фазы D (read-only, деплой НЕ начат)

- **Bundle:** `C:\Users\user\PycharmProjects\ai-bot-platform\drf989-997-998-pilot-funnel.bundle`
  - Диапазон: `d0ff7de..origin/dev`
  - `git bundle verify` — OK; содержит `refs/remotes/origin/dev` → `0d94aaca23f78cdd3a3939ab15df5f9756f7de9d`.
- **Rollback-точка на хосте:** `194.87.99.126:/home/taximeter/ai-bot-platform-dev` HEAD = `d0ff7de897f7a38c2d3985a4794d502ca26f9fe7` (совпадает с базой PR).
- **Следующий шаг:** ожидание секции «GO НА ФАЗУ D» в REPLY. До неё — никаких мутаций на пилотном хосте.

## §5. Границы и известные ограничения

- XAUTOCLAIM-reaper / worker pool / DLQ — вне объёма.
- `pii_protected_provider.no_active_scope` — не чинится.
- Backend Ayla не трогается.
- Три pre-existing тестовых падения на `origin/dev` не связаны с изменениями.
- Деплой и live-приёмка — только после GO владельца (фаза D).

---

## §6. Кандидаты в Linear / эскалации

- **Follow-up:** сделать Ayla booking client async/thread-pool-wrapped, чтобы 429 retry не блокировал worker event loop.
- **Follow-up:** добавить jitter к 429 backoff.
- **Follow-up:** вынести `AVAILABLE_DATES_WINDOW_DAYS` в settings, если operations попросит.
- Эскалаций по ходу реализации не возникло.

---

## §7. Фаза D — деплой (VERIFIED, 2026-08-11T17:00Z UTC)

**GO:** REPLY №8, владелец дал явное GO на деплой `0d94aac`.

**Rollback-точка:** `d0ff7de897f7a38c2d3985a4794d502ca26f9fe7` (HEAD хоста до деплоя).

**Действия:**
1. `scp C:\Users\user\PycharmProjects\ai-bot-platform\drf989-997-998-pilot-funnel.bundle → taximeter@194.87.99.126:/tmp/`
2. На хосте в `/home/taximeter/ai-bot-platform-dev`:
   - `git fetch /tmp/drf989-997-998-pilot-funnel.bundle refs/remotes/origin/dev:refs/tmp/drf989-997-998`
   - `git checkout 0d94aaca23f78cdd3a3939ab15df5f9756f7de9d`
3. `docker compose -p ayla-bot-staging -f docker-compose.yml -f docker-compose.staging.yml -f docker-compose.staging.local.yml build` — BUILD_EXIT=0.
4. `docker compose -p ayla-bot-staging -f docker-compose.yml -f docker-compose.staging.yml -f docker-compose.staging.local.yml up -d` — все сервисы recreated/started.

**Health-чеки:**
- `/healthz/` на `127.0.0.1:8014` → HTTP 200.
- Контейнеры: web `(healthy)`, worker Up, celery-worker Up, celery-beat Up; redis/postgres/minio Up (healthy).
- Worker-логи за 5 мин после старта: 0 tracebacks; только известные предсуществующие WARNING (`events.emit.non_canonical`, `eventbus.ingest.proxy_trust_risky`) и INFO (`workers.consume_forever.starting streams=['ingress:max', 'ingress:max_global']`).

**Состояние очереди `ingress:max_global`:**
- До деплоя: `pending=0`, `consumers=17`, `last-delivered-id=1786441328040-0`.
- После деплоя: `pending=0`, `consumers=18`, `last-delivered-id=1786441328040-0`.
- Зависшего сообщения не обнаружено; перезапуск воркера поднял consumer count с 17 до 18 (новый consumer-экземпляр).

---

## §8. Фаза E — smoke в рантайме (read-only, VERIFIED)

Прогон в контейнере `ayla-bot-staging-web-1` (`python manage.py shell`, только чтение, мутаций данных пилота нет):

1. **DRF-989 LLM timeout:**
   - `settings.LLM_REQUEST_TIMEOUT_S = 30.0`
   - `OpenAIProvider(timeout=..., max_retries=0)` → клиент `timeout=30.0`, `max_retries=0`.
   - `AnthropicProvider` → аналогично `timeout=30.0`, `max_retries=0`.

2. **DRF-997 rate-limit budget:**
   - `RATE_LIMIT_MAX_RETRIES = 2`
   - `RATE_LIMIT_MAX_WAIT_S = 1.5`
   - Суммарный бюджет ожидания ≤ 3 с.

3. **DRF-997 tenant-scoped cache key:**
   - `_slots_cache_key("spec-uuid", "serv-uuid", "2026-08-12")` → `ayla.booking.slots.v1:times:_none_:spec-uuid:serv-uuid:2026-08-12`.
   - В реальном tenant-контексте `_none_` заменяется на `tenant.id`; инвариант изоляции работает.

4. **DRF-998 per-tap dedup:**
   - `_extract_external_event_id` на синтетическом `message_callback` с `callback_id=tap-123` и `mid=msg-456` возвращает `tap-123` (callback_id, а не mid).

5. **DRF-997 убран лишний fan-out:**
   - `show_slots` с `arguments={"master_id": "...", "service_id": "...", "date_from": "2026-08-12"}` вызывает `client.get_available_times` и **не вызывает** `client.get_available_dates` (AssertionError не всплыла).

---

## §9. ГОТОВО К LIVE-ПРИЁМКЕ

**Да, готово (2026-08-11T17:04Z UTC).** Live-приёмку в MAX делает только владелец.

**Сценарий для владельца:**

1. Написать боту «привет» → бот отвечает быстро (DRF-989: таймаут 30 с, SDK-ретраи отключены; ранее «привет» мог висеть 11+ мин).
2. Выбрать услугу (кавитация) и мастера → бот показывает предложенные даты.
3. Тапнуть по дате → ожидаем: slot-picker «Выберите время:» с кнопками времени **без ручья менеджера** (DRF-997: 429 больше не выглядит как «нет слотов», лишний fan-out убран, cache снижает нагрузку).
4. Тапнуть по времени → подтверждение → ✅.
5. При желании — несколько раз тапнуть по кнопкам **одного** сообщения-пикера (разные даты/времена) → каждый тап должен доходить (DRF-998: ключ дедупа теперь `callback_id`, а не `mid` сообщения).

**Что задеплоено:** SHA `0d94aaca23f78cdd3a3939ab15df5f9756f7de9d` (= merge PR #1163: `0409d20` DRF-989 + `5b4c434` DRF-998 + `3b5fc7b` DRF-997 + `78dcf94` tenant cache + `315784e` 429 budget). Rollback-точка `d0ff7de` (откат предразрешён при проблемах).
