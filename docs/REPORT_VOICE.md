# ОКНО VOICE — отчёт главному окну

Окно: «как Ayla разговаривает» (DRF-1265 + DRF-1209), бриф `docs/BRIEF_WINDOW_VOICE.md`.
Отвечаю сюда; ваши ответы — в `docs/REPLY_VOICE.md` (монитор стоит).

---

## 2026-08-25 ~04:10 UTC — Первое действие выполнено: DRF-1310 доказан живьём

**Критерий брифа закрыт собственным ходом, не чужим логом.**

Ход: свободный текст «Расскажи, что вообще бывает из расслабляющего, я просто устал и хочу выдохнуть»
в клиентский (глобальный) бот пилота, trace `5536c26e-8277-4f0b-86eb-4b94a96c266d`.

Лог worker (`ayla-bot-staging-worker-1`, 2026-08-25T04:08:21Z):

```
orchestrator.intent_resolution.ok trace=5536c26e-… contract_version="0.5"
intent_type=DISCOVER_SERVICE, confidence=0.9, status=resolved
```

**Попутная находка, важная для методики:** резолвер стреляет только на глобальном
клиентском пути (`ingress:max_global`, tenant=None, `concierge_turn_ran` в
`GlobalMaxHandler`). Два первых моих хода ушли в `ingress:max` с
`resolved_tenant_id` (tenant-путь) — ответы бот дал, а `intent_resolution.ok`
не было: на tenant-пути этот крюк не стоит. Кто будет проверять резолвер на
tenant-ботах — там его нет вовсе, это не «сломалось», это «не подключено».

Также замерено: ход со словами про боль («спина разболелась») уходит в
детерминированный уточняющий сценарий без консьержа (~1 с, без резолвера) —
ожидаемо, но стоит иметь в виду при чтении логов.

## Дальше по брифу

1. Третий путь (резолвер записывает выбор консьержа вместо второго вызова) — в работе, разведка кода.
2. DRF-1265 — сведение голоса, реестр промптов.
3. DRF-1209 — шесть шагов, каждый со своим флагом.

Вопросов владельцу пока нет.

---

## 2026-08-25 07:01 UTC — Третий путь: код готов, ответ на вопрос про «бесплатно», одно опровержение

### Ответ на твой вопрос: сохраняется ли выбор консьержа уже сейчас

**Нет, не сохраняется — «читать оттуда» не выйдет.** Проверено по коду (`origin/dev`):

1. **Имя инструмента** — пишется, но обрезанно: `Message.action_type` глобальной беседы хранит **последний непустой** action_type за ход (`concierge.py:1305-1310` + `GlobalConversationStore`, комментарий строка 456). Без аргументов, без порядка — при мультипассе всё, кроме последнего выбора, теряется.
2. **Аргументы вызова — нигде.** `record_global_message` на глобальном пути сознательно дропает `action_data`/`tool_call` (комментарий `concierge.py:440-444`: «adapter accepts and drops them»). `AIRequestMetric` (DRF-1211) — токены/латентность/исход на вызов, имени инструмента и аргументов там нет.
3. **Опровержение комментария в коде:** `concierge.py:503` утверждает «the tool selection is in the `orchestrator.concierge.*` log line». Общего лога выбора инструмента в модуле **нет** — есть только веточные (`start_booking.no_master`, `show_masters.no_criteria`, forced_retry), то есть фиксируются отказы, а не выбор. Комментарий врёт.

**Вывод по твоей оценке «бесплатно».** В смысле вызовов модели — подтверждаю: детерминированный путь не создаёт LLM-клиента вообще (в тесте клиент-мок не инстанцируется, `usage=None`). В смысле «уже записано — просто прочти» — **опровергнута**: трассы с аргументами не существует ни в БД, ни в логе, её надо было пронести. Это и сделано (ниже).

### Что сделано (DRF-1385, ветка `feat/drf1385-resolver-tool-choice`, worktree `.claude/worktrees/drf1385-resolver-tool-choice`)

Твоя находка про незакоммиченное подтвердилась худшим образом: предыдущая моя сессия успела закоммитить резолвер и тесты, но правки проноса трассы (concierge/шов/handler) остались в рабочем дереве — и `concierge.py` был **обнулён до 0 байт**. Файл восстановлен из git, утерянная часть реализована заново по тестам-спецификации.

- `a1e812e` — тесты (было), `52074db` — резолвер (было). Оба проверены чтением: отображение инструмент→контракт фиксированное, флаг `INTENT_RESOLUTION_FROM_TOOL_CHOICE_ENABLED` умолчанием ВЫКЛ, любой пробел → откат в LLM-проход, резолвер остаётся строго послерепликовым.
- **`3c635c2` (новый)** — пронос трассы: поле `tool_trace` у `DiscoveryReply` (discovery.py) и `TurnReply` (turn_seam.py, копирование через `getattr` — legacy-производители без поля не ломаются); `concierge.py` собирает трассу по проходам и прикрепляет ко всем 20 возвратам (текстовый ход → `None`; пересборка `guard_outbound` трассу не теряет); `handler.py:1412` передаёт `tool_trace` в `resolve_and_log_turn_intent`. Ветка ahead 3 / behind 2, **не пушила, не мержу** — PR против `dev` ждёт тебя.

**Чем доказано:** тесты-спецификации сначала зафиксировали потерю (4 падения: `AttributeError: 'TurnReply' object has no attribute 'tool_trace'` и пустая трасса у консьержа), после реализации — зелёные. Прогон на venv `drf1355` (read-only, ничего не устанавливала): `test_intent_resolution_tool_choice.py` 34/34, `test_intent_resolution.py` 40/40, `test_concierge_multipass.py` 17/17, `test_concierge_promise_retry.py` 36/36, `test_global_booking_continuation.py` 29/29.

### Что решила сама

- **`concierge.py` восстановлен из git без спроса** — 0 байт это не правка, а потеря; альтернатива (восстанавливать 1900 строк по памяти) хуже по любой цене.
- **Отклонение от твоей схемы в handler:** `reply` в точке вызова резолвера — пересобранный `DiscoveryReply` (handler.py:1285), трасса взята из `turn_reply`, а не из `reply`. Иначе было бы всегда `None`. Задокументировано в коде.
- **Не ребейзила behind-2** — мутации истории не мои, разница в 2 коммита origin/dev оставлена на PR.

### Что осталось

1. **Живое доказательство** — критерий брифа: нужен мерж в `dev` (ты), включение `INTENT_RESOLUTION_FROM_TOOL_CHOICE_ENABLED=1` в окружении пилота, один свободный ход с инструментом → в логе worker `orchestrator.intent_resolution.ok ... source=tool_choice` без строки вызова модели. Сама включить не могу — флаг в окружении пилота, не в коде.
2. Дальше по брифу: DRF-1265 (сведение голоса), потом шесть шагов DRF-1209.

Вопросов владельцу нет.

---

## 2026-08-25 07:20 UTC — DRF-1265, разведка: замер 22.08 устарел, имена уже сведены

**Опровержение брифа, пишу сразу — объём работы другой, чем в замере.**

Коммит `99824a6` от 22.08 (DRF-1226, «feat(persona): one source for who the assistant says it is») **уже свёл имена** в `apps/persona/voice.py`: таблица `_SURFACE_NAMES` (marketplace → имя из библиотеки, salon → «Помощник»), единый вход `assistant_identity(surface)`. Хардкода «Помощник» в `ai_drafts.py:368` больше нет — там `assistant_identity(SURFACE_SALON)` (строка 367). Замер делался раньше этого коммита или по старому дереву.

**Фактическое состояние (разведано по коду, не по Linear):**

- `AYLA_MARKETPLACE_VOICE` живёт в ayla-ai-core (`prompts.py:324`, frozen `BrandVoiceConfig`); единственный продакшн-импорт — `apps/persona/voice.py:91` с fallback-зеркалом. Concierge и handler читают голос уже через persona.
- **Живых источников голова осталось четыре:** ① константа в ai-core + зеркало `persona/voice.py`; ② `_DEFAULT_BRAND_VOICE` «Алина, администратор салона» — `apps/skills/faq/skill.py:75-84`; ③ **дословный дубликат** — `apps/skills/booking/skill.py:162-171`; ④ `_WELCOME_TEXT` — `apps/channels/max/handler.py:198-203`.
- **Дыра дрейфта:** `discovery.py:393` читает `voice['assistant_name']` из сырого dict, минуя `assistant_identity()` — concierge сам предупреждает об этом в комментарии (concierge.py:1032-1037).
- **Неподключённых механизмов — ТРИ, не один:** реестр `apps/promptreg/` (Sprint 4, DRF-476..481: `get_prompt(tenant, skill)`, publish/unpublish с аудитом, Redis-инвалидация — продакшн-потребителей ноль, сид-данных нет), `apps/voice/` (DRF-490, «Phase-0 passthrough stub»), `apps/persona/models.BrandVoiceConfig` (per-tenant skeleton, промптами не читается). Тот же класс «механизм задокументирован и не работает», но экземплярами больше, чем бриф считал.
- **Архитектурный зазор реестра:** `get_prompt()` требует `Tenant`, а глобальный клиентский бот tenant-less — подключение реестра к главному пути без решения этого вопроса невозможно.
- **Конфликт границ:** дубликат «Алины» №3 лежит в `apps/skills/booking/**` — твой бриф запрещает его трогать (окно записи). Свести голос «в одно место» полностью, не заходя туда, нельзя.

**Чем доказано:** grep+чтение по worktree drf1385 (код идентичен origin/dev в этой части), список точек со строками выше.

**Чего жду:** решение по объёму — (а) сведение остатков голоса без реестра (малая работа: discovery-дыра, welcome, faq-скилл; booking — твоё разрешение или обход); (б) подключение promptreg к живому пути (большая: tenant-less зазор, сиды, ~8 точек потребления); (в) оба, в каком порядке. Пока не начинаю, чтобы не потратить час зря.

Третий путь (07:01 UTC) неизменен: код готов, ждёт мержа и флага на пилоте.

---

## 2026-08-25 08:33 UTC — Оба PR открыты: DRF-1385 (#1296) и DRF-1265 вариант (а) (#1297)

### DRF-1385 — PR #1296 (против `dev`)

Ветка `feat/drf1385-resolver-tool-choice` запушена (3 коммита: тесты → резолвер → пронос трассы), PR открыт: https://github.com/AndreyDeveloper84/ai-bot-platform/pull/1296. В теле — состав, границы безопасности (строго послерепликовый, откат в LLM-проход, флаг ВЫКЛ по умолчанию), прогоны. Живое доказательство после мержа: `INTENT_RESOLUTION_FROM_TOOL_CHOICE_ENABLED=1` на пилоте + один ход с инструментом → `source=tool_choice` без вызова модели.

### DRF-1265 вариант (а) — PR #1297 (против `dev`)

Все четыре точки закрыты коммитом `59619ea`, worktree `drf1265-voice-unification`:

- `discovery.py` — имя через `assistant_identity(SURFACE_MARKETPLACE).name`, строка промпта байт-в-байт прежняя (запинена).
- `persona/voice.py` — канонические `SALON_ADMIN_NAME`/`SALON_BUSINESS_NAME`/`DEFAULT_SALON_PERSONA`.
- `faq/skill.py` + `booking/skill.py` — оба читают `DEFAULT_SALON_PERSONA` из одного места; в `booking/**` тронуты ровно разрешённые строки (импорт + persona в блоке 162–171), комментарий 160–161 оставлен — он не лжёт: tone/forbidden там по-прежнему inline.
- `_WELCOME_TEXT` — частичная привязка: название салона из `SALON_BUSINESS_NAME`. **Отклонение:** полная сборка невозможна без смены видимого текста («Пенза» и описательная часть — не identity-поля ни в `assistant_identity`, ни в зеркале ai-core). Текст для пользователя не изменился, задокументировано в коде.

**Чем доказано:** 6 новых тестов, два красных до правки (discovery игнорировал `_SURFACE_NAMES` при переименовании; скиллы держали свои литералы — `is`-проверка единого источника). Финал: **1756 passed, 6 skipped**, ruff чисто, `merge-tree` против origin/dev — конфликтов нет. Прогон на venv drf1355 read-only.

**Заметка по обстановке:** origin/dev ушёл на +10 за время работы — среди них `93dfc6e` (DRF-1384, сверка окружения с пином) и `a8af4b1` (DRF-1355). PR #1296 в списке смерженных пока нет.

### Что решено самостоятельно (подробно в отчёте сабагента)

- Импорт в `booking/skill.py` — вынужденное дополнение к разрешённым строкам: без него `DEFAULT_SALON_PERSONA` не достать.
- `is`-проверка персоны опирается на f-string (свежий объект в рантайме), неуязвима к интернированию литералов.

### Дальше по брифу

Порядок «третий путь → голос → шесть шагов»: первые два в PR. Остались **шесть шагов DRF-1209** — перенос на живой путь по одному, каждый со своим флагом. Начинаю разведку первого шага, если не остановишь. Живые доказательства обоих PR — после твоих мержей.

Вопросов владельцу нет.

---

## 2026-08-25 08:46 UTC — DRF-1209, разведка шести шагов: аудит устарел ещё в двух местах

**Прежде чем брать первый шаг — сверила «что уже сделано» с кодом, а не с аудитом. Правильно сделала: основание снова врёт.**

### Статус шагов на живом пути (по коду origin/dev, доказательства — файл:строка)

| Шаг | По аудиту | По коду сегодня |
|---|---|---|
| 12 post_check | «1-2 дня, модуль зовёт только конвейер» | **Частично:** `post_check` по-прежнему не зовётся, но DRF-1210 дал функциональный эквивалент — `guard_outbound`→`evaluate_outbound` на обоих путях (`handler.py:1329,1754,500`). Набор паттернов УЖЕ, чем у post_check (нет PII/keys/brand-voice) |
| 13 compose | «частично» | **Частично:** `compose()` только в pipeline; живой путь — `reply_text` + `_build_attachments` + `guard_outbound` |
| 9 AdminTask по safety-вердикту | «уже сделан в S1-C» | **НЕТ. Аудит спорит сам с собой** (строка 74 vs его же таблица, строка 213). По коду верна таблица: `handler.py:1620-1622` прямо — «deliberately NOT done here». То, что дал S1-C — should_handoff→AdminTask, это handoff-половина шага 10.5, не шаг 9 |
| 5 memory-snapshot | 2-4 дня | **Нет:** `load_snapshot` зовут только pipeline и shadow_turn; concierge читает память своими путями (memory_block + Message-история) |
| 6 LLM-роутер | риск, не в ценнике | **Нет** (и исключён AYLA-DEC-0045: ключевые слова + порядок реестра, `skills/apps.py:86-91` — несущая конструкция) |
| 18 replay-recorder | 2-3 дня | **Нет:** рекордер зовут только pipeline и оффлайн-раннер |

Из вне-табличного ценника Ветки B: **AIRequestMetric — частично** (DRF-1211/1283 пишут консьержные проходы; per-tenant скиллы и детерминированные ветки глобального пути не пишут вовсе); **шаг 10.5 confidence-floor — нет** (1 день); **OTel — нет** (провайдер настроен, спанов на живом пути нет).

**Попутно — тринадцатый ложный комментарий:** `concierge.py:181` утверждает «replay recorder writes ReplayTrace rows only on the live path». Кодом не подтверждается — на живом пути рекордер не зовётся вообще.

### Что это меняет

Из шести шагов полностью закрыт **ни одного**; два частично (12, 13), один исключён решением владельца (6), один рассыпался на противоречие аудита (9). Программа «шесть шагов» по факту — это: довести 12 до паритета (или признать guard_outbound ответом — **решение не моё**), 18, 5, 9-строгий, 13-доводка; плюс из ценника 10.5 и метрика.

**Предлагаемый порядок (дешёвое и обособленное — первым), каждый со своим флагом:**
1. Шаг 10.5 confidence-floor (1 день, изолирован).
2. AIRequestMetric: дописать метрику на не-консьержных ходах (1-2 дня).
3. Шаг 18 replay-recorder на живом пути (2-3 дня).
4. Шаг 12: решение — расширить `evaluate_outbound` до паттернов post_check или портировать post_check как есть.
5. Шаг 5 memory-snapshot (2-4 дня, самый дорогой).
6. Шаги 9-строгий и 13 — после решения по п.4.

**Жду подтверждение порядка или свой.** Пока жду — ничего не начинаю: оба PR (#1296, #1297) висят на твоей проверке, и дешёвое «подтверди порядок» не должно превращаться в переделку.

Вопросов владельцу нет.

---

## 2026-08-25 08:58 UTC — #1296: формат запушен. Вопрос владельцу про реестр (одним пунктом)

### #1296 — формат

Причина падения понята: я гоняла `ruff check`, а падал `ruff format --check` — две разные проверки, первой это падение не видно никогда. Сделано ровно по твоей инструкции: `ruff format` по двум файлам, контроль `ruff format --check apps/` (1266 already formatted), контрольный прогон 34/34, коммит `556211d «style: ruff format»`, запушено. **Без ребейза** — по твоей же поправке: ветка MERGEABLE, лишний пуш отменил бы идущий прогон. Голова #1296 теперь `556211d`.

### Вопрос владельцу (одним пунктом, по твоей заявке)

**Нужен ли реестр промптов вообще?**

- **НУЖЕН** — цена: решить tenant-less зазор (`get_prompt()` требует Tenant, глобальный бот его не имеет — sentinel-тенант или tenant-optional API, архитектурное решение, ~2-5 чел.-дней); сиды `PromptVersion` и перевод ~8 точек потребления (concierge, discovery, ai_drafts, assistant, faq/booking build_*_prompt, composer canned, resolver-промпт) — по ценнику Ветки B порядка 1-2 дня на точку; плюс определить судьбу двух дублей (`apps/voice/` stub, `persona.models.BrandVoiceConfig` skeleton). **Итого ~8-15 чел.-дней.** Выигрыш: промпты меняются без деплоя, per-tenant пороги.
- **НЕ НУЖЕН** — цена: удалить `apps/promptreg/` (с миграциями — аккуратно, таблицы уже в базе), `apps/voice/`, `persona.models.BrandVoiceConfig`; ~1-2 чел.-дня с тестами. Теряется: live-reload промптов без деплоя и per-tenant пороги (обе возможности сегодня используются нулём потребителей). Выигрыш: минус три построенных-неподключённых механизма — соблазн для следующего окна потратить неделю на «забыли подключить».

Сама не решаю и не начинаю ни то, ни другое — как сказано.

### Принято в работу

- Правило «коммить промежуточное перед любой рискованной операцией» — принято как постоянное.
- DRF-1209: разведка первого шага — чтением; ветку от `origin/dev` заведу **после мержа #1297**. Занятые области (`apps/miniapp/**`, `apps/miniapp_api/**`, `apps/master_api/**`, `apps/skills/health_screening/**`) не трогаю без разговора.
- Ложные комментарии `concierge.py:503` (выбор инструмента «в логе» — нет) и `concierge.py:181` (replay recorder «on the live path» — нет) — заведу отдельными строками после мержа #1296, чтобы не плодить правки в висящей ветке.
- Про лимит провайдера 403: один мой запуск сабагента уже упал на OAuth/сети (перезапуск прошёл). Если упрётся повторно — скажу сразу.

### Жду

Мерж #1296 (ничто, кроме формата, не держало) → флаг на пилоте ставишь ты → живое доказательство `source=tool_choice` снимаем вместе. Вердикт CI по #1297 — мерж по conclusion. Подтверждение порядка шагов DRF-1209 (предложение — в записи 08:46).

---

## 2026-08-25 09:37 UTC — Шаг 10.5 в PR #1304. #1297 в dev. Уточнение по голове #1296

### #1296 — голова на месте, расхождение было во времени

Ты писал «голова не сдвинулась» — проверила `gh pr view 1296`: headRefOid = `556211d` (мой формат-коммит), MERGEABLE, OPEN. Пуш ушёл в 11:58 локального, твоя запись — 11:59: разошлись на минуту. Прогон на новой голове идёт, мерж по зелёному формату — с твоей стороны.

### #1297 — смержен (96eae9d, 09:34 UTC). Голос сведён в dev.

### Шаг 10.5 (confidence-floor) — PR #1304 против dev

https://github.com/AndreyDeveloper84/ai-bot-platform/pull/1304 — два коммита (тесты красные → реализация), worktree `drf1209-confidence-floor` от a6e5287.

- Флаг `SKILL_CONFIDENCE_FLOOR_LIVE_ENABLED`, умолчание ВЫКЛ (2 characterization-теста на нетронутое поведение). Пороги — те же, что у конвейера (`AI_CONFIDENCE_HANDOFF_THRESHOLD` + per-skill dict, `None` отключает).
- Механика AdminTask **не продублирована**: floor питает существующий `_dispatch_skill_handoff` (S1-C) через keyword-only overrides; `should_handoff`+floor → один AdminTask, причина конкатенируется по формату конвейера (`"a | b"`, как `pipeline.py:814-819`).
- **Находка, без которой шаг был бы невозможен:** шов `turn_seam.py` обрезал `confidence` — поле добавлено в `TurnReply` (опциональное, обратно-совместимое). Красный roundtrip-тест это зафиксировал до реализации (`None != 0.42`).
- **Мутационная проверка:** инверсия гейта флага → 5 failed (убит); `>= → ≤` → 5 failed (убит).
- Прогоны: 24 passed (новые + шов), 359 passed соседние channels; `ruff format --check apps/` чисто (урок #1296 учтён: проверяю форматтер, не только линтер).
- **Пре-существующее, не моё:** `test_shadow_turn.py` — 12 падений уже на базовом a6e5287 (проверено подстановкой base-версии файла). Возможно, это один из твоих «настоящих красных» после поднятия потолка — посмотри, чьё.
- **Вне скоупа, к сведению:** Telegram-handler диспетчит скиллы через тот же шов (`telegram/handler.py:264`) — floor туда не переносился; helper изолирован, перенос тривиален, если решишь.

### Реестр ложных комментариев (веду по твоей заявке)

```
concierge.py:503   утверждает: выбор инструмента — в логе orchestrator.concierge.*
                   опровергается: общего лога выбора нет, только веточные отказы
concierge.py:181   утверждает: replay recorder пишет ReplayTrace на живом пути
                   опровергается: на живом пути recorder не зовётся вообще
```

### Дальше

Шаг 2 (AIRequestMetric на не-консьержных ходах, 1-2 дня) — начинаю по подтверждённому порядку, ветку от свежего origin/dev. Останови, если планы изменились.

---

## 2026-08-25 11:35 UTC — #1296: mypy запушен. Ответ про БД. Шаг 2 в PR #1309

### #1296 — mypy починен (`0f1457f`)

Твой разбор верный: починка формата открыла mypy — слой был под ним всё время. Починила сужением, как ты и ставил: `None` там **возможен** (трасса приходит извне через шов, который уже дважды молча терял поля), поэтому явная проверка до вызова — новый `_trace_entry_tool(entry) -> tuple[str, dict] | None`, malformed primary → `None`, malformed secondary → `continue`. Поведение рантайма не изменилось (guard в `build_draft_from_tool_choice` делал то же самое), но теперь типы доказаны в коде, а не предполагаются. `# type: ignore` не использован. Проверено: mypy по обоим файлам чист, `ruff format --check` чист, 34/34. Голова #1296 — `0f1457f`, держало только это.

### Ответ про `test_shadow_turn`: SQLite

Гоняла на **SQLite** — `config/settings/base.py:1788-1806`: без `POSTGRES_HOST`/`DB_HOST` тесты встают на `db.sqlite3`, а я переменные не задавала. Вопрос закрыт, гоняться не за чем. Честности ради: все мои зелёные прогоны за сегодня — SQLite; CI-Postgres отличия (если всплывут) — не мой метод, а среда.

### Шаг 2 (AIRequestMetric на живом пути) — PR #1309

https://github.com/AndreyDeveloper84/ai-bot-platform/pull/1309 — коммит `6aa9131`, ветка от 9643808, конфликтов с dev нет (merge-tree чист).

- Флаг `LIVE_PATH_AI_METRIC_ENABLED`, умолчание ВЫКЛ (3 characterization-теста). Per-tenant: скилл-ход → `success` (skill из `meta["skill"]`), handoff-ветка → `escalated` (вставка ПОСЛЕ floor-ветки #1304, его строки не тронуты), safety → `safety_pre_check`. Global (sentinel): safety/opt_out/onboarding/visits → `success`, handoff → `escalated`, stale_tap → `fallback+fallback_triggered`. Все с `llm_pass_index=None`, NULL-токены — контракт DRF-1283.
- Консьерж не задваивается — запинено тестом; запись best-effort (try/except + WARN), ход не ломается никогда.
- Красный прогон 10 failed → зелёный 13 + 365 соседних; ruff format --check, ruff check, mypy — все чисты (оба урока #1296 учтены).
- **Сознательно не покрыто:** мьюты (строка исказила бы знаменатели), остальные global-ветки (memory-команды, nutrition, catalog, cb:book:*) — отдельным шагом, если решишь.
- **Пре-существующее окружение:** `test_pipeline_ai_metric_emission.py` — 2 падения на отсутствующем локальном Redis в deprecated `pipeline.turn`. К дифу отношения нет; к `test_shadow_turn` из той же серии «среды нет» — записываю в тот же класс.

### Принято в работу

- **Шов теряет поля беззвучно** (два случая: `tool_trace`, `confidence`) — идея «новое поле DiscoveryReply должно НЕ доезжать до TurnReply шумно» записана; заводишь ты, поддержу формулировкой, когда дойдёт.
- **Telegram floor** — записано как известное расхождение, не переношу: тривиальность переноса не основание для переноса.
- #1304 вижу смерженным (6188125, 10:41 UTC).

### Дальше

Шаг 3 (replay-recorder на живом пути, 2-3 дня) — после мержа #1309, чтобы не ветвиться в том же handler.py поверх своего же PR. Если хочешь параллельно — скажи, разведу по разным файлам.

---

## 2026-08-25 17:04 UTC — #1296 и #1309 вижу смерженными. Шаг 3 (replay) в PR #1314

### Мержи

#1296 (DRF-1385, третий путь) — 16:27:44 UTC. #1309 (шаг 2, метрика) — 16:27:54 UTC. Живое доказательство третьего пути ждёт флаг `INTENT_RESOLUTION_FROM_TOOL_CHOICE_ENABLED=1` на пилоте — он за тобой, снимем вместе, как ставил.

### Шаг 18 (replay-recorder на живом пути) — PR #1314

https://github.com/AndreyDeveloper84/ai-bot-platform/pull/1314 — коммит `39d1d44`, ветка от 14e49f7 (верхушка с #1309), конфликтов нет.

- Флаг `REPLAY_LIVE_CAPTURE_ENABLED`, умолчание ВЫКЛ. **Тот же рекордер и тот же сэмплинг** (`REPLAY_SAMPLE_RATE_*` внутри `capture`) — второго механизма нет. Вызовы после отправки ответа (нулевая задержка), ошибки глотаются дважды.
- **PII:** сырой текст не пишется — рекордер редактирует до persist тем же кодом, что конвейерная точка (recorder.py:106-116); тест пинает телефон → `[PHONE]`. Пре-существующая оговорка редктора (OTP_RE режет 4-значные группы в UUID, redactor.py:131-152) наследуется как есть — не трогали.
- **Tenant-less зазор и здесь:** рекордер требует tenant, глобальный путь его не имеет — обёртка `tenant_scope(get_global_bot_tenant())` только вокруг вызова, тот же паттерн, что у sentinel-метрик. Это уже третий механизм, упирающийся в tenant-less (после promptreg и метрик) — к вопросу владельца, когда он дойдёт.
- Маркер `source:"live_path"` + `surface` в существующем JSON-поле, миграций нет. Формат шагов по образцу конвейера, но из реальных стадий живого пути.
- `_dispatch_skill_handoff` стал возвращать фактически отправленный post-guard текст (`-> str`) — `composer.final_text` теперь то, что человек реально прочитал. Поведение не изменилось (вызывающие возврат игнорировали).
- Красный прогон 5 FAILED → зелёный 11/11 + вся suite channels/replay; мутация «снять гейт флага» убита; ruff format/check + mypy чисты.
- **Сознательно не покрыто:** молчаливые ходы (`global_handoff_muted`, `should_send=False` — решения бота нет), другие каналы (telegram, salon/staff).

### Дальше по подтверждённому порядку

Шаг 4 — расширение `evaluate_outbound` (твоё решение: post_check не портировать): **PII первым и отдельно**, потом ключи и brand-voice; после — `post_check` удалить или пометить «не подключать». Начинаю разведку паттернов post_check, ветку заведу после мержа #1314 (снова handler.py и safety — не хочу самоконфликта).

---

## 2026-08-25 17:06 UTC — Заморозка RC-1 принята. Инвентарь чистый. Один вопрос на границу классов

### Принято

Новых фронтов не открываю, задач из эпика не беру. Шаг 4 (расширение `evaluate_outbound`) **не начинаю** до твоего ответа на вопрос ниже. Merge бэкенда в `dev` — только с явного слова владельца, auto-merge не ставлю никогда (у меня его и не было: мержишь ты). Красный прогон до починки — остаётся.

### Инвентарь (проверено `git status` по всем пяти worktree)

```
drf1385-resolver-tool-choice    чисто, ветка в dev (#1296)
drf1265-voice-unification       чисто, ветка в dev (#1297)
drf1209-confidence-floor        чисто, ветка в dev (#1304)
drf1209-ai-request-metric       чисто, ветка в dev (#1309)
drf1209-replay-live             чисто, всё запушено, PR #1314 OPEN
```

Незакоммиченного нет нигде. #1314: miniapp и replay-фикстуры зелёные, `pytest + ruff + mypy` в прогоне — вердикт жду, о провале скажу сразу.

### Вопрос на границу классов (не решаю сам, как велено)

**Дыра PII в стороже исходящего — это «утечка данных» в смысле плана владельца?** Основание для вопроса — твоя же запись: «сегодня в этом же контуре найдена живая утечка: conversations_list отдавал четыре цифры телефона клиента… сторож исходящего, который не умеет PII, — дыра ровно того размера». Если да — прошу разрешить первую часть шага 4 (PII-паттерны в `evaluate_outbound`, отдельно от ключей/brand-voice) как блокер-класс «утечка данных»: красный тест до починки, флаг не нужен (сторож уже жив, паттерны его сужают, не расширяют — но добавлю, если скажешь). Если нет — записываю в известные проблемы RC-1 и не трогаю.

### Незавершённое — для подхвата другим

1. **DRF-1209, остаток порядка** (подтверждён 25.08): шаг 4 — расширить `evaluate_outbound` (PII → ключи → brand-voice; `post_check` не портировать, после расширения удалить или пометить «не подключать»); шаг 5 — memory-snapshot в concierge (2-4 дня); шаги 9-строгий (AdminTask по safety-вердикту, `handler.py:1620-1622` — «deliberately NOT done») и 13 (compose-унификация) — после решения по 4. Разведка статусов всех шагов с файл:строка-доказательствами — в моём отчёте 08:46 UTC.
2. **Живое доказательство DRF-1385:** флаг `INTENT_RESOLUTION_FROM_TOOL_CHOICE_ENABLED=1` на пилоте (ставит главное окно) → один ход с инструментом → `orchestrator.intent_resolution.ok ... source=tool_choice` без вызова модели.
3. **Флаги, ждущие включения на пилоте:** `SKILL_CONFIDENCE_FLOOR_LIVE_ENABLED` (#1304), `LIVE_PATH_AI_METRIC_ENABLED` (#1309), `REPLAY_LIVE_CAPTURE_ENABLED` (#1314, после мержа). Все умолчанием ВЫКЛ — сами ничего не включат.
4. **Реестр ложных комментариев** — две записи в отчёте 09:37 UTC (concierge.py:503, :181). Формат «утверждает/опровергается» принят, веду дальше.
5. **Вопрос владельцу** (сформулирован 08:58): нужен ли реестр промптов вообще — с ценой обоих ответов. Дополнение за день: tenant-less зазор всплыл уже в ТРЁХ механизмах (promptreg, sentinel-метрики, replay-рекордер) — это усиливает пункт «нужен» или требует третьего варианта «реестр не нужен, но sentinel-паттерн узаконить».

Жду: вердикт CI по #1314, ответ на вопрос про PII-класс.

---

## 2026-08-25 21:18 UTC — Лимит 403 (говорю сразу). Шаг 4 PII спасён и в PR #1320. #1314 зелёный

### Лимит провайдера — упёрлась, как ты и предупреждал

Сабагент на шаге 4 упал дважды подряд: сначала двухчасовой таймаут, при резюме — **403 «usage limit for this billing cycle»**. Пятый случай в контуре, если считаешь. Важное: правило «коммить, когда есть что терять» сработало как задумано — резюм успел ровно на одно действие, **закоммитить работу** (`33899a9`), и умер на следующем запросе. Ничего не потеряно.

### Шаг 4 (PII) — PR #1320, догнан и проверен мной

https://github.com/AndreyDeveloper84/ai-bot-platform/pull/1320 — коммит `33899a9`, ветка от ba6cb07.

- **Что:** `evaluate_outbound` ловит хвост телефона (4 цифры) — только рядом с явным маркером контекста («номер…» с исключениями существительных, «тел.…», «заканчивается на…», «последние 4 цифры»). «код 4521» — сознательно не маркер. Verdict — замена ответа (BLOCK-класс), полные телефоны/email ловились и раньше. Калибровка против ложных срабатываний — по уроку DRF-1382 (редктор резал группы UUID).
- **Чем доказано (проверила сама после гибели сабагента, не с его слов):** новые тесты +55 строк, **ни один существующий тест не тронут** (дифф без удалённых assert'ов); прогон safety tests + outbound_guard + safety_parity — все зелёные; `ruff format --check apps/` 1287 чисто; `ruff check` чисто; `mypy` чисто.
- Ключи и brand-voice — НЕ в этой ветке (части 2 и 3 шага 4, отдельно). До заморозки их не начинаю без твоего слова: сабагенты лежат по лимиту, а руками я их к вечеру 26.08 не обещаю.

### #1314 (replay) — CI полностью зелёный

`pytest + ruff + mypy` — pass, 47м15с (в потолок 60). miniapp и replay-фикстуры зелёные. Мерж за тобой; флаг `REPLAY_LIVE_CAPTURE_ENABLED` — в твою очередь включений по одному.

### Статус очереди флагов на пилоте (напоминание, все ВЫКЛ по умолчанию)

```
INTENT_RESOLUTION_FROM_TOOL_CHOICE_ENABLED   #1296, в dev — живое доказательство снимаем вместе
SKILL_CONFIDENCE_FLOOR_LIVE_ENABLED          #1304, в dev
LIVE_PATH_AI_METRIC_ENABLED                  #1309, в dev
REPLAY_LIVE_CAPTURE_ENABLED                  #1314, зелёный, ждёт мержа
```

### Известное про мои силы

Сабагенты недоступны (403). Моя сессия работает, но без них темп — ручной. Если шаги 4.2/4.3 (ключи, brand-voice) должны войти в RC-1 — скажи, и я пойду руками в порядке PII→ключи→brand-voice; если нет, они уйдут в известные проблемы заморозки без запроса исключения, как ты и установил.

---

## 2026-08-25 21:22 UTC — Финальное перед заморозкой: реестр ложных комментариев + известные проблемы RC-1

Ничего сверх #1320 не начинаю — принято. #1320: miniapp и replay-фикстуры зелёные, `pytest + ruff + mypy` в прогоне; вердикт увижу по монитору, о провале скажу сразу.

### Реестр ложных комментариев — финальный список от окна VOICE

```
1. apps/orchestrator/concierge.py:503
   утверждает:  «the tool selection is in the orchestrator.concierge.* log line»
   опровергается: общего лога выбора инструмента в модуле нет — только веточные
                  логи отказов (no_criteria, no_master, forced_retry). Найдено 25.08
                  при проверке оценки «резолвер бесплатно — выбор уже записан».
   статус: комментарий врёт и сейчас (ветка #1296 его не трогала).

2. apps/orchestrator/concierge.py:181
   утверждает:  «the replay recorder writes ReplayTrace rows only on the live path»
   опровергается: до #1314 рекордер на живом пути не звался ВООБЩЕ (только
                  deprecated pipeline и оффлайн-раннер).
   статус: с мержем #1314 утверждение СТАЛО правдой — земля сместилась под
           комментарием; при чистке реестра перечитать, возможно уже не лжёт.
```

Формулировка класса (твоя, поддерживаю обеими находками): имя или описание обещает больше, чем делает код. Две записи у меня против тринадцати у окна сверки — свод за тобой после RC-1.

### Известные проблемы RC-1 (мой текст, с ценой)

**1. Сторож исходящего не проверяет ключи/API-секреты в тексте ответа (шаг 4.2).**
Что: `evaluate_outbound` после #1320 покрывает medical/promise/contact/PII-телефоны; ключи и токены в исходящем тексте не детектируются (паттерны есть в неподключённом `post_check.py`).
Цена закрытия: ~0.5-1 чел.-день (та же механика, что #1320: паттерны из post_check, калибровка, красный тест первым).
Цена незакрытия: ответ бота с утёкшим ключом доедет до клиента без препятствий; живой пары «утечка того же размера» на 26.08 не найдено — риск потенциальный, не подтверждённый.

**2. Сторож исходящего не проверяет brand-voice (шаг 4.3).**
Что: запрещённые формулировки бренда (списки есть в скилловых BrandVoiceConfig и в `post_check.py`) в исходящем тексте не проверяются.
Цена закрытия: ~1 чел.-день + решение, чей список канонический (после #1297 персона сведена, tone/forbidden у faq/booking остались inline).
Цена незакрытия: тон ответов держится только промптом, кодом не ограничен; класс «тон», не «данные».

**3. post_check остаётся построенным-неподключённым до завершения шага 4.**
Что: после переноса всех трёх классов в `evaluate_outbound` `post_check` надо удалить или пометить «не подключать» (решение 25.08), иначе — четвёртый соблазн класса promptreg.
Цена: включается в шаг 4.3.

### Что ещё открыто после заморозки (для подхвата, полный список в записи 17:06 UTC 25.08)

- Флаги на пилоте — по одному: `INTENT_RESOLUTION_FROM_TOOL_CHOICE_ENABLED` (живое доказательство снимаем вместе), `SKILL_CONFIDENCE_FLOOR_LIVE_ENABLED`, `LIVE_PATH_AI_METRIC_ENABLED`, `REPLAY_LIVE_CAPTURE_ENABLED`.
- DRF-1209 остаток: шаг 5 (memory-snapshot, 2-4 дня), шаги 9-строгий и 13.
- Вопрос владельцу про реестр промптов — расширен твоим третьим вариантом (узаконить sentinel-паттерн).
- Идея «шов должен терять поля шумно» — у тебя на заведение.

Готова к заморозке. Монитор на REPLY_VOICE.md оставляю включённым — если #1320 упадёт на CI или понадобится блокер-класс, я здесь.

---

## 2026-08-25 22:08 UTC — #1320 зелёный целиком

`pytest + ruff + mypy` — pass, 41м25с (в потолок); miniapp и replay-фикстуры зелёные. Мерж за тобой. Открытых PR у окна не осталось — инвентарь к заморозке чистый полностью.

---

## 2026-08-25 22:15 UTC — #1320 в dev. Окно сдало всё

#1320 (шаг 4 PII) смержен 22:08:53 UTC. Все шесть PR окна в `dev`: #1296 (резолвер по выбору консьержа), #1297 (голос из одного места), #1304 (confidence-floor), #1309 (метрика живого пути), #1314 (replay на живом пути), #1320 (PII в evaluate_outbound). Открытой работы нет. К заморозке готова.

---

## 2026-08-26 — Закрытие окна подтверждено

Принято: шесть PR в `dev`, инвентарь чистый, флаги включаешь ты по одному после выкладки. Жду твоего зова на живое доказательство третьего пути (`source=tool_choice` без вызова модели — снимаем вместе). Монитор на REPLY_VOICE.md включён, окно в режиме заморозки: только блокер-классы.
