# Cross-System Contract Matrix

> **Status:** DRAFT v2 — W3 security columns landed 2026-06-02; pending tech-lead review.
> **Date:** 2026-06-01 (initial) · 2026-06-02 (W3 security pass)
> **Gap-registry item:** M-P0-1 (Block A deliverable)
> **Owner:** tech-lead. The `🔒 Security review (W3)` column in every table is W3's authoritative call — `OK` / `RISK` / `GAP` plus actionable note + cross-links to open trackers.

## Purpose

This is the single authoritative registry of every contract/seam between the three systems:

- **`ai-bot-platform`** — the AI backbone (this repo). Owns channel identity, AI memory, conversations, skills, tools, KB/RAG, channels, eventbus consumers, tenancy runtime, observability.
- **`beautygo_backend`** = the **"Ayla djangoproject"** — canonical identity / booking / payments / catalog / reviews / nutrition. Source of truth per ADR-0009.
- **`ayla-ai-core`** — the shared, pinned Python AI library used by both consumers.

It is the stabilizing backbone that stops the three systems drifting apart. Every row is grounded in repo evidence (file + section quoted). Where a contract is undefined, undiscoverable in code, or flagged as drift by the source docs, the row is explicitly marked **DRIFT / MISSING / TBD** rather than papered over.

### Scope

3 systems, MVP surface. REST seams (Section A), the 12 MVP domain events (Section B), auth & service-to-service (Section C), env/config (Section D), and open-P0 status (Section E).

### How to read

- **Direction** is from the perspective of the caller → callee.
- **Owner service** is the canonical home per ADR-0009 §Domain ownership matrix.
- **Status** = `stable` (contract locked + code/tests exist), `DRIFT` (a real divergence the source flags), `MISSING` (no code/contract found), `TBD` (under-specified, needs a follow-up).
- **`🔒 Security review (W3)`** = W3 verdict + actionable note. Vocabulary:
  - **OK** — verified against threat model on `dev`, no actionable security gap today
  - **RISK** — known weak spot, exploitable today bounded by mitigations OR pilot-acceptable but pre-pilot tightening recommended
  - **GAP** — contract / code missing or under-specified; cannot verify; block on referenced ADR / ticket
  - Severity per memory `feedback_severity_discipline_rubric` 4-tier (MUST_FIX_PRE_MERGE / MUST_FIX_PRE_PILOT / FOLLOW_UP / NICE_TO_HAVE)

### Authoritative sources (read 2026-06-01, all on `dev` @ `5f925b56`)

- `docs/architecture/event-contract.md` — 12 MVP events, envelope, delivery, HMAC, PII rules.
- `docs/architecture/jwt-contract.md` (v1.3) — token taxonomy, claims, RS256/JWKS, `tenant_id` = active-tenant semantics, s2s consent-binding.
- `docs/adr/ADR-0009-ayla-split-domain-architecture.md` — domain ownership matrix, hard rules, mandatory event contract.
- Code: `apps/integrations/ayla/` (`profile_client.py`, `recommendations_client.py`, `nutrition_client.py`, `user_proxy.py`), `apps/integrations/ayla_payments/client.py`, `apps/eventbus/` (`ingest_security.py`, `consumers/*.py`).

> **Doc-gap note:** The two audit docs named in the M-P0-1 brief — `docs/architecture/api-spec-contract-drift-audit.md` and `docs/architecture/unified-system-architecture-audit.md` — **do not exist on `dev`** (only `event-contract.md`, `jwt-contract.md`, `event-consumers.md` are present under `docs/architecture/`). P0 statuses in Section E are therefore reconstructed from ADR-0009 + the contract docs + this session's knowledge, NOT from those audits. **The audits are themselves a gap (see Section E / follow-ups).**

---

## Section A — REST contracts (bot-platform ↔ beautygo / Ayla)

All endpoints below are grounded in actual client code in `apps/integrations/`. No endpoint is invented; where the brief asked for a contract that has no client, it is listed as **MISSING**.

> **⚙️ S0-B / S0-C reconciliation (2026-07-03) — read before trusting a row below.**
> The auth/path columns in this section were written pre-S0-B and several are now stale. S0-B (PR #1071, #978/#1048/#1050) **unified s2s auth and fixed the paths**: profile + recommendations + payments + booking authenticate with `Authorization: Bearer {AYLA_INTERNAL_API_TOKEN}` (the single Bearer Ayla validates); nutrition uses `X-Service-Token: {NUTRITION_SERVICE_TOKEN}`. The conflated `AYLA_SERVICE_TOKEN` Bearer **never existed on Ayla's side and is removed from code** (§C row updated). Paths corrected: profile → `GET /api/v1/internal/users/{id}/`; recommendations → `POST /api/v1/internal/me/catalog/recommendations/` (previously missing the `/api/v1` prefix).
>
> **G-Contract is now closed by a CI gate.** `apps/integrations/ayla/tests/test_contract_route_table.py` (S0-C) exercises every bot Ayla client through its real code and fails the build on ANY drift of (method, path, auth header + secret) vs Ayla's internal route-table — it runs network-free on every push. That test's `ROUTE_TABLE` is the machine-checked mirror of the table below; **when the two disagree, the CI test is authoritative** and this doc must be updated to match. Live end-to-end confirmation is the nightly staging round-trip (#1079).

| Contract / endpoint | Direction | Owner service (ADR-0009) | Request summary | Response summary | Auth model | Env vars | Covering tests/fixtures | Status | P0 ref | 🔒 Security review (W3) |
|---|---|---|---|---|---|---|---|---|---|---|
| `POST /api/v1/payments/create` | bot-platform → Ayla | Ayla (payments) | `{amount_rub:"X.XX", description, kind, recipient_name, buyer_email}` + header `Idempotence-Key: <uuid>`; only `kind="certificate"` exercised today | `{payment_id, checkout_url, status}` (YooKassa-hosted checkout URL) | `Authorization: Bearer {AYLA_INTERNAL_API_TOKEN}` | `AYLA_BASE_URL`, `AYLA_INTERNAL_API_TOKEN`, `AYLA_PAYMENTS_TEST_MODE` | `apps/integrations/ayla_payments/tests/` (test-mode short-circuit + mocked HTTP) | stable | — | **RISK** — static-bearer auth-fragmentation (see §C row 5). No per-user binding (leaked secret = full s2s impersonation). Idempotence-Key correctly prevents replay double-charge. Founder verdict B1: bot-platform doesn't create payments pre-pilot (Ayla canonical only) → endpoint scope shrinks; observe-only at pilot. FOLLOW_UP: deprecate `create_payment` client method post-B2-defer per memory `payment_creation_ownership`. |
| Payment **retry** | bot-platform → Ayla | Ayla (payments) | No dedicated retry endpoint. Retry = re-POST `/payments/create` with the SAME `Idempotence-Key` (returns same Payment row). urllib3 `Retry(total=3, status_forcelist=[502,503,504])` on the client. | same as create | as above | as above | partial (idempotence-key asserted in client tests) | DRIFT | — | **RISK** — same static-bearer surface as create. Idempotence-Key correctly dedup-protects retry replay. urllib3 Retry on 5xx only is right (no auto-retry on 4xx). FOLLOW_UP: explicit retry-budget cap in client (memory `feedback_adversarial_rounds_convergence` — drill-style retry caps). |
| `POST /api/v1/internal/me/catalog/recommendations/` | bot-platform → Ayla | Ayla (catalog) | body forwarded as-is (`lat`/`lon`/`goal`/`tenant_history`); pass-through, no shape gate on bot side | parsed JSON object, pass-through | `Authorization: Bearer {AYLA_INTERNAL_API_TOKEN}` **+** `X-External-User-ID: bot:{channel}:{id}` (no client JWT forwarded) | `AYLA_BASE_URL`, `AYLA_INTERNAL_API_TOKEN` | `apps/integrations/ayla/tests/` (incl. `test_contract_route_table.py`) | stable (S0-B #1048/#1071) | — | **RISK** — *(S0-B #1071 fixed the two items originally flagged here: now `Bearer {AYLA_INTERNAL_API_TOKEN}` (not the phantom AYLA_SERVICE_TOKEN) and path `/api/v1/internal/...` (prefix restored)).* Remaining: `X-External-User-ID` is a bot-supplied identity claim with NO consent binding per `jwt-contract.md` §5.4 spec; pass-through body shape uninspected bot-side = trust delegated entirely to Ayla. PRE_PILOT: drive s2s-auth ADR (§5.4 RS256 consent-bound token). |
| `GET /api/v1/internal/users/{user_id}/` (profile fetch) | bot-platform → Ayla | Ayla (identity/PII) | path `user_id` (UUID) | closed-shape parse: **only** `display_name`, `avatar_url` retained (PII §7) | `Authorization: Bearer {AYLA_INTERNAL_API_TOKEN}` | `AYLA_BASE_URL`, `AYLA_INTERNAL_API_TOKEN` | `apps/integrations/ayla/tests/` (incl. `test_contract_route_table.py`, `test_profile_client.py`) | stable (S0-B #978/#1071) | — | **OK with auth-fragmentation flag** — closed-shape parse honors PII §7 (only `display_name` + `avatar_url`; phone/email/birthday DROPPED on response parse). Static bearer per §C row 5 — s2s-auth ADR converges this. Cross-link **#978** (profile_client s2s URL canonicalization). |
| `POST /api/v1/nutrition/internal/scan/` | bot-platform → Ayla | Ayla (nutrition) | multipart `image` + optional `portion_multiplier` | `data.{scan_id, dish_name, confidence, portion_g, nutrition, provider}` | `X-Service-Token: {NUTRITION_SERVICE_TOKEN}` **+** `X-External-User-ID` | `AYLA_BASE_URL`, `NUTRITION_SERVICE_TOKEN` | `apps/integrations/ayla/tests/` | DRIFT | — | **RISK (HIGH after adversarial pass)** — third-style header (`X-Service-Token`), auth-fragmentation peak. Photo multipart upload PII-sensitive. **EXIF strip is CLIENT-SIDE ONLY** (miniapp browser, `apps/miniapp/src/screens/FoodScannerCaptureScreen.tsx:120-143`); `apps/integrations/ayla/nutrition_client.py:scan_photo()` (lines 304-345) passes `image_bytes` UNCHANGED to Ayla. **Channel-originated photo paths still cross border with EXIF**. Manual consent landed (#961). 152-ФЗ exposure: bounded only if miniapp is the SOLE pre-pilot ingress. **PRE_PILOT MUST_FIX:** (a) file W4 server-side strip ticket OR (b) explicitly accept channel-photo-ingress disabled pre-pilot in runbook. |
| `POST /api/v1/nutrition/internal/food-log/` | bot-platform → Ayla | Ayla (nutrition) | `{meal_type, portion_multiplier, scan_id?, dish_name?}` + optional `X-Idempotency-Key` | `data.{id, dish_name, meal_type, calories}` | `X-Service-Token` + `X-External-User-ID` | `AYLA_BASE_URL`, `NUTRITION_SERVICE_TOKEN` | `apps/integrations/ayla/tests/` | DRIFT | — | **RISK** — `X-Service-Token` style + write endpoint with OPTIONAL `X-Idempotency-Key`. **GAP**: idempotency key should be REQUIRED on write paths (mirror pattern of `Idempotence-Key` on /payments/create). Without it, network retry can double-log a meal. FOLLOW_UP: require idempotency key + reject if absent. |
| `GET /api/v1/nutrition/internal/summary/` | bot-platform → Ayla | Ayla (nutrition) | query `date?`, `with_comment?` | daily totals + goals + entries (+ optional `ai_comment`) | `X-Service-Token` + `X-External-User-ID` | `AYLA_BASE_URL`, `NUTRITION_SERVICE_TOKEN` | `apps/integrations/ayla/tests/` | stable | — | **OK** — read-only endpoint, no PII at envelope (numerical totals + goals + opaque entry IDs). Static `X-Service-Token` per §C row 5 — converged by s2s-auth ADR. `ai_comment` is server-generated text — verify upstream that it cannot leak cross-tenant fragments (Ayla-side concern). |
| `GET/POST /api/v1/nutrition/internal/profile/` | bot-platform → Ayla | Ayla (nutrition) | GET none / POST partial-or-full profile dict; norms computed server-side under `data.norms.*` | profile + BMR/norms + `goal_overridden_by`; 200 `exists=false` or 404 = no profile | `X-Service-Token` + `X-External-User-ID` | `AYLA_BASE_URL`, `NUTRITION_SERVICE_TOKEN` | `apps/integrations/ayla/tests/` | stable | — | **RISK** — profile WRITE crosses tenant boundary on bot-supplied `X-External-User-ID` with no consent binding. Same structural concern as G5.1 (#925) on miniapp_api side — REST trust boundary lacks live `TenantUserRelationship` re-check. PRE_PILOT: belongs in the s2s-auth ADR scope. |
| `POST/DELETE/GET /api/v1/nutrition/internal/water/*` | bot-platform → Ayla | Ayla (nutrition) | add `{ml, beverage_slug?, ts?}` (+`X-Idempotency-Key`), undo `…/{entry_id}/`, today `…/today/` | water entry / today totals; coefficient applied server-side | `X-Service-Token` + `X-External-User-ID` | `AYLA_BASE_URL`, `NUTRITION_SERVICE_TOKEN` | `apps/integrations/ayla/tests/` | stable | — | **OK** — additive write with `X-Idempotency-Key` (this endpoint has it, food-log does not — inconsistency). PII low (volume + beverage slug + ts). DELETE by entry_id is canonical-row scoped server-side. |
| `GET /api/v1/nutrition/internal/deficits/` + `…/insights/cross_domain/*` | bot-platform → Ayla | Ayla (nutrition) | deficits `?days=N`; insight GET/seen/dismiss/convert (`convert` carries `appointment_id`) | hint (prompt-injection sanitized client-side) / insight object | `X-Service-Token` + `X-External-User-ID` | `AYLA_BASE_URL`, `NUTRITION_SERVICE_TOKEN` | `apps/integrations/ayla/tests/` | stable | — | **OK** — client-side prompt-injection sanitization on the `hint` field (good defense; bot-side LLM never sees raw Ayla string). Insight payload opaque IDs. Note: cross-domain insight safety gap (memory `cross_domain_insight_safety_gap`) — cards REMOVED from MVP, post-pilot Alpha audit required if reintroduced. |
| Slots lookup (`GET /api/v1/specialists/{id}/slots`) | bot-platform → Ayla | Ayla (catalog/schedule) | ADR-0009 §Hard rule #5 + §AI-initiated booking flow name this path, but **no client exists in `apps/integrations/`**. | — | TBD (presumably s2s bearer) | TBD | none found | MISSING | — | **GAP** — no client, no auth model defined. Block on s2s-auth ADR (§C row 5) before implementing — otherwise it'll land as a 4th auth-fragmentation style. PRE_PILOT only if AI-initiated booking flow ships pre-pilot; otherwise post-pilot. |
| Booking **create** (`POST /api/v1/appointments`) | bot-platform → Ayla | Ayla (booking) | named in ADR-0009 §AI-initiated booking flow + §Hard rule #5; **no REST client found in code.** Booking state reaches bot-platform only via the `booking.*` events (Section B). | — | TBD | TBD | none found | MISSING | — | **GAP** — same blocker as slots: no s2s-auth pattern locked → implementing now would freeze the wrong shape. Tenant-binding contract for this endpoint MUST verify `TenantUserRelationship` server-side (memory `identity_bridging_pattern`). |
| Booking **cancel** / **reschedule** | bot-platform → Ayla | Ayla (booking) | bot-platform must call Ayla REST to mutate (ADR-0009 §The contract is one-way; bot never writes Ayla state directly). **No client found.** Cancel/reschedule are observed inbound only via `booking.cancelled` / `booking.rescheduled` events. | — | TBD | TBD | none found | MISSING | — | **GAP** — most urgent of the three MISSING booking endpoints. Current miniapp_api workaround (G5.1 #925 + breadth-expansion #968) directly writes booking projection — the proper fix IS this REST surface landing. Cross-link **#925 / #968**. PRE_PILOT priority. |
| Internal **events ingest** `POST /api/v1/internal/events/ingest` | Ayla → bot-platform | bot-platform (eventbus) | single event envelope per POST (no batching MVP); see Section B + C | HTTP 200 / 401 / 422 / 400 / 500 per `event-contract.md` §8 | HMAC-SHA256 over raw body (`X-Ayla-Event-Signature`) + `X-Ayla-Event-Timestamp` ±300s | `EVENT_INGEST_HMAC_SECRET` | `apps/eventbus/tests/` (`ingest_security`) | stable | P0-4 (delivery) | **OK** — HMAC-SHA256 over raw body + ±300s timestamp + constant-time compare (`apps/eventbus/ingest_security.py:verify_signature`). Defence-in-depth on top of envelope dedup: per-event NotificationDispatchDedupe (#927/#979 W3 §H.3 APPROVED), PaymentTerminalDedupe (Round-2 NEW-1 tenant scope), ReviewProcessedDedupe. Quarterly rotation per §D. |

**Section A drift / gap callouts:**
- **DRIFT (auth inconsistency, HIGH) — PARTIALLY RESOLVED by S0-B (#1071):** *(original W3 finding, 2026-06-02)* three s2s auth conventions coexisted — `Bearer {AYLA_INTERNAL_API_TOKEN}` (payments), `Bearer {AYLA_SERVICE_TOKEN}` (recommendations, profile), `X-Service-Token: {AYLA_SERVICE_TOKEN}` (nutrition). **S0-B update:** the phantom `AYLA_SERVICE_TOKEN` Bearer is gone; recommendations + profile now use `Bearer {AYLA_INTERNAL_API_TOKEN}`, nutrition uses `X-Service-Token: {NUTRITION_SERVICE_TOKEN}`. Remaining by design = 2 conventions for 2 distinct auth models (internal-Bearer vs nutrition-service-token), CI-gated by `test_contract_route_table.py`. The `jwt-contract.md` §5.4 RS256 consent-bound s2s token remains the post-pilot convergence target.
- ~~**DRIFT:** recommendations endpoint sits under `/internal/...` while everything else is `/api/v1/...`~~ **RESOLVED (S0-B #1048/#1071):** recommendations is now `POST /api/v1/internal/me/catalog/recommendations/` (the missing `/api/v1` prefix was the bug); all Ayla URLs go through `AylaUrlBuilder` which owns the prefix.
- **MISSING:** slots lookup, booking create, booking cancel, booking reschedule have **no REST client in this repo** despite being mandated by ADR-0009. They are the canonical AI-initiated-booking write path and must exist before that flow ships.

---

## Section B — Events (Ayla → bot-platform), the 12 MVP events

Producer is always **Ayla djangoproject** (outbox → HMAC-signed HTTP POST). Consumer handlers all live in `apps/eventbus/consumers/`. All events are `event_version: 1`. Envelope-level dedupe is `IngestDedupe(event_id)` at the dispatcher (`event-contract.md` §5); additional per-event idempotency keys noted below.

| event_name | version | Producer | Consumer handler (`apps/eventbus/consumers/...`) | Idempotency key | Side-effects | Fixture | Status | P0 ref | 🔒 Security review (W3) |
|---|---|---|---|---|---|---|---|---|---|
| `booking.created` | 1 | Ayla | `booking.py::handle_booking_created` (#442) | `event_id` (IngestDedupe) + `RemoteBookingProxy.appointment_id` upsert + `BookingReminder(ayla_appointment_id,tenant,kind)` | upsert `RemoteBookingProxy`; schedule T-24h/T-2h reminders; set `Conversation.last_booking_at`; emit internal `booking_created` | `apps/eventbus/tests/` | stable | — | **OK** — IngestDedupe (envelope) + `(appointment_id,tenant,kind)` reminder key; closed-shape payload; no raw PII at handler. Tenant from envelope verified upstream via `assert_envelope_tenant_authorized`. |
| `booking.cancelled` | 1 | Ayla | `booking.py::handle_booking_cancelled` (#442) | `event_id` + `proxy.last_synced_event_id` | proxy → CANCELLED; cancel PENDING reminders; emit `booking_cancelled`. Out-of-order (cancel-before-create) is **dropped**, not stubbed (DoS defense) | `apps/eventbus/tests/` | stable | — | **OK** — IngestDedupe + `proxy.last_synced_event_id`. Out-of-order drop is the right DoS defense (memory `feedback_adversarial_rounds_convergence` — fail-closed pattern). |
| `booking.rescheduled` | 1 | Ayla | `booking.py::handle_booking_rescheduled` (#442) | `event_id` + `proxy.last_synced_event_id` | move `start_at`/`end_at` (preserve duration); re-peg reminders; refresh `Conversation.last_booking_at`; emit `booking_rescheduled`. Refuses non-positive-duration proxy | `apps/eventbus/tests/` | stable | — | **OK** — IngestDedupe + `proxy.last_synced_event_id`; non-positive-duration refusal closes a corruption vector. |
| `booking.confirmed` | 1 | Ayla | `booking.py` (#442) | `event_id` + `proxy.last_synced_event_id` | hold/confirmation signal emitted on the payment-hold transition; **adopted into the contract as a v1 event in Block B-1** (replaces the deferred `payment.authorized` as the hold marker) | `apps/eventbus/tests/` | emitted/aligned (adopted B-1) | — | **OK** — B-1 adoption clean; replaces deferred `payment.authorized` as hold marker (founder Block B-1 align 2026-06-01). |
| `booking.completed` | 1 | Ayla | `booking.py::handle_booking_completed` (#442) | `event_id` + `proxy.last_synced_event_id` | proxy → COMPLETED + `completed_by` mirrored; emit `booking_completed`. **Contract steps 3 (post-visit review skill) + 4 (RFM/sentiment) deferred to follow-up tickets** | `apps/eventbus/tests/`, `apps/bookings/tests/test_completed_by_gate.py` | DRIFT | — | **RISK (deferred-scope)** — review skill + RFM/sentiment side-effects DEFERRED. Current handler is no-effect-beyond-status + actor mirror; no security risk in the truncated form. Post-pilot tickets must re-audit when re-enabled — review skill touches user-facing copy (152-ФЗ class) and MUST gate on `completed_by` (owner decision 30.08): a visit closed by the 3-hour auto-close earns no review request. |
| `payment.authorized` | 1 | Ayla | `payment.py::handle_payment_authorized` (#443) | `event_id` + `Conversation.last_payment_event_id` | set `Conversation.pending_payment_id`; no DM | `apps/eventbus/tests/` | DEFERRED post-pilot (hold = `booking.confirmed`) | — | **OK (DEFERRED)** — hold marker moved to `booking.confirmed` per B-1; this handler is dormant post-flag. No active exposure. |
| `payment.captured` | 1 | Ayla | `payment.py::handle_payment_captured` (#443) | `event_id` + `PaymentTerminalDedupe(tenant_id,payment_id,CAPTURED)` | clear pending slot (if matches); set captured ts; reset failure counter; emit `loyalty_bonus_eligible`; no DM | `apps/eventbus/tests/` | DRIFT being fixed in B-1 (Ayla emits `payment.confirmed` today → rename) | — | **OK** — tenant-scoped `PaymentTerminalDedupe (tenant_id, payment_id, CAPTURED)` per Round-2 NEW-1: cross-tenant pre-claim DoS blocked. Loyalty fan-out idempotency-protected. |
| `payment.failed` | 1 | Ayla | `payment.py::handle_payment_failed` (#443) | `event_id` + `Conversation.last_payment_event_id` (under row lock) | map reason → closed enum; increment `consecutive_payment_failures`; clear pending; dispatch `payment_failed` skill **exactly at threshold** (`PAYMENT_FAILED_HANDOFF_THRESHOLD`) via `on_commit` | `apps/eventbus/tests/` | NEW — Ayla emit being added in B-1, bot consumer pending Gamma | — | **OK post-#979** — NotificationDispatchDedupe tenant-scoped exactly-once guard for outbound DM (claim-before-send + tenant-scoped key + redelivery-no-dup test). Cross-link **#927/#979** (W3 §H.3 APPROVED 2026-06-02). Channel-side SETNX (Variant 3) + G6 import boundary = post-pilot per founder decision. |
| `payment.refunded` | 1 | Ayla | `payment.py::handle_payment_refunded` (#443) | `event_id` + `PaymentTerminalDedupe(tenant_id,payment_id,REFUNDED)` | set refunded ts; emit `loyalty_refund_reverse`; customer DM owned by Ayla, not here | `apps/eventbus/tests/` | stable | — | **OK** — mirrors captured: tenant-scoped `PaymentTerminalDedupe (tenant_id, payment_id, REFUNDED)`. DM ownership cleanly on Ayla side (no consumer-skill-channel chain to dedup). |
| `review.created` | 1 | Ayla | `reviews.py::handle_review_created` (#445) | `event_id` + `ReviewProcessedDedupe(tenant_id,review_id)` | derive sentiment from rating; set `low_rating_flag` (sticky) on `ClientProfile`; never fetch review text (PII §7). Degrades pre-W4 ClientProfile migration via `hasattr` | `apps/eventbus/tests/` | DRIFT | — | **OK** — tenant-scoped `ReviewProcessedDedupe`. Review TEXT never fetched (PII §7 honored). Pre-W4 migration degrades gracefully via `hasattr`. FOLLOW_UP: confirm `low_rating_flag` sticky semantics intentional (current = once-flagged, always-flagged — operator triage class). |
| `service.updated` | 1 | Ayla | `catalog.py::handle_service_updated` (#444) | `event_id` + idempotent `cache_version` bump | bump `CatalogService.cache_version` (tenant-scoped); flip `is_active` via `previous_values` negation. **`duration`-change slot-cache invalidation is a no-op / FOLLOW_UP — no scheduling cache exists** | `apps/eventbus/tests/` | DRIFT | — | **OK** — idempotent tenant-scoped `cache_version` bump. Slot-cache no-op = FOLLOW_UP only (no security impact — there's no cache to leak). |
| `master.schedule.updated` | 1 | Ayla | `schedule.py::handle_master_schedule_updated` (#445) | `event_id` + additive `cache_version` bump | bump `CatalogMaster.cache_version`; per-date granularity NOT used (global counter per tech-lead Q3). **No active slot cache reader yet — forward-compat signal only** | `apps/eventbus/tests/` | DRIFT | — | **OK** — additive `cache_version` bump; forward-compat signal only. No active read path → no current exposure. |
| `user.profile.updated` | 1 | Ayla | `identity.py::handle_user_profile_updated` (#446) | `event_id` + value-comparison (no-op if unchanged) | sync `display_name`/`avatar_url` only into `BotUser`; phone/email/birthday/language cause zero traffic (PII §7). `tenant_id` may be **null** (only such event). `avatar_url` write gated on W4 migration | `apps/eventbus/tests/` | DRIFT | — | **OK with audit ask** — PII-restricted to `display_name` + `avatar_url` (PII §7 honored). **Verify no consumer code path asserts `tenant_id` non-null** — this is the ONLY MVP event allowing null tenant; a downstream skill/handler asserting non-null would 500 on global-scope updates. FOLLOW_UP: assertion sweep + explicit null-handling test. |

**Section B drift / gap callouts:**
- **DRIFT (deferred side-effects):** `booking.completed` (review-skill + RFM), `review.created` (ClientProfile fields behind W4 migration), `service.updated` & `master.schedule.updated` (slot-cache invalidation is a no-op because `apps/scheduling` has no cache layer), and `user.profile.updated` (`avatar_url` field behind W4 migration). These handlers exist and are registered, but parts of their contract-defined side-effects are stubbed/deferred — flagged so they are not mistaken for fully closed.
- All 12 events are registered and have a handler — no event is MISSING.

---

## Section C — Auth & service-to-service

| Mechanism | Type / claims | Issuer → Verifier | Spec | Code | Status | 🔒 Security review (W3) |
|---|---|---|---|---|---|---|
| `access` JWT | 15-min; `sub`, `aud:["ai-bot-platform"]`, `ayla.{token_type,tenant_id,relationships,scope,user_role,contract_version}` | Ayla issues (RS256) → bot-platform verifies via JWKS | `jwt-contract.md` §2–§3, §8 | bot-platform verifier middleware **not yet in `apps/identity`** (ticket filed post-merge per doc §11) | TBD | **GAP — PRE_PILOT** — verifier middleware not yet in `apps/identity`. Bot REST surface CANNOT enforce JWT claims at edge today. Until middleware lands, REST trust boundary relies on s2s bearer + bot-supplied `X-External-User-ID` (no cryptographic user-identity proof at bot side). **Partial coverage exists via channel/init-data gates** — `apps/miniapp_api/views.py` + `apps/master_api/views.py` use `@require_init_data` (MAX-cryptographically-verified) for their surface, and `apps/tenancy/middleware.py` provides scope binding — but neither is the JWT verifier specified in `jwt-contract.md`. Block on s2s-auth ADR + middleware ship. Tied to **#925 / #968** (G5.1 + breadth-expansion) — same root cause: REST surface needs live tenant-relationship re-check. |
| `refresh` JWT | 90-day; rotates on use, blacklist on rotation; never reaches bot-platform | Ayla only | `jwt-contract.md` §2, §7 | n/a (Ayla side) | TBD | **OK (out-of-scope)** — refresh token never reaches bot-platform per spec. No bot-side surface to attack. Ayla-side concern. |
| `anonymous` JWT | 30-day; narrow scope (`provider:directory:read`, `memory:write:green_zone_only`, `chat:anonymous`); `tenant_id:null` | Ayla → bot-platform | `jwt-contract.md` §2, §6 | verifier TBD | TBD | **GAP — PRE_PILOT** — same blocker as access JWT: verifier TBD. Anonymous-scope flow (provider directory read + green-zone memory write + anonymous chat) unverifiable on bot side until middleware lands. Lower urgency than access (narrow scope by design) but same s2s-auth ADR dependency. |
| `service_to_service` JWT (spec) | 5-min RS256; MUST embed nested user-token + consent binding (`user_on_behalf_of`, scope-subset, jti single-use dedup) | each side own keypair | `jwt-contract.md` §4.4, §5.4 | **NOT implemented** — current s2s is plain bearer secret(s), see below | DRIFT | **GAP — PRE_PILOT** — `jwt-contract.md` §5.4 spec (RS256 + nested user-token + consent binding `user_on_behalf_of` + scope-subset + jti single-use dedup) NOT implemented. Current s2s is plain static bearer (row below). This is the **s2s-auth ADR driver**. Without §5.4, every s2s call is bot-impersonates-user with no consent proof; cross-tenant skill misroute could exfiltrate. |
| s2s bearer (actual) | static long-lived shared secret(s); two distinct secrets + two header styles | bot-platform → Ayla | clients in `apps/integrations/*` | `AYLA_INTERNAL_API_TOKEN` (payments, `Authorization: Bearer`); `AYLA_SERVICE_TOKEN` (recommendations/profile `Authorization: Bearer`; nutrition `X-Service-Token`) | DRIFT | **RISK (HIGH) — auth-fragmentation, PRE_PILOT driver** — TWO secrets (`AYLA_INTERNAL_API_TOKEN`, `AYLA_SERVICE_TOKEN`) × TWO header styles (`Authorization: Bearer`, `X-Service-Token`) = 3 active conventions across Ayla clients. No per-user binding (leaked secret = full s2s impersonation of any user under that tenant). No documented rotation cadence (compare §D HMAC's quarterly rotation). No consent binding per §5.4 spec. **Cross-link:** #928 (G2.1 skills→YClients direct = sibling auth-fragmentation pattern in third-party integration clients). **This is the single biggest s2s seam to converge.** Block A handoff: drives the s2s-auth ADR. |
| Event HMAC signing | HMAC-SHA256 over raw body; `X-Ayla-Event-Signature: sha256=<hex>` + `X-Ayla-Event-Timestamp` (±300s, constant-time compare) | Ayla → bot-platform ingest | `event-contract.md` §6.2 | `apps/eventbus/ingest_security.py` (`verify_signature`) | stable | **OK — gold-standard** — HMAC-SHA256 over raw body, ±300s timestamp window, constant-time compare. Quarterly rotation per §D. Verified in `apps/eventbus/ingest_security.py:verify_signature` + `apps/eventbus/tests/`. **Defence in depth above:** per-event dedup (IngestDedupe, PaymentTerminalDedupe Round-2 NEW-1, ReviewProcessedDedupe, NotificationDispatchDedupe #927/#979). This is the model the s2s-auth ADR should aspire to for REST (HMAC + replay window + dedup ledger). |
| `tenant_id` claim semantics | = **active** tenant, NOT ownership; null = global user scope; verifier must re-check `TenantUserRelationship` live | — | `jwt-contract.md` §5, ADR-0009 §Hard rule #6 | mirror table consulted in eventbus consumers (`assert_envelope_tenant_authorized`) | stable (event side); JWT verifier TBD | **RISK (mixed, adversarial-tightened)** — Eventbus side: `assert_envelope_tenant_authorized` re-checks `TenantUserRelationship` live per ADR-0009 Hard rule #6 **ONLY WHEN** (a) Sprint 1 #246 `TenantUserRelationship` model is importable AND (b) `EVENT_INGEST_TENANT_VERIFY_FAIL_OPEN=False` (`apps/eventbus/ingest_tenancy.py:119-169`). The FAIL_OPEN bridge logs + passes if either condition is missing — there is NO startup assertion that #246 imports successfully. **PRE_PILOT runbook MUST verify:** (i) `EVENT_INGEST_TENANT_VERIFY_FAIL_OPEN` is False in production settings, (ii) `from apps.identity.models import TenantUserRelationship` succeeds at boot. Otherwise the «live re-check» is silently bypassed and audit rows accumulate at the sampler threshold ladder. JWT REST side **GAP** until verifier middleware lands (see access JWT row). **Cross-link #925 (G5.1 miniapp_api booking-create cross-tenant leak) + #968 (G5.1 baseline contract expansion — transitions+feedback breadth)** — both have the same root cause: REST trust boundary needs the same live tenant-relationship re-check the eventbus consumers already have. PRE_PILOT priority for REST side; eventbus side requires the runbook gate above. |

**Section C callouts:** the JWT contract is documented (v1.3, heavily reviewed) but the **bot-platform verifier middleware is not yet in the repo**, and the **real s2s auth is a static bearer secret diverging from the §5.4 consent-bound RS256 spec**. Both are weak spots W3 should prioritize.

**W3 priority ladder (pre-pilot):**
1. **s2s-auth ADR** — converge the 3 conventions (rows 4 + 5) under §5.4 spec OR explicitly accept a pilot-bounded simplification with rotation cadence + consent-claim shim.
2. **JWT verifier middleware in `apps/identity`** — unblocks access + anonymous rows; closes the REST-side TenantUserRelationship re-check gap; structurally fixes #925 / #968.
3. **#925 / #968 fix landing** — the practical near-term defence-in-depth while (1) + (2) are in flight.

---

## Section D — Env vars / config registry

| Name | Owner | Meaning | Where set / read |
|---|---|---|---|
| `AYLA_BASE_URL` | bot-platform infra | **Host-only** base URL of Ayla djangoproject (`scheme://host[:port]`, **no `/api/v1`** — the builder inserts it; currently `gobeauty.site` infra; `api.ayla.app` deferred per ADR-0009 domain update) | read by all `apps/integrations/ayla*` clients via `settings`, normalised through `AylaUrlBuilder` (#1049) |
| `AYLA_INTERNAL_API_TOKEN` | bot-platform infra / shared | **The single s2s Bearer** (`Authorization: Bearer`). Used by **payments**, **booking**, **profile**, and **recommendations** (recommendations + profile migrated onto it in S0-B, #978/#1048/#1050, PR #1071). CI-gated by `test_contract_route_table.py`. | `apps/integrations/ayla_payments/client.py`, `apps/integrations/ayla/{booking,profile,recommendations}_client.py` |
| `NUTRITION_SERVICE_TOKEN` | bot-platform infra / shared | Nutrition `X-Service-Token` shared secret, named to match what Ayla validates. Falls back to legacy `AYLA_SERVICE_TOKEN` env **only when unset** (an explicit empty value is honoured). **Wired into `nutrition_client` in S0-B (#1050, PR #1071) — this is now the canonical name; setting only `NUTRITION_SERVICE_TOKEN` is sufficient.** | `config/settings/base.py`; consumed by `apps/integrations/ayla/nutrition_client.py` |
| `AYLA_SERVICE_TOKEN` | bot-platform infra / shared | **REMOVED from code (S0-B #1050, PR #1071)** — was a Bearer secret that does **not** exist on Ayla's side + a conflated nutrition secret. The settings *attribute* is deleted; the env var survives ONLY as the back-compat fallback source for `NUTRITION_SERVICE_TOKEN` in `config/settings/base.py`. No client reads it any more (enforced by `test_contract_route_table.py`'s secret-binding assertions). | `config/settings/base.py` (env fallback only) |
| `AYLA_PAYMENTS_TEST_MODE` | bot-platform | when True (default) `create_payment` returns a stub URL, no HTTP | `apps/integrations/ayla_payments/client.py` |
| `EVENT_INGEST_HMAC_SECRET` | shared (Vault, quarterly rotation) | HMAC secret for inbound event signature verification | `apps/eventbus/ingest_security.py` |
| `PAYMENT_FAILED_HANDOFF_THRESHOLD` | bot-platform | consecutive-failure count at which `payment.failed` triggers the customer handoff skill | `apps/eventbus/consumers/payment.py` |

**Section D callout — DRIFT (S0-A in progress, #1050):** the historical `AYLA_INTERNAL_API_TOKEN` / `AYLA_SERVICE_TOKEN` split is being converged. S0-A (this PR) declares the canonical pair — **`AYLA_INTERNAL_API_TOKEN`** (the single s2s Bearer) and **`NUTRITION_SERVICE_TOKEN`** (nutrition's `X-Service-Token`, matching Ayla's validated name) — and deprecates `AYLA_SERVICE_TOKEN` (Bearer half never existed on Ayla). The client rewiring (recommendations/profile → Bearer `AYLA_INTERNAL_API_TOKEN`; nutrition → `NUTRITION_SERVICE_TOKEN`) lands in **S0-B (#978/#1048/#1050)**. Migrating to the §5.4 RS256 consent-bound s2s token remains the post-pilot follow-up.

**Section D callout — URL builder (#1049):** `AYLA_BASE_URL` is now **host-only** and every Ayla URL is built through `apps/integrations/ayla/url_builder.py::AylaUrlBuilder`, which inserts the `api/v1` prefix and fails loud on: a base carrying a path/query/fragment/userinfo/backslash, a scheme-in-path (`https://…`/`//host`), a path re-stating `api/v1` (case-insensitive), or a `.`/`..` dot-segment. Request-path segments are percent-encoded so their contents cannot restructure the URL. `booking_client` is the reference consumer; the remaining clients route through it in S0-B. **Rollout:** `AYLA_BASE_URL` must be host-only (no `/api/v1`) — an old ambiguous value now fails loud at client construction instead of producing `/api/v1/api/v1/…`.

---

## Section E — Open P0 status

> Reconstructed from ADR-0009 + the contract docs + this session's knowledge. The originally-cited audit docs (`api-spec-contract-drift-audit.md`, `unified-system-architecture-audit.md`) are **absent from the repo**, so individual P0-1..P0-7 wordings could not be quoted verbatim. Treated as best-effort mapping — **verify against the real audits once they land.**

| P0 | Topic | Status (this session) | Evidence / note |
|---|---|---|---|
| P0-1 | Cross-system contract registry (this matrix, M-P0-1) | IN PROGRESS → addressed by this DRAFT v1 | this document |
| P0-4 | Outbox / event delivery reliability | IN PROGRESS (Block C) | ingest dispatcher, HMAC, dedupe, DLQ present (`apps/eventbus/`); delivery-side hardening ongoing |
| P0-5 | ayla-ai-core version drift | **CLOSED** (per A9 / version-drift; PR #176 + #935) | this session knows A9 / version-drift is CLOSED — `ayla-ai-core` pinned per ADR-0009 repo-roles |
| P0-2 | (topic TBD — audit absent) | UNKNOWN | could not pin — audit doc missing |
| P0-3 | (topic TBD — audit absent) | UNKNOWN | could not pin — audit doc missing |
| P0-6 | (topic TBD — audit absent) | UNKNOWN | could not pin — audit doc missing |
| P0-7 | (topic TBD — audit absent) | UNKNOWN | could not pin — audit doc missing |

---

## Follow-up gaps for tech-lead

1. **Missing audit docs** — recreate / locate `api-spec-contract-drift-audit.md` and `unified-system-architecture-audit.md`; Section E P0-2/3/6/7 cannot be pinned without them.
2. **Booking write path MISSING** — no REST client for slots lookup, booking create, cancel, reschedule despite ADR-0009 mandating them. Required for the AI-initiated booking flow. **W3 marks all three MISSING rows as GAP** — block on s2s-auth ADR.
3. **s2s auth convergence (W3 driver row 5 §C)** — unify `AYLA_INTERNAL_API_TOKEN` / `AYLA_SERVICE_TOKEN` and the `Authorization: Bearer` vs `X-Service-Token` split; migrate toward the §5.4 RS256 consent-bound s2s token. **PRE_PILOT priority** per W3 ladder.
4. **JWT verifier middleware** — not yet present in `apps/identity`; whole of Section C user-token verification is TBD in code. **PRE_PILOT priority** per W3 ladder.
5. **Deferred event side-effects** — close the stubbed parts of `booking.completed`, `review.created`, `service.updated`, `master.schedule.updated`, `user.profile.updated` (slot-cache layer + W4 migrations).
6. **W3 follow-ups surfaced during this pass (and during own adversarial refutation):**
   - `/api/v1/nutrition/internal/food-log/` — require `X-Idempotency-Key` (write endpoint, currently optional)
   - `user.profile.updated` — assertion sweep + null-tenant explicit test (only event allowing `tenant_id: null`)
   - `review.created` `low_rating_flag` sticky semantics — confirm intentional or document the operator-triage rationale
   - `/nutrition/scan/` photo cross-border — server-side EXIF strip is MISSING (`nutrition_client.py:scan_photo()` passes `image_bytes` unchanged); current strip is miniapp browser-only. **PRE_PILOT MUST_FIX:** ship W4 server-side strip OR codify that channel-photo-ingress is disabled pre-pilot.
   - **EVENT_INGEST_TENANT_VERIFY_FAIL_OPEN runbook gate** — flip runbook + pre-pilot operator checklist must verify (i) flag is False in production and (ii) `TenantUserRelationship` model is importable at boot. Without these, eventbus «live tenant re-check» silently bypasses.
