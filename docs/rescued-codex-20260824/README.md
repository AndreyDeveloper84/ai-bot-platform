# Спасено из `ai-bot-platform-codex`, 24.08.2026

## Откуда

Каталог `C:\Users\user\PycharmProjects\ai-bot-platform-codex` — **отдельный клон**, не рабочее дерево: в `git worktree list` его не было. Ветка `dev`, отставшая до **27 мая**. 494 МБ.

В нём лежало **21 несохранённое изменение** — двадцать не отслеживаемых markdown-файлов и правка одного теста. Нигде не закоммичено, нигде больше не существовало.

Каталог удалён после спасения.

## Что здесь

```
architecture/   аудиты архитектуры, дрейфа контрактов, индекс проекта,
                дорожная карта стабилизации          4 файла
screens/        описания экранов провайдера          14 файлов
documentation-index-and-gap-registry.md
docs.zip                                             2.3 МБ
test_recompute.patch    правка apps/identity/tests/test_recompute.py,
                        сохранена патчем, а не файлом: она сделана
                        поверх майского dev и на нынешний не ляжет
```

## Чем это не является

**Не канон.** Материал майской эпохи, до перехода на MAX-бота. Часть описаний экранов относится к отдельному мобильному приложению, а не к нынешнему Mini App.

**Не проверено на соответствие сегодняшнему коду.** Спасено ради того, чтобы не исчезло, а не потому что применимо.

Прежде чем на что-то здесь опираться — сверить с кодом. Файлы с суффиксами `.updated`, `.final`, `.smart-landing-*` были рабочими версиями одного и того же экрана, и какая из них последняя, по датам не восстанавливается.
