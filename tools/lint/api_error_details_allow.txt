# Места, которые ещё роняют `details` из тела отказа сервера.
#
# Формат: <путь от apps/miniapp>::<сколько непочиненных мест осталось>
#
# ЧИСЛО ТОЛЬКО УМЕНЬШАЕТСЯ. Починили одно — уменьшите на единицу; сторож
# краснеет и когда мест стало больше, и когда стало меньше, а число забыли.
# Пополнение списка — решение, а не побочный эффект правки.
#
# Откуда взялся долг (DRF-2373). Перепись по конструктору `new ApiError(`
# нашла 12 мест в 6 файлах — а не три клиента, как мы помнили. Из них:
#
#   * 3 уже передают `details` (api.ts — DRF-1708, admin-api.ts — DRF-2273,
#     master-api.ts — DRF-2373, тот провод, ради которого лист и заводился);
#   * 1 освобождён ПРАВИЛОМ, а не списком: экран строит `ApiError` из
#     литералов, разобранного тела отказа у него нет вовсе;
#   * 8 роняют — они здесь.
#
# Отдельно стоит отметить `admin-api.ts`: его «починили» в DRF-2273, и он
# всё ещё роняет поле в четырёх местах из пяти. Починка места не чинит
# правило — ровно поэтому появился сторож.
#
# Чинить все восемь в листе про подтверждение действия значило бы спрятать
# восемь правок за чужим заголовком. Долг назван числом и заведён отдельным
# листом — DRF-2439, с формой работы «сокращать под сторожем», а не «обойти
# восемь мест разом».

src/lib/admin-api.ts::4
src/lib/internal-chat-api.ts::1
src/lib/master-api.ts::1
src/lib/personal-data.ts::1
