# Долг ноль.
#
# Список пуст НАМЕРЕННО, и файл оставлен: он — место, куда пришлось бы
# вписывать новое место, если бы кто-то его завёл. Пустой файл читается
# как «все вызовы доносят details», а отсутствие файла — как «а был ли
# вообще долг»; это разные утверждения.
#
# Перепись DRF-2373: 12 вызовов new ApiError( в 6 файлах. Три передавали
# details (api.ts — DRF-1708, admin-api.ts — DRF-2273, master-api.ts —
# DRF-2373), один освобождён правилом (ошибка построена из литералов,
# разобранного тела отказа у неё нет вовсе), восемь роняли.
#
# DRF-2439 починил все восемь. Живой из них был ОДИН — загрузка фото
# профиля мастера: каталог присылал в details, что именно не так с фото,
# а человек видел общее «Каталог не принял профиль.». Семь остальных были
# пусты сегодня и починены, чтобы не стать настоящими молча.
