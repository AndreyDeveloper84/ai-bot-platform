# Objects in the pilot deploy tree NOT owned by the deploy user (DRF-1646).
#
# ─── WHICH BOX ──────────────────────────────────────────────────────────────
#
#   host       ruvds-o1mqo / 176.119.159.141   ← api-dev.gobeauty.site
#   tree       /home/taximeter/ai-bot-platform-dev
#   owner      taximeter, uid 1000
#   measured   2026-09-11T11:39:50Z at HEAD d8a1804d, by ssh, 0 unreadable
#
# The host is named because the first version of this file did not name it and
# was measured on the WRONG BOX — ruvds-l2wyz / 194.87.99.126 carries an
# abandoned checkout at the same path with the same compose project names. The
# path is not an address. What settles it is asking the live contour where it
# was launched from (`docker inspect ... project.working_dir`) plus DNS.
#
# ─── THE LEAK IS CLOSED. What this file now measures is the RESIDUE. ─────────
#
# Source fix landed in three steps and was verified by count, not by merge:
#   #1581  --user on docker compose run      → void: deploy ran as root, so
#                                              $(id -u) expanded to 0:0
#   #1586  every writing command via          → 4 deploys 10:23–10:31 wrote
#          sudo -u <tree owner>                 .git as owner; 0 fresh foreign
#   #1588  root hands dist/ back to the owner  → first full deploy 11:07: mini
#          on entry (self-healing)               app step passed, dist/ owner
#
# Measured after that first full deploy, 2026-09-11T11:39:50Z, HEAD d8a1804d:
#
#   fresh foreign files since 11:00      0
#   fresh foreign directories            0
#   fresh files written by the owner    25   ← the deploy did write; as owner
#
# So the per-deploy leak is ZERO. The numbers below are not a rate any more —
# they are what root left behind BEFORE the fix and nobody has rewritten since:
# git objects (immutable), unchanged working-tree files, static files that
# collectstatic found unmodified. They cannot shrink on their own; they need
# one cleanup by the owner, after which this file reads all zeros.
#
# The guard's job from here: fail if the residue GROWS. Growth now means the
# leak reopened — a new root-run step, a new manual root session — and that is
# precisely the event worth a red check.
#
# ─── STILL NOT MEASURED, stated so it is not read as measured ───────────────
#
#   collectstatic under --user   has written ZERO files across every deploy
#                                since the fix (all static unmodified). It is
#                                neither proven nor disproven. Proves itself
#                                on the first deploy that changes static.
#   .github/* under the owner    no PR has added a new directory since #1586.
#                                Proves itself on the first one that does.
#
# ─── THE TWO ROOT DIRECTORIES LEFT ──────────────────────────────────────────
#
#   .github/linear-reality-check           root 755, owner write REFUSED
#   .github/linear-reality-check/fixtures  root 755, owner write REFUSED
#
# Created 06:37 by root's checkout of a merged PR, before #1586. Harmless to
# the deploy (nothing writes there) but they are the residue's only
# directories, and a directory is what blocks — see DRF-1646's history. Part
# of the one cleanup.
#
# ─── THE NUMBERS — residue on 2026-09-11T11:39:50Z, 12 fewer than at 10:55 ────
#
#   .git/         303   objects 258 + refs/logs/index 45 — created by root
#                       before the fix; git never rewrites objects
#   staticfiles/  153   collectstatic skips unmodified files; root's stay
#   other         105   working-tree files unchanged since root's checkout,
#                       plus the two .github directories above
#   __pycache__/    0   measured, not assumed
#
# 561 total. Was 573; the 12 that vanished were apps/miniapp/dist/**,
# rewritten by the owner in the 11:07 deploy — the first residue to heal by
# itself, and proof that rewriting is what heals.

.git/ = 303
staticfiles/ = 153
__pycache__/ = 0
other = 105
